---
phase: 07-tooling-guardrail-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.jsonc
  - package.json
files_created:
  - biome.jsonc
autonomous: true
requirements:
  - PIPE-02
user_setup: []
must_haves:
  truths:
    - The repo has an explicit Biome configuration rather than relying on `biome ... .` defaults.
    - Biome is configured to parse the Tailwind v4 directives used in `src/styles/app.css`.
    - `bun run format` succeeds on the authored-source boundary without scanning generated output or aborting on the Tailwind CSS layer.
    - The repo has a clean non-mutating formatter-check path if pre-commit needs one.
  artifacts:
    - biome.jsonc
    - package.json
  key_links:
    - Biome config encodes the authored-source scope that Phase 7 is restoring.
    - The top-level formatting command remains `bun run format` even if supporting scripts are added underneath.
---

<objective>
Restore the formatter contract and add the Biome configuration the repo currently lacks.

Purpose: Make formatting work again on the real authored-source boundary, including the Tailwind v4 CSS layer.
Output: Biome config, stable formatting command behavior, and a formatter-check path suitable for pre-commit.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tooling-guardrail-cleanup/07-CONTEXT.md
@.planning/phases/07-tooling-guardrail-cleanup/07-RESEARCH.md
@package.json
@.gitignore
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit Biome configuration for the repo</name>
  <files>biome.jsonc</files>
  <action>Create a repo-level `biome.jsonc` that encodes the authored-source boundary for this project. It should exclude generated/build output, opt into Git ignore behavior where helpful, and define a stable source-level scope for formatter and lint tools.</action>
  <verify>test -f biome.jsonc && rg -n 'files|vcs|includes|ignore' biome.jsonc</verify>
  <done>The repo has an explicit Biome configuration instead of relying on defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Tailwind v4 CSS parsing and restore format command behavior</name>
  <files>biome.jsonc, package.json</files>
  <action>Configure Biome so the Tailwind v4 CSS directives in `src/styles/app.css` are parsed correctly. Keep `bun run format` as the top-level formatting command, but make it actually succeed on the authored-source boundary. Add a non-mutating format-check path if that is the cleanest pre-commit companion.</action>
  <verify>bun run format</verify>
  <done>The repo's top-level format command works again and no longer aborts on the Tailwind CSS layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run format`
- [ ] `bunx tsc --noEmit`
- [ ] `git diff --check`
- [ ] formatter config clearly excludes generated output from the intended source-level boundary
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The formatter contract is restored cleanly at the authored-source boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/07-tooling-guardrail-cleanup/07-01-SUMMARY.md`
</output>
