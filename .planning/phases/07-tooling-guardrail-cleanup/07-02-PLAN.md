---
phase: 07-tooling-guardrail-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - biome.jsonc
  - package.json
  - scripts/precommit.ts
autonomous: true
requirements:
  - PIPE-02
user_setup: []
must_haves:
  truths:
    - `bun run lint` now operates on the intended authored-source boundary instead of reporting diagnostics from `.output/`, `.vinxi/`, or other generated outputs.
    - Any real source issues exposed by the cleaned-up lint scope are fixed as part of the phase.
    - Pre-commit now includes the restored formatting and lint checks in addition to the existing content/PDF trust contract.
    - The expanded pre-commit path remains practical and passes on a clean tree.
  artifacts:
    - biome.jsonc
    - package.json
    - scripts/precommit.ts
  key_links:
    - The lint contract aligns with the Phase 7 scope decisions captured in `07-CONTEXT.md`.
    - Pre-commit runs the trustworthy format/lint checks without falling back to generated-output noise.
---

<objective>
Restore lint as a trustworthy source-level command and promote the restored checks into pre-commit.

Purpose: Turn the cleaned-up formatter/linter into real repository guardrails rather than local-only convenience commands.
Output: Scoped linting, any necessary source fixes, and a pre-commit contract that includes format/lint plus the existing content/PDF checks.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tooling-guardrail-cleanup/07-CONTEXT.md
@.planning/phases/07-tooling-guardrail-cleanup/07-RESEARCH.md
@package.json
@scripts/precommit.ts
@biome.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope lint to authored source and remove generated-output noise</name>
  <files>biome.jsonc, package.json</files>
  <action>Make `bun run lint` a trustworthy source-level command. Ensure the effective scope excludes `.output/`, `.vinxi/`, build assets, PDFs, and other generated outputs, while still covering the authored source boundary defined for Phase 7.</action>
  <verify>bun run lint</verify>
  <done>Linting is scoped to authored source and no longer floods diagnostics from generated output.</done>
</task>

<task type="auto">
  <name>Task 2: Fix real source issues surfaced by the cleaned-up guardrails</name>
  <files>biome.jsonc, package.json, src/**, scripts/**, content/**</files>
  <action>Resolve any real source-level diagnostics that appear once formatting and linting are correctly scoped. Keep the fixes narrow and avoid turning this into a repo-wide cleanup sweep.</action>
  <verify>bun run lint && bun run format</verify>
  <done>The restored source-level guardrails pass on the intended repo boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Expand pre-commit to include format and lint guardrails</name>
  <files>scripts/precommit.ts, package.json</files>
  <action>Update pre-commit so it runs the trustworthy format/lint checks before or alongside the existing validation, compile, and PDF steps. Prefer a non-mutating format check in pre-commit if the repo now has one. Keep the contract practical and deterministic.</action>
  <verify>bun run precommit</verify>
  <done>The repo-owned pre-commit hook enforces the restored source-level format and lint guardrails alongside the existing content/PDF contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run format`
- [ ] `bun run lint`
- [ ] `bunx tsc --noEmit`
- [ ] `bun run precommit`
- [ ] generated-output diagnostics are no longer the reason lint fails
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Format, lint, and pre-commit become trustworthy authored-source guardrails
  </success_criteria>

<output>
After completion, create `.planning/phases/07-tooling-guardrail-cleanup/07-02-SUMMARY.md`
</output>
