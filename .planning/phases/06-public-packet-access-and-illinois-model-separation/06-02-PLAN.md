---
phase: 06-public-packet-access-and-illinois-model-separation
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/routes/(site)/index.tsx
  - src/components/editorial/PacketFeature.tsx
  - src/routes/(site)/explainers/bond-financed-reserve-accumulation.tsx
  - src/app.tsx
  - src/routes/(site)/states/[slug].tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - SITE-01
  - SITE-05
  - SITE-06
user_setup: []
must_haves:
  truths:
    - Homepage, explainer, packet feature, and footer packet CTAs no longer route to `/states/illinois`.
    - The public shell preserves a clear distinction between the Illinois packet and the Illinois HB1844 registry record.
    - The Illinois registry page remains descriptive and now points readers to the packet honestly where useful.
    - The intended model / argument / proof reading path is navigable end to end in the built shell.
  artifacts:
    - src/routes/(site)/index.tsx
    - src/components/editorial/PacketFeature.tsx
    - src/routes/(site)/explainers/bond-financed-reserve-accumulation.tsx
    - src/app.tsx
    - src/routes/(site)/states/[slug].tsx
  key_links:
    - Packet CTAs now point to the Illinois packet route created in 06-01.
    - The Illinois registry record is linked as a descriptive reference rather than mislabeled as the packet.
---

<objective>
Rewire the public shell around the new Illinois packet surface and restore honest model-versus-registry labeling.

Purpose: Close the audit-level flow break by making the model / argument / proof path real instead of rhetorical.
Output: Corrected CTAs, clearer packet-versus-registry separation, and a verified end-to-end public reading path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-public-packet-access-and-illinois-model-separation/06-RESEARCH.md
@src/routes/(site)/index.tsx
@src/components/editorial/PacketFeature.tsx
@src/routes/(site)/explainers/bond-financed-reserve-accumulation.tsx
@src/app.tsx
@src/routes/(site)/states/[slug].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire packet CTAs to the real packet route</name>
  <files>src/routes/(site)/index.tsx, src/components/editorial/PacketFeature.tsx, src/routes/(site)/explainers/bond-financed-reserve-accumulation.tsx, src/app.tsx</files>
  <action>Update every Illinois packet CTA in the public shell so it points to the real packet route instead of `/states/illinois`. Keep labels honest and specific: packet CTAs should mean packet, not registry record.</action>
  <verify>rg -n '/packet/illinois|Read the Illinois model packet|Featured packet' src/routes/'(site)'/index.tsx src/components/editorial/PacketFeature.tsx src/routes/'(site)'/explainers/bond-financed-reserve-accumulation.tsx src/app.tsx</verify>
  <done>The shell no longer misroutes packet readers into the descriptive Illinois registry entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add honest packet and registry cross-links</name>
  <files>src/routes/(site)/states/[slug].tsx</files>
  <action>Adjust the Illinois state-detail support copy so the Illinois HB1844 page remains descriptive and clearly points readers to the normative packet where appropriate. Keep the page’s factual registry role intact and do not turn it into a packet page.</action>
  <verify>rg -n 'packet|registry|descriptive|HB1844' src/routes/'(site)'/states/'[slug]'.tsx</verify>
  <done>The Illinois registry page and packet route are distinct but mutually understandable to readers.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the model / argument / proof reading path</name>
  <files>src/routes/(site)/index.tsx, src/routes/(site)/explainers/bond-financed-reserve-accumulation.tsx, src/routes/(site)/states/[slug].tsx</files>
  <action>Run a final pass on the public reading flow. Ensure the homepage packet CTA, explainer packet CTA, packet route, and Illinois registry record now form a coherent and honestly labeled sequence. Verify built output, not just source code.</action>
  <verify>bun run build</verify>
  <done>The end-to-end packet and registry flow is correct in the prerendered public shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bunx tsc --noEmit`
- [ ] `bun run build`
- [ ] `.output/public/index.html` and `.output/public/explainers/bond-financed-reserve-accumulation/index.html` contain packet CTAs to `/packet/illinois`
- [ ] `.output/public/states/illinois/index.html` keeps the Illinois page descriptive and points readers toward the packet honestly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The public shell now has a truthful and navigable model / argument / proof path
  </success_criteria>

<output>
After completion, create `.planning/phases/06-public-packet-access-and-illinois-model-separation/06-02-SUMMARY.md`
</output>
