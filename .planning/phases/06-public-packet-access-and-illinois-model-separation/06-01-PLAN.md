---
phase: 06-public-packet-access-and-illinois-model-separation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/(site)/packet/illinois.tsx
  - app.config.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - SITE-01
  - SITE-06
user_setup: []
must_haves:
  truths:
    - The public shell exposes a real Illinois packet route rather than describing the packet without making it reachable.
    - The Illinois packet route renders canonical packet content from the existing one-pager and draft-bill markdown sources.
    - Readers can reach the packet PDFs from the public packet surface through honest labels.
    - The Illinois packet route is explicitly prerendered in the public build output.
  artifacts:
    - src/routes/(site)/packet/illinois.tsx
    - app.config.ts
  key_links:
    - The packet route renders `illinois-one-pager` and `illinois-draft-bill` as the normative Illinois model surface.
    - The packet route provides a clearly labeled path to the descriptive Illinois registry record rather than replacing it.
---

<objective>
Create the public Illinois packet surface that the milestone audit found missing.

Purpose: Restore the model layer of the intended model / argument / proof reading path.
Output: A real Illinois packet route in the public shell, backed by canonical packet docs and honest PDF access.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-public-packet-access-and-illinois-model-separation/06-RESEARCH.md
@src/lib/site/content.ts
@src/lib/site/raw-content.ts
@src/routes/(site)/methodology.tsx
@content/docs/illinois-one-pager.md
@content/docs/illinois-draft-bill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the public Illinois packet route</name>
  <files>src/routes/(site)/packet/illinois.tsx</files>
  <action>Create a dedicated public route for the Illinois packet, recommended as `/packet/illinois`. Render the canonical Illinois one-pager and draft bill from their markdown sources using the existing document-loading patterns. The route should read as the normative Illinois model, not as a registry record.</action>
  <verify>rg -n 'illinois-one-pager|illinois-draft-bill|Illinois model packet|HB1844' src/routes/'(site)'/packet/illinois.tsx</verify>
  <done>The site has a real public packet surface rather than an implied one.</done>
</task>

<task type="auto">
  <name>Task 2: Add honest packet downloads and registry distinction</name>
  <files>src/routes/(site)/packet/illinois.tsx</files>
  <action>Add clearly labeled links for the one-pager and draft-bill PDFs from the packet route, and include an equally clear link to the descriptive Illinois registry record. Keep the route explicit about the distinction between the project model and the live HB1844 legislative record.</action>
  <verify>rg -n 'pdf|registry|HB1844|descriptive' src/routes/'(site)'/packet/illinois.tsx</verify>
  <done>Readers can reach both the normative packet artifacts and the descriptive Illinois record without confusing the two.</done>
</task>

<task type="auto">
  <name>Task 3: Prerender the Illinois packet route</name>
  <files>app.config.ts</files>
  <action>Update the SolidStart prerender configuration so the Illinois packet route is part of the public build. Keep the route explicit and narrow; do not widen prerendering into a generic document crawl.</action>
  <verify>bun run build</verify>
  <done>The Illinois packet route is included in the prerendered public shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bunx tsc --noEmit`
- [ ] `bun run validate:content`
- [ ] `bun run build`
- [ ] `.output/public/packet/illinois/index.html` exists
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Illinois packet becomes a reachable public surface in the site shell
  </success_criteria>

<output>
After completion, create `.planning/phases/06-public-packet-access-and-illinois-model-separation/06-01-SUMMARY.md`
</output>
