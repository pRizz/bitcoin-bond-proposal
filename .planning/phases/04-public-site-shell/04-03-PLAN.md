---
phase: 04-public-site-shell
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/routes/(site)/states/index.tsx
  - src/routes/(site)/states/[slug].tsx
  - src/components/editorial/*
  - src/lib/site/*
autonomous: true
requirements:
  - SITE-03
  - SITE-04
user_setup: []
must_haves:
  truths:
    - Readers can browse a catalog route that surfaces the first registry batch with medium density and visible freshness metadata.
    - Readers can open state detail routes backed by canonical generated content.
    - The public shell preserves record-type honesty, especially for the New Hampshire authority-action entry.
  artifacts:
    - src/routes/(site)/states/index.tsx
    - src/routes/(site)/states/[slug].tsx
  key_links:
    - The catalog route consumes generated registry data rather than hardcoded state lists.
    - State detail routes surface facts, analysis, and sources in the approved narrative-first order.
---

<objective>
Build the catalog and state detail routes for the public site shell.

Purpose: Turn the registry artifacts from Phase 3 into a public browsing experience that preserves the project’s factual honesty and editorial clarity.
Output: Catalog route, state detail route, and generated-content integration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-site-shell/04-CONTEXT.md
@.planning/phases/04-public-site-shell/04-RESEARCH.md
@generated/content-graph.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the registry catalog route</name>
  <files>src/routes/(site)/states/index.tsx</files>
  <action>Create the catalog route using the approved medium-density layout. Each item should surface state, title or bill id, status, proposal focus, one-line significance, and freshness metadata. Support the approved v1 filters and sorts without turning the page into a dashboard wall.</action>
  <verify>rg -n 'status|focus|reviewed|priority|sort|filter' src/routes/(site)/states/index.tsx</verify>
  <done>The catalog route makes the registry batch browsable while preserving readability and priority cues.</done>
</task>

<task type="auto">
  <name>Task 2: Build the state detail route with honest record typing</name>
  <files>src/routes/(site)/states/[slug].tsx</files>
  <action>Create the dynamic state detail route so it presents the at-a-glance block, plain-English effect, bond/reserve analysis, why-it-matters or why-it-is-limited line, key statutory mechanics, and source trail in the approved order. Make record type explicit and especially ensure the New Hampshire authority-action entry is not flattened into a normal bill page.</action>
  <verify>rg -n 'record type|source|what this would actually do|why this matters|why this is limited' src/routes/(site)/states/[slug].tsx</verify>
  <done>The state detail route presents registry entries with factual honesty, visible sources, and the approved narrative flow.</done>
</task>

<task type="auto">
  <name>Task 3: Wire generated registry data into public routes</name>
  <files>src/routes/(site)/states/index.tsx, src/routes/(site)/states/[slug].tsx, src/lib/site/*</files>
  <action>Wire both routes to the generated content graph rather than hardcoded or duplicate state data. Keep the integration thin and route-friendly. The public shell should read from the existing generated registry layer created in Phase 3 and preserve the distinction between state metadata and packet materials.</action>
  <verify>rg -n 'content-graph|publishedSlugs|states' src/routes/(site)/states/index.tsx src/routes/(site)/states/[slug].tsx src/lib/site/*</verify>
  <done>The public routes are backed by the generated registry data and remain consistent with the canonical content pipeline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bunx tsc --noEmit`
- [ ] `bun run validate:content`
- [ ] `bun run build` (or project-equivalent frontend build once added)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The catalog and state detail routes turn the registry into a trustworthy public browsing surface
  </success_criteria>

<output>
After completion, create `.planning/phases/04-public-site-shell/04-03-SUMMARY.md`
</output>
