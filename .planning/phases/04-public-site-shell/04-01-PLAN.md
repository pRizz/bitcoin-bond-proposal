---
phase: 04-public-site-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - app.config.ts
  - src/app.tsx
  - src/routes/(site)/index.tsx
  - src/routes/(site)/methodology.tsx
  - src/components/editorial/*
  - src/lib/site/*
  - src/styles/app.css
autonomous: true
requirements:
  - SITE-01
  - SITE-02
user_setup: []
must_haves:
  truths:
    - The repo has a real SolidStart public site shell with file-based routes and explicit prerender configuration.
    - A dedicated theme layer and shared editorial component layer exist before route-specific UI work begins.
    - The visual system points toward editorial-financial seriousness rather than default utility styling.
  artifacts:
    - app.config.ts
    - src/app.tsx
    - src/styles/app.css
    - src/components/editorial
    - src/lib/site
  key_links:
    - The app shell is configured for static prerendering.
    - Shared editorial components are available for homepage, methodology, catalog, and state routes.
---

<objective>
Bootstrap the public site shell, theme layer, and shared editorial primitives.

Purpose: Create a stable public-site foundation so Phase 4 route work can focus on actual content presentation rather than re-bootstrap and ad hoc styling.
Output: SolidStart app shell, theme tokens, root layout, and shared editorial components.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-site-shell/04-CONTEXT.md
@.planning/phases/04-public-site-shell/04-RESEARCH.md
@package.json
@generated/content-graph.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the SolidStart site shell</name>
  <files>package.json, bun.lock, app.config.ts, src/app.tsx</files>
  <action>Install the current SolidStart, Solid, and route-layer dependencies needed for the site shell. Add an `app.config.ts` with explicit prerender configuration and wire the root application entrypoint around file-based routes. Keep the setup static-site oriented and aligned with the project’s existing Bun workflow.</action>
  <verify>bun install && bunx tsc --noEmit</verify>
  <done>The repo contains a functioning SolidStart shell with the right build and prerender foundations.</done>
</task>

<task type="auto">
  <name>Task 2: Create the dedicated theme layer and global shell styles</name>
  <files>src/styles/app.css</files>
  <action>Create a tokenized theme layer using Tailwind v4 theme variables and any supporting CSS variables needed for the project’s editorial-financial tone. Avoid crypto-hype aesthetics and avoid bland defaults. This file should establish the palette, typography, spacing, and surface language the whole shell will inherit.</action>
  <verify>rg -n '@theme|--color-|--font-|--spacing' src/styles/app.css</verify>
  <done>The site has an explicit theme layer that encodes the approved visual direction.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared editorial primitives for Phase 4 routes</name>
  <files>src/components/editorial/*, src/lib/site/*</files>
  <action>Create the shared editorial building blocks the route plans will rely on: page headers, proof strips, section wrappers, infographic containers, and any small utility helpers needed for route composition. Keep Kobalte usage wrapped behind local components where useful, and do not let route files become layout construction zones.</action>
  <verify>test -d src/components/editorial && test -d src/lib/site</verify>
  <done>Wave 2 routes can compose content from shared primitives instead of hand-building the shell repeatedly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install`
- [ ] `bunx tsc --noEmit`
- [ ] `bun run validate:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a real public-site foundation with theme and editorial primitives
  </success_criteria>

<output>
After completion, create `.planning/phases/04-public-site-shell/04-01-SUMMARY.md`
</output>
