---
phase: 12-public-coverage-and-freshness-surfaces
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site/content.ts
  - src/routes/(site)/states/index.tsx
  - src/components/editorial/StateCard.tsx
  - src/styles/app.css
autonomous: true
requirements:
  - SITE-07
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260411T153046Z
generated_at: 2026-04-11T15:30:46.617Z
must_haves:
  truths:
    - The states catalog exposes published, queued, and unresearched coverage instead of only published records.
    - Readers can filter or scan the catalog by status without mistaking queued or unresearched states for published records.
    - Catalog summary surfaces expose the current published/queued/unresearched posture and freshness debt counts.
  artifacts:
    - src/lib/site/content.ts
    - src/routes/(site)/states/index.tsx
    - src/components/editorial/StateCard.tsx
  key_links:
    - Catalog rendering uses manifest-wide coverage data while only linking to published state detail routes.
---

<objective>
Turn the states index into a true coverage-and-status surface.

Purpose: Make the national registry posture legible instead of showing only the published subset.
Output: Coverage-aware helpers and a catalog UI that distinguishes published, queued, and unresearched states.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-public-coverage-and-freshness-surfaces/12-CONTEXT.md
@src/lib/site/content.ts
@src/routes/(site)/states/index.tsx
@src/components/editorial/StateCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-wide coverage helpers</name>
  <files>src/lib/site/content.ts</files>
  <read_first>src/lib/site/content.ts</read_first>
  <action>Extend the site-content helper layer so the catalog can render all 50 states with merged manifest status, optional published record data, and compact coverage summary counts. Keep the API small and derived from the existing generated graph.</action>
  <verify>bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - Helper output can distinguish published, queued, and unresearched entries.
    - Helper output still preserves published-state detail data for linked cards.
  </acceptance_criteria>
  <done>The catalog has a single coverage-aware data source.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign the catalog around coverage status</name>
  <files>src/routes/(site)/states/index.tsx, src/components/editorial/StateCard.tsx, src/styles/app.css</files>
  <read_first>src/routes/(site)/states/index.tsx, src/components/editorial/StateCard.tsx</read_first>
  <action>Refactor the states index so it surfaces summary counts, status-aware filters, and clearly differentiated published versus non-published cards. Published cards should remain rich and linked; queued and unresearched states should read as coverage ledger items, not broken links.</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - The page shows published, queued, and unresearched counts.
    - Non-published states render clearly without record links.
    - Published states remain easy to scan and open.
  </acceptance_criteria>
  <done>The catalog reads as a coverage surface instead of a published-only list.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bunx tsc --noEmit`
- [ ] `bun run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Coverage distinctions are obvious on the catalog route
  </success_criteria>

<output>
After completion, create `.planning/phases/12-public-coverage-and-freshness-surfaces/12-01-SUMMARY.md`
</output>
