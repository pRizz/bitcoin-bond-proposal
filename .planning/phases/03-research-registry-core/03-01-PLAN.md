---
phase: 03-research-registry-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/data/state-registry-manifest.json
  - content/data/state-registry-manifest.README.md
  - src/lib/content/schema.ts
  - scripts/validate-content.ts
  - scripts/compile-content.ts
autonomous: true
requirements:
  - CATA-01
user_setup: []
must_haves:
  truths:
    - A 50-state skeleton exists as a manifest-level canonical layer rather than as fake publishable state pages.
    - The registry distinguishes `unresearched`, `queued`, and `published` states.
    - Internal editorial priority is encoded without polluting public-facing record content.
    - Validation and compile steps understand the skeleton manifest and the publishable-vs-queued distinction.
  artifacts:
    - content/data/state-registry-manifest.json
    - content/data/state-registry-manifest.README.md
    - src/lib/content/schema.ts
    - scripts/validate-content.ts
    - scripts/compile-content.ts
  key_links:
    - The manifest status model is wired into validation and compile scripts.
    - Published state-entry files remain separate from manifest-only or queued state records.
---

<objective>
Build the 50-state skeleton manifest and the content-boundary support needed for a real registry workflow.

Purpose: Establish the canonical registry backbone and publishability boundary before authoring the first researched state entries.
Output: Skeleton manifest, supporting schema changes, and updated validation/compile behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-research-registry-core/03-CONTEXT.md
@.planning/phases/03-research-registry-core/03-RESEARCH.md
@content/data/proposal-taxonomy.json
@src/lib/content/schema.ts
@scripts/validate-content.ts
@scripts/compile-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 50-state skeleton manifest</name>
  <files>content/data/state-registry-manifest.json, content/data/state-registry-manifest.README.md</files>
  <action>Create the 50-state skeleton as a manifest-level artifact, not as 50 fake public records. Encode only the approved minimal fields: state, slug, registry status, proposal focus, short note, and internal editorial priority. The README should explain the difference between manifest-only states, queued research, and publishable state entries.</action>
  <verify>rg -n '\"unresearched\"|\"queued\"|\"published\"|\"bond-priority\"|\"reserve-priority\"' content/data/state-registry-manifest.json</verify>
  <done>The repo contains a complete 50-state skeleton manifest with clear status and priority semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Extend schemas for skeleton and registry boundary rules</name>
  <files>src/lib/content/schema.ts</files>
  <action>Extend the content schema layer to validate the state-registry manifest and encode the distinction between manifest-level skeleton records and publishable state-entry files. Keep the new schema strict but small, mirroring the locked Phase 3 context rather than inventing extra fields just because they might be useful later.</action>
  <verify>bunx tsc --noEmit && bun test</verify>
  <done>The schema layer can validate the skeleton manifest and its internal priority/status fields cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Teach validation and compile scripts the registry workflow</name>
  <files>scripts/validate-content.ts, scripts/compile-content.ts</files>
  <action>Update validation and compile behavior so the registry manifest is part of the canonical content workflow, while publishable state-entry files remain distinct from manifest-only and queued states. The compile layer should expose enough of the new manifest data for later site work without flattening queued and published records into one undifferentiated list.</action>
  <verify>bun run validate:content && bun run compile:content</verify>
  <done>The content pipeline understands the 50-state skeleton and preserves the publishable-versus-queued boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bunx tsc --noEmit`
- [ ] `bun test`
- [ ] `bun run validate:content`
- [ ] `bun run compile:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The registry skeleton and publishability boundary exist as real canonical infrastructure
  </success_criteria>

<output>
After completion, create `.planning/phases/03-research-registry-core/03-01-SUMMARY.md`
</output>
