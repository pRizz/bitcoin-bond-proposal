---
phase: 08-dark-theme-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/app.css
  - src/app.tsx
autonomous: true
requirements:
  - THEME-01
  - THEME-02
user_setup: []
must_haves:
  truths:
    - The public shell is dark by default at the token and root-shell level.
    - The theme uses layered warm near-black neutrals rather than flat black.
    - The accent system introduces restrained Bitcoin-orange emphasis tiers rather than a single loud accent.
    - Shared shell chrome remains readable after the dark token refactor.
  artifacts:
    - src/styles/app.css
    - src/app.tsx
  key_links:
    - The dark token layer is strong enough that later shared component adoption can consume it without ad hoc route-specific overrides.
    - The root shell still frames content as an editorial product rather than a crypto dashboard.
---

<objective>
Establish the dark-first token layer and root-shell foundation for the v1.1 visual refactor.

Purpose: Make the webapp unmistakably dark by default before shared components are tuned against the new system.
Output: Dark token system, background treatment, and root shell chrome aligned with the approved warm editorial direction.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dark-theme-foundation/08-CONTEXT.md
@.planning/phases/08-dark-theme-foundation/08-RESEARCH.md
@src/styles/app.css
@src/app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the global dark token layer</name>
  <files>src/styles/app.css</files>
  <action>Replace the current light-first token system with a layered dark token system using warm near-black neutrals. Add restrained Bitcoin-orange accent tiers and preserve sufficient contrast for body text, borders, panels, and selection states.</action>
  <verify>rg -n '--color-canvas|--color-panel|--color-accent|--color-border' src/styles/app.css</verify>
  <done>The repo has a real dark-first theme token foundation rather than light tokens with isolated dark patches.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the shell background and chrome foundation</name>
  <files>src/styles/app.css, src/app.tsx</files>
  <action>Update the global background treatment and the root app chrome so the site reads as a dark editorial product by default. Keep the atmosphere restrained and avoid neon, cyber, or glow-heavy effects.</action>
  <verify>rg -n 'bg-canvas|bg-canvas-soft|border-border-soft|Public research shell' src/app.tsx src/styles/app.css</verify>
  <done>The app shell looks structurally dark and coherent before any route-specific polish begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run format`
- [ ] `bun run lint`
- [ ] `bunx tsc --noEmit`
- [ ] `bun run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The webapp is dark by default at the shared token and shell level
  </success_criteria>

<output>
After completion, create `.planning/phases/08-dark-theme-foundation/08-01-SUMMARY.md`
</output>
