---
phase: 15-first-expansion-publication-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/states/florida-sb-550.md
  - content/states/kansas-sb-352.md
  - content/states/north-dakota-hb-1184.md
  - content/data/state-registry-manifest.json
autonomous: true
requirements:
  - CATA-15
  - REFR-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-05-31T23-51-15
generated_at: 2026-06-01T00:27:02Z
must_haves:
  truths:
    - "Florida, Kansas, and North Dakota have canonical public state records with official-source failed status and dated review footing."
    - "Kansas uses the current official 2026-04-10 died-in-committee posture, not the stale Phase 14 queue date."
    - "Published-refresh work is inspected before lower-risk expansion authoring proceeds."
  artifacts:
    - path: "content/states/florida-sb-550.md"
      provides: "Florida failed public-funds Bitcoin investment record"
      contains: "Died in Banking and Insurance"
    - path: "content/states/kansas-sb-352.md"
      provides: "Kansas failed bitcoin and digital assets reserve fund record"
      contains: "2026-04-10"
    - path: "content/states/north-dakota-hb-1184.md"
      provides: "North Dakota failed digital asset and precious metal investment record"
      contains: "Second reading, failed to pass"
    - path: "content/data/state-registry-manifest.json"
      provides: "Published manifest rows for Florida, Kansas, and North Dakota"
      contains: "\"registryStatus\": \"published\""
  key_links:
    - from: "content/states/florida-sb-550.md"
      to: "content/data/state-registry-manifest.json"
      via: "matching frontmatter slug `florida`"
      pattern: "\"slug\": \"florida\""
    - from: "content/states/kansas-sb-352.md"
      to: "content/data/state-registry-manifest.json"
      via: "matching frontmatter slug `kansas`"
      pattern: "\"slug\": \"kansas\""
    - from: "content/states/north-dakota-hb-1184.md"
      to: "content/data/state-registry-manifest.json"
      via: "matching frontmatter slug `north-dakota`"
      pattern: "\"slug\": \"north-dakota\""
---

<objective>
Publish the first three terminal, official-source-rich Phase 15 state entries: Florida SB 550, Kansas SB 352, and North Dakota HB 1184.

Purpose: These failed records are useful legislative posture evidence when framed as inactive/failed proposals, and they let the expansion wave start with high-confidence source trails while honoring the refresh queue gate.
Output: Three canonical Markdown state records plus matching manifest rows marked `published`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
@.planning/phases/15-first-expansion-publication-wave/15-RESEARCH.md
@.planning/phases/15-first-expansion-publication-wave/15-UI-SPEC.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-03-SUMMARY.md
@generated/refresh/state-priority-queue.md
@content/states/arizona-sb-1025.md
@content/states/texas-sb-21.md
@content/states/north-carolina-s-327.md
@content/data/state-registry-manifest.json
@content/data/state-registry-manifest.README.md
@src/lib/content/schema.ts
@scripts/validate-content.ts
@package.json

<interfaces>
Existing state entry contract from `src/lib/content/schema.ts`:
```typescript
export const StateEntryFrontmatterSchema = z.object({
	title: NonEmptyStringSchema,
	slug: SlugSchema,
	summary: NonEmptyStringSchema,
	state: NonEmptyStringSchema,
	recordType: RecordTypeSchema,
	proposalKind: ProposalKindValueSchema,
	proposalSubtype: ProposalSubtypeValueSchema,
	billId: NonEmptyStringSchema,
	chamber: NonEmptyStringSchema,
	status: NonEmptyStringSchema,
	legislativeStatusGroup: LegislativeStatusGroupSchema,
	statusAsOf: IsoDateSchema,
	lastReviewed: IsoDateSchema,
	sponsors: z.array(NonEmptyStringSchema).min(1),
	primarySources: z.array(SourceLinkSchema).min(1),
	secondarySources: z.array(SourceLinkSchema).default([]),
	confidence: z.enum(["high", "medium", "low"]),
	effect: NonEmptyStringSchema,
	classificationNote: z.string().trim().optional(),
});
```

Existing manifest row contract from `src/lib/content/schema.ts`:
```typescript
export const StateRegistryManifestEntrySchema = z.object({
	state: NonEmptyStringSchema,
	slug: SlugSchema,
	registryStatus: z.enum(["unresearched", "queued", "published"]),
	proposalFocus: z.enum(["bond", "reserve", "both", "unknown"]),
	region: z.enum(["northeast", "midwest", "south", "west"]),
	shortNote: NonEmptyStringSchema,
	editorialPriority: z.enum(["bond-priority", "reserve-priority", "neutral"]),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run REFR-05 published-refresh blocker gate before authoring</name>
  <files>generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md, .planning/phases/15-first-expansion-publication-wave/15-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
    - .planning/phases/15-first-expansion-publication-wave/15-RESEARCH.md
    - generated/refresh/state-priority-queue.json
    - generated/refresh/state-priority-queue.md
  </read_first>
  <action>Before authoring any new state record, inspect `generated/refresh/state-priority-queue.json.publishedRefreshWork` per REFR-05 and D-06. Record the gate outcome in the eventual `.planning/phases/15-first-expansion-publication-wave/15-01-SUMMARY.md`: include the number of published refresh entries inspected, the due slugs, and either `no pre-authoring blockers found` or the exact blocking published record(s). Treat a blocker as a stale existing published record whose current posture is required for Phase 15 comparison, catalog, or freshness claims; do not broaden this gate into a refresh of every due published record.</action>
  <action>If the gate identifies a concrete blocker, stop before authoring Florida, Kansas, or North Dakota and return the blocker list so refresh work can be planned separately. If the gate finds no blocker, continue with Task 2 and leave broad published refresh work visible in the generated queue instead of refreshing it in this phase.</action>
  <verify>
    <automated>bun -e 'const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const work = priority.publishedRefreshWork ?? []; if (!Array.isArray(work) || work.length === 0) throw new Error("publishedRefreshWork is missing or empty"); const due = work.filter((entry) => entry.priority === "due").map((entry) => entry.slug); console.log(`REFR-05 inspected ${work.length} published refresh entries; due slugs: ${due.join(",")}`);'</automated>
    <automated>bun -e 'const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const expansionSlugs = new Set(["florida","kansas","north-dakota"]); const blockers = (priority.publishedRefreshWork ?? []).filter((entry) => expansionSlugs.has(entry.slug)); if (blockers.length) throw new Error(`Published refresh work overlaps Plan 15-01 authoring slugs: ${blockers.map((entry) => entry.slug).join(",")}`); console.log("REFR-05 no-blocker outcome: published refresh work remains queued; no existing published slug blocks Plan 15-01 authoring scope.");'</automated>
  </verify>
  <acceptance_criteria>
    - Gate output lists the inspected `publishedRefreshWork` count and due slugs before any new Markdown file is authored.
    - `.planning/phases/15-first-expansion-publication-wave/15-01-SUMMARY.md` records either `no pre-authoring blockers found` or the exact blocking published record(s).
    - Broad refresh work is not performed in this plan unless a concrete blocker is identified.
  </acceptance_criteria>
  <done>REFR-05 is satisfied by a documented pre-authoring refresh-work inspection and a refresh/no-blocker outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Author terminal failed records for Florida, Kansas, and North Dakota</name>
  <files>content/states/florida-sb-550.md, content/states/kansas-sb-352.md, content/states/north-dakota-hb-1184.md</files>
  <read_first>
    - .planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
    - .planning/phases/15-first-expansion-publication-wave/15-RESEARCH.md
    - generated/refresh/state-priority-queue.md
    - content/states/arizona-sb-1025.md
    - content/states/texas-sb-21.md
    - content/states/north-carolina-s-327.md
    - src/lib/content/schema.ts
  </read_first>
  <action>Before authoring, confirm the required source posture from official pages per D-01, D-05, D-06, D-07, D-08, and REFR-05: Florida SB 550 official page contains `Died in Banking and Insurance`, Florida HB 487 companion page contains `Died in Government Operations Subcommittee`, Kansas SB 352 history contains `Fri, Apr 10, 2026` and `Died in Committee`, and North Dakota HB 1184 overview contains `Second reading, failed to pass, yeas 32 nays 57`. Create exactly these files: `content/states/florida-sb-550.md`, `content/states/kansas-sb-352.md`, and `content/states/north-dakota-hb-1184.md`. Use the same heading order as `content/states/arizona-sb-1025.md`: `#`, `## At a Glance`, `## Legislative Status`, `## What This Would Actually Do`, `## Bond / Reserve Analysis`, `## Key Statutory Mechanics`, `## Why This Matters` or `## Why This Is Limited`, `## Source Trail`, and `## Confidence / Completeness Note`. Do not create route files or UI components. Keep mechanics, effect, classification, and sponsor prose limited to facts verified by the official-source automated checks in this task; add a new official-source check before adding any additional nontrivial claim.</action>
  <action>Use these exact frontmatter targets. Florida: `title: Florida SB 550`, `slug: florida`, `state: Florida`, `recordType: legislative-bill`, `proposalKind: reserve`, `proposalSubtype: strategic-reserve`, `billId: SB 550`, `chamber: Florida Senate`, `status: Died in Banking and Insurance`, `legislativeStatusGroup: failed`, `statusAsOf: 2025-06-16`, `lastReviewed: 2026-06-01`, sponsors `Sen. Joe Gruters`, primary sources `https://www.flsenate.gov/Session/Bill/2025/550` and `https://www.flsenate.gov/Session/Bill/2025/487`, `confidence: high`, and an `effect` explaining that the bill would have authorized public-fund Bitcoin investment authority for the Chief Financial Officer and State Board of Administration but did not become law. Kansas: `title: Kansas SB 352`, `slug: kansas`, `state: Kansas`, `proposalKind: reserve`, `proposalSubtype: mixed-digital-asset-reserve`, `billId: SB 352`, `chamber: Kansas Senate`, `status: Died in Committee`, `legislativeStatusGroup: failed`, `statusAsOf: 2026-04-10`, `lastReviewed: 2026-06-01`, sponsor `Sen. Craig Bowser`, primary sources `https://kslegislature.gov/b2025_26/bills/sb352/` and `https://kslegislature.gov/b2025_26/bills/SB352/history/`, `confidence: high`, and a classification note that the reserve fund covers bitcoin and broader digital assets. North Dakota: `title: North Dakota HB 1184`, `slug: north-dakota`, `state: North Dakota`, `proposalKind: reserve`, `proposalSubtype: mixed-digital-asset-reserve`, `billId: HB 1184`, `chamber: North Dakota House`, `status: Second reading, failed to pass, yeas 32 nays 57`, `legislativeStatusGroup: failed`, `statusAsOf: 2025-01-31`, `lastReviewed: 2026-06-01`, sponsors `Rep. Toman`, `Rep. Christy`, `Rep. Heilman`, `Rep. Hendrix`, `Rep. D. Johnston`, `Rep. S. Olson`, `Rep. Porter`, `Sen. Barta`, `Sen. Cory`, `Sen. Meyer`, and `Sen. Paulson`, primary sources `https://ndlegis.gov/assembly/69-2025/regular/bill-overview/bo1184.html?bill_number=1184&bill_year=2025` and `https://ndlegis.gov/assembly/69-2025/regular/documents/25-0515-03000.pdf`, `confidence: high`, and an `effect` explaining the proposed up-to-ten-percent investment authority for specified state-managed funds in precious metals, digital assets above the market-cap threshold, or stablecoins.</action>
  <verify>
    <automated>curl -fsSL https://www.flsenate.gov/Session/Bill/2025/550 | rg "Died in Banking and Insurance"</automated>
    <automated>curl -fsSL https://www.flsenate.gov/Session/Bill/2025/487 | rg "Died in Government Operations Subcommittee"</automated>
    <automated>bun -e 'const normalize = (value) => value.replace(/\s+/g, " "); const get = async (url) => { const response = await fetch(url); if (!response.ok) throw new Error(`${url} returned ${response.status}`); return normalize(await response.text()); }; const bill = await get("https://www.flsenate.gov/Session/Bill/2025/550"); const text = await get("https://www.flsenate.gov/Session/Bill/2025/550/BillText/Filed/HTML"); const companion = await get("https://www.flsenate.gov/Session/Bill/2025/487"); const senator = await get("https://www.flsenate.gov/Senators/S22"); const required = [["Florida SB 550 failed status", bill, "Died in Banking and Insurance"], ["Florida HB 487 companion failed status", companion, "Died in Government Operations Subcommittee"], ["Florida public-funds Bitcoin subject", bill, "Investments of Public Funds in Bitcoin"], ["Florida bill sponsor by chamber source", text, "Senator Gruters"], ["Florida sponsor full name", senator, "Senator Joe Gruters"], ["Florida CFO public-funds Bitcoin authority", text, "authorizing the Chief Financial Officer to invest money in Bitcoin from certain public funds"], ["Florida State Board of Administration System Trust Fund authority", text, "Trustees of the State Board of Administration to invest and reinvest available funds of the System Trust Fund in Bitcoin"]]; const missing = required.filter(([, source, fact]) => !source.includes(fact)).map(([label]) => label); if (missing.length) throw new Error("Missing official Florida SB 550 fact(s): " + missing.join("; "));'</automated>
    <automated>curl -fsSL https://kslegislature.gov/b2025_26/bills/SB352/history/ | rg "Fri, Apr 10, 2026"</automated>
    <automated>curl -fsSL https://kslegislature.gov/b2025_26/bills/SB352/history/ | rg "Died in Committee"</automated>
    <automated>curl -fsSL "https://kslegislature.gov/b2025_26/bills/download/?apn=b2025_26/year2/ready_for_publication/sb_352/sb352_00_0000.pdf" -o /tmp/phase15-ks-sb352.pdf && pdftotext /tmp/phase15-ks-sb352.pdf - > /tmp/phase15-ks-sb352.txt && bun -e 'const normalize = (value) => value.replace(/\s+/g, " "); const get = async (url) => { const response = await fetch(url); if (!response.ok) throw new Error(`${url} returned ${response.status}`); return normalize(await response.text()); }; const page = await get("https://kslegislature.gov/b2025_26/bills/sb352/"); const history = await get("https://kslegislature.gov/b2025_26/bills/SB352/history/"); const text = normalize(await Bun.file("/tmp/phase15-ks-sb352.txt").text()); const required = [["Kansas sponsor", page, "Sen. Craig Bowser"], ["Kansas died status date", history, "Fri, Apr 10, 2026"], ["Kansas died-in-committee status", history, "Died in Committee"], ["Kansas reserve fund title", text, "creating the bitcoin and digital assets reserve fund"], ["Kansas state treasury reserve fund creation", text, "There is hereby created in the state treasury the bitcoin and digital assets reserve fund"], ["Kansas state treasurer administration", text, "The state treasurer shall administer the fund"], ["Kansas digital-assets definition", text, "\"Digital assets\" means any of the following"], ["Kansas broader digital-assets coverage", text, "cryptocurrencies; or (C) any other digital-only assets"]]; const missing = required.filter(([, source, fact]) => !source.includes(fact)).map(([label]) => label); if (missing.length) throw new Error("Missing official Kansas SB 352 fact(s): " + missing.join("; "));'</automated>
    <automated>curl -fsSL "https://ndlegis.gov/assembly/69-2025/regular/bill-overview/bo1184.html?bill_number=1184&bill_year=2025" | rg "Second reading, failed to pass, yeas 32 nays 57"</automated>
    <automated>curl -fsSL "https://ndlegis.gov/assembly/69-2025/regular/documents/25-0515-03000.pdf" -o /tmp/phase15-nd-hb1184.pdf && pdftotext /tmp/phase15-nd-hb1184.pdf - > /tmp/phase15-nd-hb1184.txt && bun -e 'const normalize = (value) => value.replace(/(^|\n)\s*\d+\s*(?=\n)/g, " ").replace(/\s+/g, " "); const get = async (url) => { const response = await fetch(url); if (!response.ok) throw new Error(`${url} returned ${response.status}`); return normalize(await response.text()); }; const overview = await get("https://ndlegis.gov/assembly/69-2025/regular/bill-overview/bo1184.html?bill_number=1184&bill_year=2025"); const text = normalize(await Bun.file("/tmp/phase15-nd-hb1184.txt").text()); const required = [["North Dakota failed floor vote", overview, "Second reading, failed to pass, yeas 32 nays 57"], ["North Dakota House sponsors", text, "Representatives Toman, Christy, Heilman, Hendrix, D. Johnston, S. Olson, Porter"], ["North Dakota Senate sponsors", text, "Senators Barta, Cory, Meyer, Paulson"], ["North Dakota digital asset and precious metal subject", text, "digital asset and precious metal investments"], ["North Dakota board of university and school lands authority", text, "The board of university and school lands may invest up to ten percent"], ["North Dakota state investment board authority", text, "The state investment board may invest up to ten percent"], ["North Dakota state treasurer authority", text, "state treasurer may invest up to ten percent"], ["North Dakota eligible assets", text, "in precious metals, digital assets"], ["North Dakota market-cap threshold", text, "market capitalization exceeding five hundred billion dollars"], ["North Dakota stablecoin inclusion", text, "or stablecoins"]]; const missing = required.filter(([, source, fact]) => !source.includes(fact)).map(([label]) => label); if (missing.length) throw new Error("Missing official North Dakota HB 1184 fact(s): " + missing.join("; "));'</automated>
    <automated>test -f content/states/florida-sb-550.md</automated>
    <automated>test -f content/states/kansas-sb-352.md</automated>
    <automated>test -f content/states/north-dakota-hb-1184.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^slug: (florida|kansas|north-dakota)$" content/states/florida-sb-550.md content/states/kansas-sb-352.md content/states/north-dakota-hb-1184.md` returns exactly 3 matches.
    - `rg -n "statusAsOf: 2026-04-10|Died in Committee" content/states/kansas-sb-352.md` returns matches.
    - `rg -n "Died in Banking and Insurance|Died in Government Operations Subcommittee" content/states/florida-sb-550.md` returns matches.
    - `rg -n "Second reading, failed to pass, yeas 32 nays 57|25-0515-03000.pdf" content/states/north-dakota-hb-1184.md` returns matches.
    - `rg -n "## Source Trail|## Confidence / Completeness Note|## Bond / Reserve Analysis" content/states/florida-sb-550.md content/states/kansas-sb-352.md content/states/north-dakota-hb-1184.md` returns matches in all 3 files.
  </acceptance_criteria>
  <done>Three terminal failed records exist with official source trails, dated status/review fields, conservative classifications, and policy-effect summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Publish terminal records in the manifest and validate content</name>
  <files>content/data/state-registry-manifest.json</files>
  <read_first>
    - content/data/state-registry-manifest.json
    - content/data/state-registry-manifest.README.md
    - content/states/florida-sb-550.md
    - content/states/kansas-sb-352.md
    - content/states/north-dakota-hb-1184.md
    - scripts/validate-content.ts
  </read_first>
  <action>Update only the `florida`, `kansas`, and `north-dakota` rows in `content/data/state-registry-manifest.json`, per D-10 and D-12. Florida row target: `registryStatus: "published"`, `proposalFocus: "reserve"`, `region: "south"`, `shortNote: "Failed 2025 public-funds Bitcoin investment proposal."`, `editorialPriority: "reserve-priority"`. Kansas row target: `registryStatus: "published"`, `proposalFocus: "reserve"`, `region: "midwest"`, `shortNote: "Failed 2026 bitcoin and digital assets reserve fund proposal."`, `editorialPriority: "reserve-priority"`. North Dakota row target: `registryStatus: "published"`, `proposalFocus: "reserve"`, `region: "midwest"`, `shortNote: "Failed 2025 digital-asset and precious-metals investment proposal."`, `editorialPriority: "reserve-priority"`. Leave `south-dakota` and `wyoming` as `registryStatus: "unresearched"` in this phase.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bun -e 'const manifest = await Bun.file("content/data/state-registry-manifest.json").json(); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); for (const slug of ["florida","kansas","north-dakota"]) { if (rows.get(slug)?.registryStatus !== "published") throw new Error(`${slug} not published`); } for (const slug of ["south-dakota","wyoming"]) { if (rows.get(slug)?.registryStatus !== "unresearched") throw new Error(`${slug} should remain unresearched`); }'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0 and reports 13 published state entries.
    - `bun -e 'const manifest = await Bun.file("content/data/state-registry-manifest.json").json(); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); if (rows.get("florida").shortNote !== "Failed 2025 public-funds Bitcoin investment proposal.") process.exit(1); if (rows.get("kansas").shortNote !== "Failed 2026 bitcoin and digital assets reserve fund proposal.") process.exit(1); if (rows.get("north-dakota").shortNote !== "Failed 2025 digital-asset and precious-metals investment proposal.") process.exit(1);'` exits 0.
    - `rg -n '"slug": "(florida|kansas|north-dakota)"' content/data/state-registry-manifest.json` returns exactly 3 matches.
    - `rg -n '"slug": "(south-dakota|wyoming)"' content/data/state-registry-manifest.json` still returns rows with nearby `registryStatus` set to `unresearched`.
  </acceptance_criteria>
  <done>The manifest exposes Florida, Kansas, and North Dakota as published records while optional South Dakota and Wyoming remain unpublished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official state source -> authored Markdown | Current legal status and mechanics cross from official pages/PDFs into repo-authored public prose. |
| authored Markdown -> manifest | New state files become public only when their state slug has a compatible manifest row. |
| generated priority queue -> publication work | Queue data guides ordering but must not replace official-source checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | `content/states/kansas-sb-352.md` | mitigate | Verify the official Kansas history page and require `statusAsOf: 2026-04-10`, avoiding the stale Phase 14 queue date. |
| T-15-01-02 | Repudiation | failed-state status claims | mitigate | Include official source links, `statusAsOf`, `lastReviewed`, source trail sections, and validation through `StateEntryFrontmatterSchema`. |
| T-15-01-03 | Information Disclosure | optional candidate boundary | mitigate | Keep South Dakota and Wyoming manifest rows `unresearched`; do not create public Markdown for optional candidates in this plan. |
| T-15-01-04 | Tampering | public route data | mitigate | Do not edit route files; publish through Markdown plus manifest so later compilation uses the existing canonical path. |
</threat_model>

<verification>
Run after both tasks:

```bash
bun run validate:content
rg -n "statusAsOf: 2026-04-10|Died in Committee" content/states/kansas-sb-352.md
rg -n '"slug": "(florida|kansas|north-dakota)"' content/data/state-registry-manifest.json
```
</verification>

<success_criteria>
- Florida, Kansas, and North Dakota Markdown files exist under `content/states/`.
- Each entry has official primary sources, status date, review date, classification, confidence, source trail, and policy-effect summary.
- Kansas uses the official 2026-04-10 died-in-committee posture.
- Manifest rows for Florida, Kansas, and North Dakota are marked `published`; South Dakota and Wyoming remain `unresearched`.
- `bun run validate:content` passes after the three entries are published.
</success_criteria>

<output>
After completion, create `.planning/phases/15-first-expansion-publication-wave/15-01-SUMMARY.md`.
</output>
