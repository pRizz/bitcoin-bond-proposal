---
phase: 15-first-expansion-publication-wave
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - content/data/state-candidate-intake.json
  - src/lib/site/content.test.ts
  - generated/content-graph.json
  - generated/refresh/state-refresh-queue.json
  - generated/refresh/state-refresh-queue.md
  - generated/refresh/state-priority-queue.json
  - generated/refresh/state-priority-queue.md
autonomous: true
requirements:
  - CATA-14
  - CATA-15
  - REFR-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-05-31T23-51-15
generated_at: 2026-06-01T00:27:02Z
must_haves:
  truths:
    - "The compiled content graph contains 15 published state entries and the five required Phase 15 state slugs."
    - "New state records render through the existing `/states/[slug]` data path without route-specific exceptions."
    - "Published candidates are removed from unpublished candidate intake so refresh/candidate generation stays truthful after publication."
    - "Refresh priority artifacts still surface published refresh work ahead of remaining candidate work."
    - "Content validation, compilation, refresh generation, TypeScript, tests, build, and precommit pass after the expansion batch."
  artifacts:
    - path: "content/data/state-candidate-intake.json"
      provides: "Remaining unpublished candidate intake after the required batch is published"
      contains: "\"south-dakota\""
    - path: "generated/content-graph.json"
      provides: "Compiled public route data for the expanded registry"
      contains: "\"publishedSlugs\""
    - path: "generated/refresh/state-priority-queue.json"
      provides: "Regenerated combined refresh/candidate priority artifact"
      contains: "\"publishedRefreshWork\""
    - path: "generated/refresh/state-priority-queue.md"
      provides: "Maintainer-readable refresh and remaining candidate queue"
      contains: "Published Refresh Work"
  key_links:
    - from: "content/states/*.md"
      to: "generated/content-graph.json"
      via: "bun run compile:content"
      pattern: "\"florida\""
    - from: "content/data/state-candidate-intake.json"
      to: "generated/refresh/state-priority-queue.json"
      via: "bun run refresh:queue"
      pattern: "\"firstPublicationCandidates\""
    - from: "generated/content-graph.json"
      to: "src/routes/(site)/states/[slug].tsx"
      via: "src/lib/site/content.ts import"
      pattern: "getStateBySlug"
    - from: "src/routes/(site)/states/index.tsx"
      to: "src/routes/(site)/states/[slug].tsx"
      via: "StateCard canonical slug href"
      pattern: "/states/${state.slug}"
---

<objective>
Regenerate the public content graph and refresh artifacts, clean up candidate intake, and run the full Phase 15 verification contract.

Purpose: This proves the first expansion batch compiles into the existing canonical content path, keeps unpublished candidate artifacts honest, and satisfies the required generated-content count/slug check.
Output: Updated generated content graph, refresh/priority artifacts, remaining-candidate intake, and full verification evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
@.planning/phases/15-first-expansion-publication-wave/15-RESEARCH.md
@.planning/phases/15-first-expansion-publication-wave/15-UI-SPEC.md
@.planning/phases/15-first-expansion-publication-wave/15-01-SUMMARY.md
@.planning/phases/15-first-expansion-publication-wave/15-02-SUMMARY.md
@content/data/state-candidate-intake.json
@content/data/state-registry-manifest.json
@generated/content-graph.json
@generated/refresh/state-priority-queue.json
@generated/refresh/state-priority-queue.md
@scripts/compile-content.ts
@scripts/build-refresh-queue.ts
@scripts/precommit.ts
@src/lib/site/content.ts
@src/routes/(site)/states/[slug].tsx
@package.json

<interfaces>
Compiled graph shape from `scripts/compile-content.ts`:
```typescript
return {
	states: compiledStates,
	registry: {
		manifest: registryManifest,
		publishedSlugs: compiledStates.map((state) => state.slug),
		generatedAt,
		groups: buildRegistryGroups(compiledStates),
	},
	taxonomy,
};
```

Existing route accessor from `src/lib/site/content.ts`:
```typescript
export function getStateBySlug(slug: string): PublishedState | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove published required states from candidate intake</name>
  <files>content/data/state-candidate-intake.json</files>
  <read_first>
    - content/data/state-candidate-intake.json
    - content/data/README.md
    - content/data/state-registry-manifest.json
    - src/lib/content/schema.ts
    - .planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
  </read_first>
  <action>Update `content/data/state-candidate-intake.json` so it remains an unpublished-candidate artifact after the required publication batch. Remove exactly these published candidate rows: `florida`, `kansas`, `north-dakota`, `ohio`, and `utah`. Keep exactly these remaining rows: `south-dakota` and `wyoming`, with their existing `readiness: "needs-status-confirmation"` and `nextAction: "confirm-final-status"` values. Do not publish South Dakota or Wyoming in Phase 15; they remain optional/deferred per D-03 and the phase constraints.</action>
  <verify>
    <automated>bun -e 'import { parseStateCandidateIntake } from "./src/lib/content/schema.ts"; const data = await Bun.file("content/data/state-candidate-intake.json").json(); const parsed = parseStateCandidateIntake(data); const slugs = parsed.candidates.map((entry) => entry.slug).sort(); if (slugs.join(",") !== "south-dakota,wyoming") throw new Error(`Unexpected candidate slugs: ${slugs.join(",")}`);'</automated>
    <automated>bun -e 'const manifest = await Bun.file("content/data/state-registry-manifest.json").json(); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); for (const slug of ["south-dakota","wyoming"]) { if (rows.get(slug)?.registryStatus !== "unresearched") throw new Error(`${slug} should remain unresearched`); }'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '"slug": "(south-dakota|wyoming)"' content/data/state-candidate-intake.json` returns exactly 2 matches.
    - `rg -n '"slug": "(florida|kansas|north-dakota|ohio|utah)"' content/data/state-candidate-intake.json` returns no matches.
    - `bun -e 'import { parseStateCandidateIntake } from "./src/lib/content/schema.ts"; const data = await Bun.file("content/data/state-candidate-intake.json").json(); const parsed = parseStateCandidateIntake(data); if (parsed.candidates.length !== 2) process.exit(1);'` exits 0.
  </acceptance_criteria>
  <done>Candidate intake no longer lists newly published states as unpublished work, while South Dakota and Wyoming remain separable follow-up candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate content graph and refresh artifacts with targeted slug checks</name>
  <files>generated/content-graph.json, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - scripts/compile-content.ts
    - scripts/build-refresh-queue.ts
    - src/lib/site/content.ts
    - src/routes/(site)/states/[slug].tsx
    - content/data/state-registry-manifest.json
    - content/data/state-candidate-intake.json
    - generated/content-graph.json
    - generated/refresh/state-priority-queue.json
  </read_first>
  <action>Run `bun run validate:content`, `bun run compile:content`, and `bun run refresh:queue` in that order, per D-12, D-13, and D-14. Do not create route-specific exceptions, slug-specific imports, or alternate templates. The required route slugs are the manifest/frontmatter state slugs `florida`, `kansas`, `north-dakota`, `ohio`, and `utah`; filenames may include bill IDs, but public `/states/[slug]` routes must use the state slugs already present in `content/data/state-registry-manifest.json`.</action>
  <action>Run a targeted generated-content check that proves `generated/content-graph.json` contains exactly 15 state entries, includes `florida`, `kansas`, `north-dakota`, `ohio`, and `utah` in `registry.publishedSlugs`, and maps those slugs to these paths: `content/states/florida-sb-550.md`, `content/states/kansas-sb-352.md`, `content/states/north-dakota-hb-1184.md`, `content/states/ohio-hb-18.md`, and `content/states/utah-hb-230.md`. Run a priority artifact check that fails if any of those five slugs remain in `generated/refresh/state-priority-queue.json.firstPublicationCandidates`, and also fails if `publishedRefreshWork` is empty.</action>
  <action>Run a targeted route/card smoke check for the UI-SPEC route interaction requirement: use `getStatesIndexModel()` to confirm each new slug appears in the state-card data set, use `getStateBySlug(slug)` to confirm each corresponding `/states/[slug]` detail model exists, and confirm `src/routes/(site)/states/index.tsx` still passes the canonical `/states/${state.slug}` href expression into `StateCard`. This is a data-driven route/card smoke check through existing route artifacts; do not add Playwright, route files, or UI code unless this smoke check exposes a real rendering breakage.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bun -e 'const graph = await Bun.file("generated/content-graph.json").json(); const expectedPaths = new Map([["florida","content/states/florida-sb-550.md"],["kansas","content/states/kansas-sb-352.md"],["north-dakota","content/states/north-dakota-hb-1184.md"],["ohio","content/states/ohio-hb-18.md"],["utah","content/states/utah-hb-230.md"]]); if (graph.states.length !== 15) throw new Error(`Expected 15 states, got ${graph.states.length}`); const statesBySlug = new Map(graph.states.map((state) => [state.slug, state])); for (const [slug, path] of expectedPaths) { const state = statesBySlug.get(slug); if (!state) throw new Error(`Missing compiled slug ${slug}`); if (state.path !== path) throw new Error(`${slug} path ${state.path} !== ${path}`); } console.log("Compiled 15 state entries with required Phase 15 slugs: florida,kansas,north-dakota,ohio,utah");'</automated>
    <automated>bun -e 'const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const published = new Set(["florida","kansas","north-dakota","ohio","utah"]); const stale = priority.firstPublicationCandidates.filter((entry) => published.has(entry.slug)).map((entry) => entry.slug); if (stale.length) throw new Error(`Published slugs still in firstPublicationCandidates: ${stale.join(",")}`); if (!priority.publishedRefreshWork?.length) throw new Error("publishedRefreshWork should stay visible"); console.log(`Remaining candidate count: ${priority.firstPublicationCandidates.length}`);'</automated>
    <automated>bun -e 'import { getStateBySlug, getStatesIndexModel } from "./src/lib/site/content.ts"; const slugs = ["florida","kansas","north-dakota","ohio","utah"]; const indexModel = getStatesIndexModel(); const indexSlugs = new Set(indexModel.states.map((state) => state.slug)); if (indexModel.stats.publishedCount !== 15) throw new Error(`Expected 15 state cards, got ${indexModel.stats.publishedCount}`); for (const slug of slugs) { const detail = getStateBySlug(slug); if (!indexSlugs.has(slug)) throw new Error(`Missing state-card data for ${slug}`); if (!detail) throw new Error(`Missing detail model for /states/${slug}`); if (detail.slug !== slug) throw new Error(`Detail slug mismatch for /states/${slug}`); } console.log(`Route/card smoke passed for ${slugs.map((slug) => `/states/${slug}`).join(", ")}`);'</automated>
    <automated>bun -e 'const route = await Bun.file("src/routes/(site)/states/index.tsx").text(); if (!route.includes("href={`/states/${state.slug}`}")) throw new Error("StateCard canonical href is missing from states index route");'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0 and reports 15 published state entries.
    - `bun run compile:content` exits 0 and reports 15 state entries.
    - `bun run refresh:queue` exits 0 and writes both refresh and priority artifacts.
    - `rg -n '"slug": "(florida|kansas|north-dakota|ohio|utah)"' generated/content-graph.json` returns matches for all five required slugs.
    - `rg -n 'content/states/(florida-sb-550|kansas-sb-352|north-dakota-hb-1184|ohio-hb-18|utah-hb-230)\\.md' generated/content-graph.json` returns matches for all five required files.
    - `rg -n "Published Refresh Work|First Publication Candidates|Source Boundaries" generated/refresh/state-priority-queue.md` returns matches.
    - The route/card smoke command confirms all five new state-card data rows and `/states/[slug]` detail models exist, and the states index still passes canonical slug hrefs into `StateCard`.
  </acceptance_criteria>
  <done>The expanded registry compiles to generated route data, the required slugs are present, and remaining candidate artifacts no longer list newly published states.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 15 verification contract</name>
  <files>src/lib/site/content.test.ts, generated/content-graph.json, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - package.json
    - scripts/precommit.ts
    - src/lib/site/content.test.ts
    - .planning/phases/15-first-expansion-publication-wave/15-CONTEXT.md
    - .planning/phases/15-first-expansion-publication-wave/15-UI-SPEC.md
  </read_first>
  <action>Before the full verification run, update only the generated-count expectations in `src/lib/site/content.test.ts` that drift because the compiled registry grew from 10 to 15 states. Target values after the required Phase 15 batch: region counts `midwest: 6`, `northeast: 1`, `south: 6`, `west: 2`; legislative status counts `introduced: 5`, `advanced: 3`, `approved: 1`, `enacted: 2`, `failed: 4`. Do not weaken assertions, remove tests, or change comparison/cluster behavior beyond these count expectations unless a failing test points to a real Phase 15 data-shape change that is backed by generated content.</action>
  <action>Run the full verification set from D-13 after generated artifacts are current: `bun run format:check`, `bun run lint`, `bunx tsc --noEmit`, `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bun test`, `bun run build`, and `bun run precommit`. Review the diff after the commands and confirm only Phase 15 content/data/generated artifacts, `src/lib/site/content.test.ts` count expectations, and planning summaries changed; no `src/routes/`, `src/components/`, `src/styles/`, package, or lockfile changes should be introduced by this phase.</action>
  <verify>
    <automated>bun run format:check</automated>
    <automated>bun run lint</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bun test</automated>
    <automated>bun run build</automated>
    <automated>bun run precommit</automated>
    <automated>test -z "$(git diff --name-only -- src/routes src/components src/styles package.json bun.lock)"</automated>
    <automated>rg -n "midwest: 6|south: 6|west: 2|\\[\\\"introduced\\\", 5\\]|\\[\\\"enacted\\\", 2\\]|\\[\\\"failed\\\", 4\\]" src/lib/site/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run format:check` exits 0.
    - `bun run lint` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - `bun run validate:content` exits 0.
    - `bun run compile:content` exits 0.
    - `bun run refresh:queue` exits 0.
    - `bun test` exits 0.
    - `bun run build` exits 0.
    - `bun run precommit` exits 0.
    - `test -z "$(git diff --name-only -- src/routes src/components src/styles package.json bun.lock)"` exits 0.
    - `rg -n "midwest: 6|south: 6|west: 2|\\[\\\"introduced\\\", 5\\]|\\[\\\"enacted\\\", 2\\]|\\[\\\"failed\\\", 4\\]" src/lib/site/content.test.ts` returns matches for all listed Phase 15 count expectations.
  </acceptance_criteria>
  <done>All required repo-native checks pass and the phase remains a content/data/generated-artifact publication wave, not a UI redesign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content/data -> generated graph | Markdown and manifest data become public route data in `generated/content-graph.json`. |
| candidate intake -> generated priority queue | Remaining unpublished candidate rows become maintainer queue output. |
| generated graph -> public state route | `/states/[slug]` reads compiled data through `src/lib/site/content.ts` with no slug-specific branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | `generated/content-graph.json` | mitigate | Run compile plus targeted count/path/slug checks for all five required Phase 15 slugs. |
| T-15-03-02 | Repudiation | `generated/refresh/state-priority-queue.json` | mitigate | Remove newly published slugs from candidate intake and verify they no longer appear as first-publication candidates. |
| T-15-03-03 | Information Disclosure | public routes | mitigate | Verify no route/component/style changes were introduced; new pages flow through existing public route accessors. |
| T-15-03-04 | Denial of Service | build and route generation | mitigate | Run TypeScript, test, build, and precommit after content graph generation to catch compile-time or prerender failures. |
</threat_model>

<verification>
Run after all tasks:

```bash
bun run format:check
bun run lint
bunx tsc --noEmit
bun run validate:content
bun run compile:content
bun run refresh:queue
bun test
bun run build
bun run precommit
```
</verification>

<success_criteria>
- `content/data/state-candidate-intake.json` contains only `south-dakota` and `wyoming` after the five required states are published.
- `generated/content-graph.json` contains 15 state entries and published slugs `florida`, `kansas`, `north-dakota`, `ohio`, and `utah`.
- `generated/refresh/state-priority-queue.json` still contains published refresh work and no longer lists the five newly published slugs as first-publication candidates.
- No route-specific exceptions, UI component changes, package changes, or lockfile changes are introduced.
- All full verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15-first-expansion-publication-wave/15-03-SUMMARY.md`.
</output>
