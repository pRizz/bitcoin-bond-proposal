---
phase: 10-coverage-expansion-contract-and-data-shape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/data/README.md
  - content/data/state-registry-manifest.README.md
  - content/data/state-registry-manifest.json
  - content/states/arizona-sb-1025.md
  - content/states/illinois-hb-1844.md
  - content/states/new-hampshire-bfa-bitcoin-backed-bond.md
  - content/states/oklahoma-hb-1203.md
  - content/states/texas-sb-21.md
  - generated/refresh/README.md
  - src/lib/content/schema.test.ts
  - src/lib/content/schema.ts
autonomous: true
requirements:
  - CATA-08
  - REFR-02
must_haves:
  truths:
    - Maintainer can author canonical region and legislative-status grouping metadata without route-local conventions.
    - Schema validation rejects manifest or published state records that omit the Phase 10 grouping contract.
    - Refresh workflow artifacts have a documented non-published home outside canonical state content.
  artifacts:
    - path: src/lib/content/schema.ts
      provides: Controlled schema for region and legislative-status grouping plus refresh-boundary types.
      exports:
        - StateRegistryManifestEntrySchema
        - StateEntryFrontmatterSchema
        - parseStateRegistryManifest
        - parseStateEntryFrontmatter
    - path: content/data/state-registry-manifest.json
      provides: 50-state manifest with explicit region metadata for every state.
      contains: '"region"'
    - path: generated/refresh/README.md
      provides: Explicit non-published location for future refresh queue and report artifacts.
      contains: not canonical published content
  key_links:
    - from: content/data/state-registry-manifest.json
      to: src/lib/content/schema.ts
      via: parseStateRegistryManifest
      pattern: region
    - from: content/states/illinois-hb-1844.md
      to: src/lib/content/schema.ts
      via: parseStateEntryFrontmatter
      pattern: legislativeStatusGroup
    - from: generated/refresh/README.md
      to: content/data/README.md
      via: documented generated-artifact boundary
      pattern: outside canonical authored content
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-04-11T18-51-47
generated_at: 2026-04-11T19:12:30Z
---

<objective>
Lock the canonical Phase 10 contract for grouping and refresh boundaries before any compile or route work depends on it.

Purpose: Give later compile, comparison, and refresh phases one validated authored source of truth for status grouping, proposal-type grouping reuse, region metadata, and refresh-artifact placement.
Output: Updated schema/test coverage, manifest and published-state contract data, and an explicit non-published refresh-artifact directory contract.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-coverage-expansion-contract-and-data-shape/10-CONTEXT.md
@.planning/phases/10-coverage-expansion-contract-and-data-shape/10-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@content/data/README.md
@content/data/state-registry-manifest.README.md
@content/data/state-registry-manifest.json
@content/docs/methodology.md
@src/lib/content/schema.ts
@src/lib/content/schema.test.ts
@/Users/peterryszkiewicz/Repos/coding-and-architecture-requirements/standards/core/architecture.md
@/Users/peterryszkiewicz/Repos/coding-and-architecture-requirements/standards/core/code-shape.md
@/Users/peterryszkiewicz/Repos/coding-and-architecture-requirements/standards/core/verification.md
@/Users/peterryszkiewicz/Repos/coding-and-architecture-requirements/standards/core/testing.md
@/Users/peterryszkiewicz/Repos/coding-and-architecture-requirements/standards/languages/typescript-javascript.md

<interfaces>
From src/lib/content/schema.ts:
```typescript
export const StateRegistryManifestEntrySchema = z.object({
  state: NonEmptyStringSchema,
  slug: SlugSchema,
  registryStatus: RegistryStatusSchema,
  proposalFocus: ProposalFocusSchema,
  shortNote: NonEmptyStringSchema,
  editorialPriority: EditorialPrioritySchema,
});

export const StateEntryFrontmatterSchema = z.object({
  title: NonEmptyStringSchema,
  slug: SlugSchema,
  summary: NonEmptyStringSchema,
  state: NonEmptyStringSchema,
  recordType: RecordTypeSchema,
  proposalKind: ProposalKindValueSchema,
  proposalSubtype: ProposalSubtypeValueSchema,
  billId: NonEmptyStringSchema,
  chamber: NonEmptyStringSchema,
  status: NonEmptyStringSchema,
  statusAsOf: IsoDateSchema,
  lastReviewed: IsoDateSchema,
  sponsors: z.array(NonEmptyStringSchema).min(1),
  primarySources: z.array(SourceLinkSchema).min(1),
  secondarySources: z.array(SourceLinkSchema).default([]),
  confidence: z.enum(["high", "medium", "low"]),
  effect: NonEmptyStringSchema,
  classificationNote: z.string().trim().optional(),
});
```

From content/data/state-registry-manifest.README.md:
```text
Each state entry should contain only:
- state
- slug
- registryStatus
- proposalFocus
- shortNote
- editorialPriority
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the canonical schema for Phase 10 grouping metadata</name>
  <files>src/lib/content/schema.ts, src/lib/content/schema.test.ts</files>
  <behavior>
    - Test 1: manifest entries parse only when they include a controlled `region` value.
    - Test 2: published state frontmatter parses only when it includes a controlled `legislativeStatusGroup` value.
    - Test 3: invalid region or legislative-status values fail at the schema boundary instead of leaking loose strings downstream.
  </behavior>
  <action>Read `src/lib/content/schema.ts`, `src/lib/content/schema.test.ts`, `content/data/state-registry-manifest.README.md`, and `content/docs/methodology.md` first. Then, per D-01 and D-02, extend the canonical contract with controlled grouping types only for this milestone: add manifest-level `region`, add state-entry `legislativeStatusGroup`, and reuse the existing `proposalFocus`, `proposalKind`, and `proposalSubtype` contract instead of inventing broader taxonomy fields. Export the new inferred types from `schema.ts`, keep the parsing boundary centralized there, and add focused Bun tests with Arrange/Act/Assert coverage for both valid and invalid values.</action>
  <verify>
    <automated>bun test src/lib/content/schema.test.ts</automated>
  </verify>
  <done>The schema layer exposes explicit region and legislative-status grouping types, and the unit tests prove invalid grouping values are rejected before compile or route code runs.</done>
</task>

<task type="auto">
  <name>Task 2: Author manifest regions and document the refresh-artifact boundary</name>
  <files>content/data/README.md, content/data/state-registry-manifest.README.md, content/data/state-registry-manifest.json, generated/refresh/README.md</files>
  <action>Read `content/data/README.md`, `content/data/state-registry-manifest.README.md`, and `content/data/state-registry-manifest.json` first. Then, per D-01, D-03, D-06, and D-07, add a controlled `region` value to every manifest entry, update the manifest README so it explains the new field and the distinction between published state entries and refresh-only artifacts, update the broader data README to keep refresh outputs outside `content/`, and create `generated/refresh/README.md` as the explicit non-published home for later queue or report artifacts. Do not introduce live-tracker semantics, public route inputs, or any extra taxonomy beyond status, proposal type/focus, and region.</action>
  <verify>
    <automated>bun run format:check</automated>
  </verify>
  <done>All 50 manifest entries carry an explicit region, the docs explain the canonical boundary clearly, and the repo has a named refresh-artifact directory outside authored published content.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill the current published state entries to the new grouping contract</name>
  <files>content/states/arizona-sb-1025.md, content/states/illinois-hb-1844.md, content/states/new-hampshire-bfa-bitcoin-backed-bond.md, content/states/oklahoma-hb-1203.md, content/states/texas-sb-21.md</files>
  <action>Read `content/states/illinois-hb-1844.md` and `content/states/new-hampshire-bfa-bitcoin-backed-bond.md` first, then update all currently published state files to include the new `legislativeStatusGroup` frontmatter required by Task 1. Choose values that match the existing plain-English status text, preserve the current descriptive narrative, and keep the grouping surface limited to Phase 10 scope per D-02. Do not expand research depth, add new states, or rewrite editorial copy beyond the minimum needed to keep frontmatter and the status table aligned.</action>
  <verify>
    <automated>bun run validate:content && bun run compile:content</automated>
  </verify>
  <done>The five published state entries validate under the stricter contract, compile into the content graph successfully, and each record now carries explicit legislative-status grouping metadata for later grouping and comparison reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical JSON/Markdown → schema parser | Maintainer-authored manifest and state-entry data crosses into trusted domain types here |
| generated refresh artifacts → repo readers | Non-published workflow files must stay clearly outside canonical public content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `src/lib/content/schema.ts` + authored manifest/state data | mitigate | Add controlled enums for `region` and `legislativeStatusGroup` so malformed grouping data is rejected at parse time instead of silently reaching compile and route code. |
| T-10-02 | I | `content/` versus `generated/refresh/` boundary | mitigate | Document and enforce a dedicated `generated/refresh/` home for refresh artifacts, and update canonical data docs so queued/report outputs are not mistaken for published state content. |
</threat_model>

<verification>
- `bun run format:check`
- `bun test src/lib/content/schema.test.ts`
- `bun run validate:content`
- `bun run compile:content`
</verification>

<success_criteria>
- Manifest, schema, and published-state frontmatter encode region and legislative-status grouping consistently.
- `generated/refresh/README.md` and `content/data/README.md` make the refresh-artifact boundary explicit and outside canonical published content.
- Content validation and compile complete without route-local exceptions or ad hoc grouping rules.
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-expansion-contract-and-data-shape/10-01-SUMMARY.md`
</output>
