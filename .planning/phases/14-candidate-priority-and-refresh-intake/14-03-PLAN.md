---
phase: 14-candidate-priority-and-refresh-intake
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - scripts/build-refresh-queue.ts
  - generated/refresh/state-refresh-queue.json
  - generated/refresh/state-refresh-queue.md
  - generated/refresh/state-priority-queue.json
  - generated/refresh/state-priority-queue.md
  - generated/refresh/README.md
autonomous: true
requirements:
  - CATA-11
  - REFR-04
  - REFR-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-05-27T13-54-27
generated_at: 2026-05-27T14:12:47Z
must_haves:
  truths:
    - "Maintainer can run `bun run refresh:queue` and receive the existing published refresh queue plus new combined priority artifacts."
    - "The combined priority artifacts include machine-readable JSON and maintainer-readable Markdown with timestamp, summary counts, source boundaries, published refresh work, and first publication candidates."
    - "All generated refresh and priority artifacts remain under `generated/refresh/`."
    - "Existing `state-refresh-queue.{json,md}` outputs preserve their current behavior while the combined artifacts are added."
  artifacts:
    - path: "scripts/build-refresh-queue.ts"
      provides: "Single generator for refresh and combined priority artifacts"
      contains: "state-priority-queue"
    - path: "generated/refresh/state-priority-queue.json"
      provides: "Machine-readable combined priority queue"
      contains: "\"firstPublicationCandidates\""
    - path: "generated/refresh/state-priority-queue.md"
      provides: "Maintainer-readable combined priority queue"
      contains: "First Publication Candidates"
    - path: "generated/refresh/README.md"
      provides: "Generated artifact boundary documentation"
      contains: "state-priority-queue"
  key_links:
    - from: "scripts/build-refresh-queue.ts"
      to: "content/data/state-candidate-intake.json"
      via: "readFile plus parseStateCandidateIntake"
      pattern: "parseStateCandidateIntake"
    - from: "scripts/build-refresh-queue.ts"
      to: "src/lib/site/candidate-priority.ts"
      via: "buildCombinedPriorityQueueModel"
      pattern: "buildCombinedPriorityQueueModel"
    - from: "scripts/build-refresh-queue.ts"
      to: "generated/refresh/state-priority-queue.json"
      via: "writeFile"
      pattern: "state-priority-queue\\.json"
---

<objective>
Wire candidate priority into the repo-owned refresh generator and write the combined generated artifacts.

Purpose: This extends `bun run refresh:queue` so maintainers can choose Phase 15 work from both stale published records and unpublished candidate readiness, satisfying D-01, D-02, D-03, D-12, D-13, CATA-11, REFR-04, and REFR-06.
Output: `generated/refresh/state-priority-queue.{json,md}` plus updated generated-artifact docs, while preserving `state-refresh-queue.{json,md}`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-RESEARCH.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-01-SUMMARY.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-02-SUMMARY.md
@scripts/build-refresh-queue.ts
@src/lib/content/schema.ts
@src/lib/site/candidate-priority.ts
@src/lib/site/content.ts
@generated/refresh/README.md
@package.json

<interfaces>
Created by Plan 14-01:
```typescript
export function parseStateCandidateIntake(
	rawCandidates: unknown,
): StateCandidateIntake;
```

Created by Plan 14-02:
```typescript
export function buildCombinedPriorityQueueModel(input: {
	refreshQueue: RefreshQueueModel;
	candidates: ReadonlyArray<StateCandidateIntakeEntry>;
	generatedAt: string;
}): CombinedPriorityQueueModel;
```

Existing generator contract from `scripts/build-refresh-queue.ts`:
```typescript
const queue = getRefreshQueueModel();
await Promise.all([
	writeFile(`${jsonPath}`, `${JSON.stringify(queue, null, "\t")}\n`),
	writeFile(markdownPath, renderMarkdown(queue)),
]);
console.log("Wrote generated/refresh/state-refresh-queue.{json,md}");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend refresh generator to write combined priority artifacts</name>
  <files>scripts/build-refresh-queue.ts, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-RESEARCH.md
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-01-SUMMARY.md
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-02-SUMMARY.md
    - scripts/build-refresh-queue.ts
    - src/lib/content/schema.ts
    - src/lib/site/candidate-priority.ts
    - src/lib/site/content.ts
    - generated/refresh/state-refresh-queue.json
    - generated/refresh/state-refresh-queue.md
  </read_first>
  <action>Modify `scripts/build-refresh-queue.ts` so `bun run refresh:queue` still writes `generated/refresh/state-refresh-queue.json` and `generated/refresh/state-refresh-queue.md` with the existing model and Markdown headings, then also writes `generated/refresh/state-priority-queue.json` and `generated/refresh/state-priority-queue.md`, per D-01, D-02, and D-12. Add a `readCandidateIntake()` helper that reads `content/data/state-candidate-intake.json`, parses it with `parseStateCandidateIntake`, and passes `candidateIntake.candidates` plus `getRefreshQueueModel()` into `buildCombinedPriorityQueueModel`. Render priority Markdown with these exact top-level sections: `# State Priority Queue`, `## Summary`, `## Source Boundaries`, `## Published Refresh Work`, `## First Publication Candidates`, and `## Candidate Intake Notes`. Include generated timestamp, summary counts, and separate tables for published refresh work and first publication candidates. Add an `escapeMarkdownCell(value: string)` helper that replaces `|` with `\\|` and newlines with spaces before rendering freeform `status`, `evidenceNote`, `nextAction`, `deferralReason`, or `reasons` values. End the script with this exact log text: `Wrote generated/refresh/state-refresh-queue.{json,md} and generated/refresh/state-priority-queue.{json,md}`.</action>
  <verify>
    <automated>bun run refresh:queue</automated>
    <automated>bun -e 'import priority from "./generated/refresh/state-priority-queue.json"; if (!priority.firstPublicationCandidates?.length) process.exit(1);'</automated>
    <automated>test -f generated/refresh/state-priority-queue.md</automated>
  </verify>
  <acceptance_criteria>
    - `bun run refresh:queue` exits 0 and prints `Wrote generated/refresh/state-refresh-queue.{json,md} and generated/refresh/state-priority-queue.{json,md}`.
    - `test -f generated/refresh/state-priority-queue.json` exits 0.
    - `test -f generated/refresh/state-priority-queue.md` exits 0.
    - `bun -e 'import priority from "./generated/refresh/state-priority-queue.json"; if (!priority.sourceBoundary || !priority.publishedRefreshWork || !priority.firstPublicationCandidates) process.exit(1);'` exits 0.
    - `rg -n "State Priority Queue|Published Refresh Work|First Publication Candidates|Source Boundaries" generated/refresh/state-priority-queue.md` returns matches.
    - `rg -n "State Refresh Queue|Review Due|Review Soon" generated/refresh/state-refresh-queue.md` returns matches.
  </acceptance_criteria>
  <done>`bun run refresh:queue` preserves existing refresh artifacts and adds combined priority JSON/Markdown under `generated/refresh/`.</done>
</task>

<task type="auto">
  <name>Task 2: Document generated priority artifacts and run full Phase 14 verification</name>
  <files>generated/refresh/README.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - generated/refresh/README.md
    - package.json
    - scripts/precommit.ts
  </read_first>
  <action>Update `generated/refresh/README.md` so `Current generated outputs` lists `generated/refresh/state-priority-queue.json` as the machine-readable combined refresh/candidate priority queue and `generated/refresh/state-priority-queue.md` as the maintainer-readable combined refresh/candidate priority queue. Keep the existing `state-refresh-queue.{json,md}` bullets. State that generated priority artifacts must remain under `generated/refresh/` and must not replace `content/states/*.md` records, per D-07 and REFR-06. Run the full Phase 14 verification set requested in D-13 in this order: `bun run format:check`, `bun run lint`, `bunx tsc --noEmit`, `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bun test`, `bun run build`, and `bun run precommit`.</action>
  <verify>
    <automated>bun run format:check</automated>
    <automated>bun run lint</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bun test</automated>
    <automated>bun run build</automated>
    <automated>bun run precommit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "state-priority-queue\\.json|state-priority-queue\\.md|must not replace" generated/refresh/README.md` returns matches.
    - `git diff --quiet -- content/states` exits 0.
    - `bun run format:check` exits 0.
    - `bun run lint` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - `bun run validate:content` exits 0.
    - `bun run compile:content` exits 0.
    - `bun run refresh:queue` exits 0.
    - `bun test` exits 0.
    - `bun run build` exits 0.
    - `bun run precommit` exits 0.
  </acceptance_criteria>
  <done>Generated artifact documentation names the new combined queue outputs and all requested Phase 14 verification commands pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file system -> generator | `scripts/build-refresh-queue.ts` reads controlled JSON and generated content graph data, then writes generated artifacts. |
| generated artifact -> maintainer reader | Markdown/JSON outputs include freeform evidence notes that must not be confused with canonical public content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | Markdown rendering | mitigate | Escape pipe and newline characters in Markdown table cells before writing candidate evidence, status, next-action, and reasons. |
| T-14-03-02 | Repudiation | generated priority artifacts | mitigate | Include `generatedAt`, summary counts, source boundaries, and deterministic model output in both JSON and Markdown. |
| T-14-03-03 | Information Disclosure | generated/refresh boundary | mitigate | Write only under `generated/refresh/`, update `generated/refresh/README.md`, and verify no `content/states` diff. |
| T-14-03-04 | Denial of Service | `bun run refresh:queue` | accept | The script reads small repo-controlled JSON and generated graph files; failures should be visible through thrown parser or write errors. |
</threat_model>

<verification>
Run after both tasks:

```bash
bun run format:check
bun run lint
bunx tsc --noEmit
bun run validate:content
bun run compile:content
bun run refresh:queue
bun test
bun run build
bun run precommit
git diff --quiet -- content/states
```
</verification>

<success_criteria>
- `bun run refresh:queue` writes `state-refresh-queue.{json,md}` and `state-priority-queue.{json,md}` under `generated/refresh/`.
- The priority JSON contains `publishedRefreshWork` and `firstPublicationCandidates`.
- The priority Markdown has separate published refresh and first publication candidate sections.
- `generated/refresh/README.md` documents all four generated refresh artifacts.
- All requested verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-candidate-priority-and-refresh-intake/14-03-SUMMARY.md`.
</output>
