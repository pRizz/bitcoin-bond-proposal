---
phase: 14-candidate-priority-and-refresh-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/schema.ts
  - src/lib/content/schema.test.ts
  - content/data/state-candidate-intake.json
  - content/data/README.md
autonomous: true
requirements:
  - CATA-10
  - REFR-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-05-27T13-54-27
generated_at: 2026-05-27T14:12:47Z
must_haves:
  truths:
    - "Maintainer can review unpublished candidate intake as controlled repo data, not public state-entry content."
    - "Candidate intake rows classify source availability, proposal relevance, readiness, status, status date, official source URL availability, evidence note, next action, and deferral reason."
    - "Ready-to-author candidates require official legislature or state-source evidence."
  artifacts:
    - path: "src/lib/content/schema.ts"
      provides: "Zod parser and TypeScript types for candidate intake"
      contains: "parseStateCandidateIntake"
    - path: "src/lib/content/schema.test.ts"
      provides: "Parser regression tests for official-source readiness and deferral rules"
      contains: "parseStateCandidateIntake"
    - path: "content/data/state-candidate-intake.json"
      provides: "Controlled unpublished candidate intake rows"
      contains: "\"candidates\""
    - path: "content/data/README.md"
      provides: "Boundary docs for candidate intake as non-public controlled data"
      contains: "state-candidate-intake.json"
  key_links:
    - from: "content/data/state-candidate-intake.json"
      to: "src/lib/content/schema.ts"
      via: "parseStateCandidateIntake"
      pattern: "parseStateCandidateIntake"
    - from: "content/data/README.md"
      to: "content/states/*.md boundary"
      via: "explicit non-public candidate intake note"
      pattern: "must not create public state pages"
---

<objective>
Define the Phase 14 candidate intake contract and seed the controlled unpublished-candidate data source.

Purpose: This gives maintainers reviewable repo data for candidate triage without creating public state records, satisfying D-04 through D-07 and REFR-06.
Output: A parsed `content/data/state-candidate-intake.json` source with tests proving official-source readiness and deferral invariants.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
@.planning/phases/14-candidate-priority-and-refresh-intake/14-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/lib/content/schema.ts
@src/lib/content/schema.test.ts
@content/data/README.md
@content/data/state-registry-manifest.json
@content/data/state-registry-manifest.README.md

<interfaces>
Existing parser pattern from `src/lib/content/schema.ts`:
```typescript
export function parseStateRegistryManifest(
	rawManifest: unknown,
): StateRegistryManifest {
	return StateRegistryManifestSchema.parse(rawManifest);
}
```

Existing test style from `src/lib/content/schema.test.ts` uses `bun:test`, `describe`, `test`, `expect`, and explicit `// Arrange`, `// Act`, `// Assert` sections.

Create these exported contracts:
```typescript
export type CandidateSourceAvailability =
	| "official-bill-page"
	| "official-pdf-only"
	| "official-state-record"
	| "secondary-only"
	| "none-found";

export type CandidateProposalRelevance = "high" | "medium" | "low";
export type CandidateReadiness =
	| "ready-to-author"
	| "needs-status-confirmation"
	| "defer";
export type CandidateNextAction =
	| "author-state-entry"
	| "confirm-final-status"
	| "defer-until-stronger-official-source";

export type StateCandidateIntakeEntry = {
	state: string;
	slug: string;
	sourceAvailability: CandidateSourceAvailability;
	proposalRelevance: CandidateProposalRelevance;
	readiness: CandidateReadiness;
	status: string;
	statusAsOf: string;
	officialSourceUrl?: string;
	evidenceNote: string;
	nextAction: CandidateNextAction;
	deferralReason?: string;
};

export type StateCandidateIntake = {
	candidates: StateCandidateIntakeEntry[];
};

export function parseStateCandidateIntake(
	rawCandidates: unknown,
): StateCandidateIntake;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add candidate intake schema and parser tests</name>
  <files>src/lib/content/schema.ts, src/lib/content/schema.test.ts</files>
  <read_first>
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - AGENTS.bright-builds.md
    - src/lib/content/schema.ts
    - src/lib/content/schema.test.ts
  </read_first>
  <behavior>
    - Test 1: `parseStateCandidateIntake` accepts a `ready-to-author` row when `sourceAvailability` is `official-bill-page` and `officialSourceUrl` is a valid URL.
    - Test 2: `parseStateCandidateIntake` rejects a `ready-to-author` row when `sourceAvailability` is `secondary-only`.
    - Test 3: `parseStateCandidateIntake` rejects a row with `readiness: "defer"` when `deferralReason` is missing or blank.
    - Test 4: `parseStateCandidateIntake` accepts terminal source-rich rows with statuses such as `Died in Banking and Insurance` when the row has official-source evidence, per D-10.
  </behavior>
  <action>Add candidate intake Zod schemas to `src/lib/content/schema.ts` per D-04, D-05, and D-06. Use exact enum values from the `<interfaces>` block: `official-bill-page`, `official-pdf-only`, `official-state-record`, `secondary-only`, `none-found`; `high`, `medium`, `low`; `ready-to-author`, `needs-status-confirmation`, `defer`; `author-state-entry`, `confirm-final-status`, `defer-until-stronger-official-source`. Implement `StateCandidateIntakeEntrySchema.superRefine` so `ready-to-author` requires `sourceAvailability` in `official-bill-page | official-pdf-only | official-state-record` and a valid `officialSourceUrl`, and `defer` requires a non-empty `deferralReason`. Export `StateCandidateIntakeSchema`, `StateCandidateIntakeEntrySchema`, all inferred types, and `parseStateCandidateIntake(rawCandidates: unknown)`. Add focused `bun:test` coverage in `src/lib/content/schema.test.ts` using Arrange/Act/Assert comments.</action>
  <verify>
    <automated>bun test src/lib/content/schema.test.ts</automated>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "StateCandidateIntakeEntrySchema|StateCandidateIntakeSchema|parseStateCandidateIntake" src/lib/content/schema.ts` returns matches.
    - `rg -n "ready-to-author|secondary-only|deferralReason|Died in Banking and Insurance" src/lib/content/schema.test.ts` returns matches.
    - `bun test src/lib/content/schema.test.ts` exits 0.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Candidate intake parsing rejects invalid readiness and deferral states before generator logic can consume the data.</done>
</task>

<task type="auto">
  <name>Task 2: Seed controlled candidate intake data and document its boundary</name>
  <files>content/data/state-candidate-intake.json, content/data/README.md</files>
  <read_first>
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-CONTEXT.md
    - .planning/phases/14-candidate-priority-and-refresh-intake/14-RESEARCH.md
    - content/data/README.md
    - content/data/state-registry-manifest.json
    - content/data/state-registry-manifest.README.md
    - src/lib/content/schema.ts
  </read_first>
  <action>Create `content/data/state-candidate-intake.json` with top-level shape `{ "candidates": [...] }`, per D-04 and D-07. Seed exactly these unpublished candidate slugs from `content/data/state-registry-manifest.json`: `florida`, `ohio`, `kansas`, `north-dakota`, `south-dakota`, `wyoming`, and `utah`. For each row, include every field required by `StateCandidateIntakeEntrySchema`. Use official URLs from `14-RESEARCH.md`: Florida `https://www.flsenate.gov/Session/Bill/2025/550`, Ohio `https://www.legislature.ohio.gov/legislation/136/hb18`, Kansas `https://kslegislature.gov/b2025_26/bills/sb352/`, North Dakota `https://ndlegis.gov/assembly/69-2025/regular/bill-overview/bo1184.html?bill_number=1184&amp;bill_year=2025`, South Dakota `https://mylrc.sdlegislature.gov/api/Documents/298084.pdf`, Wyoming `https://www.wyoleg.gov/2025/Introduced/HB0201.pdf`, and Utah `https://le.utah.gov/Session/2025/bills/static/HB0230.html`. Classify Florida, Ohio, Kansas, and North Dakota as `sourceAvailability: "official-bill-page"`; South Dakota, Wyoming, and Utah as `official-pdf-only` unless the static official bill page supplies the status date during implementation. Set `readiness: "ready-to-author"` only for rows with official status and official source evidence sufficient to author; set `readiness: "needs-status-confirmation"` and `nextAction: "confirm-final-status"` for South Dakota and Wyoming when the official status cannot be confirmed from the static source. Do not create or modify any `content/states/*.md` file. Update `content/data/README.md` with a short bullet naming `state-candidate-intake.json` as controlled maintainer input that is not canonical public state content and must be parsed before use.</action>
  <verify>
    <automated>bun -e 'import data from "./content/data/state-candidate-intake.json"; if (data.candidates.length !== 7) process.exit(1);'</automated>
    <automated>bun test src/lib/content/schema.test.ts</automated>
    <automated>git diff --name-only -- content/states</automated>
  </verify>
  <acceptance_criteria>
    - `test -f content/data/state-candidate-intake.json` exits 0.
    - `bun -e 'import { parseStateCandidateIntake } from "./src/lib/content/schema.ts"; import data from "./content/data/state-candidate-intake.json"; const parsed = parseStateCandidateIntake(data); if (parsed.candidates.length !== 7) process.exit(1);'` exits 0.
    - `rg -n '"slug": "(florida|ohio|kansas|north-dakota|south-dakota|wyoming|utah)"' content/data/state-candidate-intake.json` returns 7 matches.
    - `rg -n "state-candidate-intake.json|not canonical public state content" content/data/README.md` returns matches.
    - `git diff --name-only -- content/states` prints no file paths.
  </acceptance_criteria>
  <done>The repo contains a parsed seven-row candidate intake snapshot under `content/data/` and no Phase 14 public state-entry changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controlled JSON -> TypeScript model | `content/data/state-candidate-intake.json` is editable repo data and must be parsed before business logic consumes it. |
| candidate intake -> public content boundary | Candidate rows must not create public state pages or route data in Phase 14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | `parseStateCandidateIntake` | mitigate | Validate candidate rows with Zod enums and `superRefine` so secondary-only or source-missing rows cannot be marked `ready-to-author`. |
| T-14-01-02 | Information Disclosure | `content/data/state-candidate-intake.json` | mitigate | Document candidate intake as maintainer input, keep it out of `content/states/`, and verify `git diff --name-only -- content/states` is empty. |
| T-14-01-03 | Repudiation | candidate status dates | mitigate | Require `statusAsOf` in `YYYY-MM-DD` format and official-source evidence for ready rows so dated-source claims are reviewable in git diffs. |
</threat_model>

<verification>
Run after both tasks:

```bash
bun test src/lib/content/schema.test.ts
bunx tsc --noEmit
git diff --name-only -- content/states
```
</verification>

<success_criteria>
- `parseStateCandidateIntake` exists and exports inferred candidate intake types.
- `content/data/state-candidate-intake.json` parses successfully and contains seven unpublished candidate rows.
- `content/data/README.md` states the candidate intake boundary.
- No `content/states/*.md` file is created or modified.
</success_criteria>

<output>
After completion, create `.planning/phases/14-candidate-priority-and-refresh-intake/14-01-SUMMARY.md`.
</output>
