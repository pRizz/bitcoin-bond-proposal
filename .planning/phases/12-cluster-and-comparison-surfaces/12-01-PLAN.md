---
phase: 12-cluster-and-comparison-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site/content.ts
  - src/lib/site/content.test.ts
  - src/routes/(site)/states/index.tsx
  - src/routes/(site)/states/clusters.tsx
autonomous: true
requirements:
  - SITE-07
must_haves:
  truths:
    - Reader can browse grouped views by legislative status, proposal type, and region using a dedicated cluster surface rather than only the base `/states` index.
    - Cluster reading paths reuse the shared registry model instead of recomputing grouping logic inside routes.
    - Every cluster section cross-links back to canonical state detail pages.
  artifacts:
    - path: src/lib/site/content.ts
      provides: Shared cluster-route model derived from the compiled ten-state registry graph.
      exports:
        - getStatesClusterModel
    - path: src/lib/site/content.test.ts
      provides: Unit coverage for cluster grouping and route-model behavior.
      contains: getStatesClusterModel
    - path: src/routes/(site)/states/clusters.tsx
      provides: Dedicated cluster reading path for grouped browsing by status, type, and region.
      contains: Cluster reading paths
    - path: src/routes/(site)/states/index.tsx
      provides: Gateway links from `/states` into the cluster surface.
      contains: /states/clusters
  key_links:
    - from: src/lib/site/content.ts
      to: generated/content-graph.json
      via: cluster route model derived from shared registry groups
      pattern: getStatesClusterModel
    - from: src/routes/(site)/states/clusters.tsx
      to: src/lib/site/content.ts
      via: shared cluster-route selector
      pattern: getStatesClusterModel
    - from: src/routes/(site)/states/clusters.tsx
      to: src/routes/(site)/states/[slug].tsx
      via: state detail links in grouped sections
      pattern: /states/
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-04-11T21-58-38
generated_at: 2026-04-11T22:10:00Z
---

<objective>
Build the dedicated cluster reading path for the ten-state registry so readers can browse
grouped views by status, proposal type, and region without turning `/states` into a
dashboard.

Purpose: Satisfy `SITE-07` with an editorial cluster route and shared grouped route model.
Output: Cluster route model helpers, unit coverage, a new `/states/clusters` route, and
gateway links from the existing `/states` index.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-CONTEXT.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-RESEARCH.md
@.planning/phases/11-broader-national-publication-batch/11-01-SUMMARY.md
@.planning/phases/11-broader-national-publication-batch/11-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/lib/site/content.ts
@src/lib/site/content.test.ts
@src/routes/(site)/states/index.tsx
@src/routes/(site)/states/[slug].tsx
@src/components/editorial/StateCard.tsx
@src/components/editorial/PageSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared cluster-route model with unit coverage</name>
  <files>src/lib/site/content.ts, src/lib/site/content.test.ts</files>
  <behavior>
    - Test 1: the shared cluster model returns grouped sections for legislative status, proposal focus/type, and region using the existing ten-state graph.
    - Test 2: each cluster section keeps links or state entries that point back to canonical state detail pages.
    - Test 3: the cluster model reuses existing grouped registry data instead of recomputing ad hoc buckets in route code.
  </behavior>
  <action>Read `src/lib/site/content.ts`, `src/lib/site/content.test.ts`, and `generated/content-graph.json` first. Then add one shared selector such as `getStatesClusterModel()` that turns the existing grouped registry buckets into route-ready editorial sections, with clear section titles, short explainer copy, and state-level drill-down data for status, proposal focus/type, and region. Keep the grouping logic inside the shared site model and cover it with focused Bun tests rather than route-level recomputation.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts</automated>
  </verify>
  <done>The shared site model exposes one tested cluster-route selector that transforms the current ten-state registry into browseable grouped sections.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated `/states/clusters` route</name>
  <files>src/routes/(site)/states/clusters.tsx</files>
  <action>Read `src/routes/(site)/states/index.tsx`, `src/routes/(site)/states/[slug].tsx`, `src/components/editorial/PageSection.tsx`, `src/components/editorial/StateCard.tsx`, and the new shared cluster model first. Then create a dedicated `/states/clusters` route that presents grouped sections for status, proposal type/focus, and region using editorial copy plus reusable cards or links back to the canonical state detail pages. Keep the route medium-density and card-led, not table-led or dashboard-like.</action>
  <verify>
    <automated>bun run build && rg -n 'getStatesClusterModel|Cluster reading paths|/states/' src/routes/'(site)'/states/clusters.tsx</automated>
  </verify>
  <done>The new `/states/clusters` route lets readers browse the ten-state registry by status, type, and region while staying clearly within the editorial registry shell.</done>
</task>

<task type="auto">
  <name>Task 3: Add gateway links from the existing states index</name>
  <files>src/routes/(site)/states/index.tsx</files>
  <action>Read the current `/states` route first, then add lightweight gateway links or calls to action that route readers from `/states` into the new cluster reading path without changing the index into a dashboard. Preserve the current filters, grouped chips, and card treatment; just make the cluster surface discoverable from the registry gateway.</action>
  <verify>
    <automated>bun run build && rg -n '/states/clusters' src/routes/'(site)'/states/index.tsx</automated>
  </verify>
  <done>The base `/states` page clearly links into the dedicated cluster route while preserving its role as the registry gateway.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated content graph → shared cluster model | Existing grouped registry data becomes reader-facing browse structure here |
| shared cluster model → `/states/clusters` route | The route must consume one shared browse model rather than inventing its own grouping rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `src/lib/site/content.ts` cluster selectors | mitigate | Derive cluster sections from the shared grouped registry model and cover them with unit tests so route-level grouping drift is caught early. |
| T-12-02 | I | `/states/clusters` | mitigate | Keep the route card-led and cross-linked to canonical detail pages instead of presenting group counts as standalone scoreboards. |
</threat_model>

<verification>
- `bun run format:check`
- `bun run lint`
- `bun test src/lib/site/content.test.ts`
- `bun run build`
</verification>

<success_criteria>
- Readers can browse grouped status, type, and region views on a dedicated cluster route.
- The cluster route uses one shared route model and keeps clear links back to state detail pages.
- `/states` remains a gateway and does not turn into a dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cluster-and-comparison-surfaces/12-01-SUMMARY.md`
</output>
