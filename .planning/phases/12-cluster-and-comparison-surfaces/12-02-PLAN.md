---
phase: 12-cluster-and-comparison-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/lib/site/content.ts
  - src/lib/site/content.test.ts
  - src/routes/(site)/states/compare.tsx
  - src/routes/(site)/states/index.tsx
autonomous: true
requirements:
  - SITE-08
  - SITE-09
must_haves:
  truths:
    - Reader can use a dedicated comparison surface that explains meaningful differences across the current ten-state registry.
    - Comparison sections cross-link directly back to canonical state detail pages.
    - The comparison route stays narrative and editorial rather than turning into a matrix or leaderboard.
  artifacts:
    - path: src/lib/site/content.ts
      provides: Shared comparison-route model derived from the ten-state registry.
      exports:
        - getStatesComparisonModel
    - path: src/lib/site/content.test.ts
      provides: Unit coverage for the comparison model and detail-link wiring.
      contains: getStatesComparisonModel
    - path: src/routes/(site)/states/compare.tsx
      provides: Dedicated editorial comparison route with links back to state detail pages.
      contains: Comparison surfaces
    - path: src/routes/(site)/states/index.tsx
      provides: Gateway links from `/states` into the comparison surface.
      contains: /states/compare
  key_links:
    - from: src/lib/site/content.ts
      to: generated/content-graph.json
      via: shared comparison-route selector
      pattern: getStatesComparisonModel
    - from: src/routes/(site)/states/compare.tsx
      to: src/lib/site/content.ts
      via: shared comparison model
      pattern: getStatesComparisonModel
    - from: src/routes/(site)/states/compare.tsx
      to: src/routes/(site)/states/[slug].tsx
      via: comparison cards or links back to canonical detail pages
      pattern: /states/
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-04-11T21-58-38
generated_at: 2026-04-11T22:10:00Z
---

<objective>
Build the dedicated comparison reading path for the ten-state registry so readers can
understand meaningful differences across reserve, crossover, and bond-side records
without leaving the canonical source trail.

Purpose: Satisfy `SITE-08` and `SITE-09` with a narrative comparison surface and direct
cross-links back to state detail pages.
Output: Comparison route model helpers, unit coverage, a new `/states/compare` route,
and gateway links from the existing `/states` index.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-CONTEXT.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-RESEARCH.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-01-PLAN.md
@.planning/phases/11-broader-national-publication-batch/11-01-SUMMARY.md
@.planning/phases/11-broader-national-publication-batch/11-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/lib/site/content.ts
@src/lib/site/content.test.ts
@src/routes/(site)/states/index.tsx
@src/routes/(site)/states/[slug].tsx
@src/components/editorial/StateCard.tsx
@src/components/editorial/PageSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared comparison-route model with unit coverage</name>
  <files>src/lib/site/content.ts, src/lib/site/content.test.ts</files>
  <behavior>
    - Test 1: the shared comparison model groups the current registry into meaningful editorial frames such as reserve benchmarks, crossover records, and bond-side official signals.
    - Test 2: each comparison section includes state entries or links that point back to canonical detail pages.
    - Test 3: the comparison model stays narrative and selective rather than exposing a giant all-state matrix.
  </behavior>
  <action>Read `src/lib/site/content.ts`, `src/lib/site/content.test.ts`, and the ten-state registry graph first. Then add one shared selector such as `getStatesComparisonModel()` that builds a few editorial comparison sections from the current registry dataset. The sections should explain meaningful differences across reserve-heavy, crossover, and bond-side records while keeping the state-detail links and avoiding matrix-style comparison logic. Cover the selector with focused Bun tests.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts</automated>
  </verify>
  <done>The shared site model exposes one tested comparison-route selector that turns the ten-state registry into a small set of meaningful editorial comparison sections.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated `/states/compare` route</name>
  <files>src/routes/(site)/states/compare.tsx</files>
  <action>Read `src/routes/(site)/states/index.tsx`, `src/routes/(site)/states/[slug].tsx`, `src/components/editorial/PageSection.tsx`, and the new comparison model first. Then create a dedicated `/states/compare` route with narrative comparison sections, short explanatory copy, and selected state cards or links back to canonical detail pages. Keep the route editorial, selective, and mobile-safe; do not build a leaderboard, giant matrix, or dashboard-like summary table.</action>
  <verify>
    <automated>bun run build && rg -n 'getStatesComparisonModel|Comparison surfaces|/states/' src/routes/'(site)'/states/compare.tsx</automated>
  </verify>
  <done>The new `/states/compare` route explains meaningful differences across the current registry and keeps the canonical state-detail pages one click away.</done>
</task>

<task type="auto">
  <name>Task 3: Add gateway links from the existing states index into the comparison route</name>
  <files>src/routes/(site)/states/index.tsx</files>
  <action>Read the current `/states` route first, then add lightweight gateway links or calls to action that route readers from `/states` into the new comparison surface. Preserve the existing index behavior and do not collapse the comparison work back into the gateway route.</action>
  <verify>
    <automated>bun run build && rg -n '/states/compare' src/routes/'(site)'/states/index.tsx</automated>
  </verify>
  <done>The base `/states` page clearly links into the dedicated comparison route while preserving its existing gateway role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated content graph → shared comparison model | Compiled registry data becomes reader-facing comparison structure here |
| shared comparison model → `/states/compare` route | The route must consume one shared comparison model instead of inventing ad hoc pairings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | T | `src/lib/site/content.ts` comparison selectors | mitigate | Centralize comparison-section derivation in one tested selector so routes do not drift into inconsistent grouping or comparison logic. |
| T-12-04 | I | `/states/compare` | mitigate | Keep the route narrative and selective, with explicit links back to canonical records, rather than presenting unsupported ranking or scoring behavior. |
</threat_model>

<verification>
- `bun run format:check`
- `bun run lint`
- `bun test src/lib/site/content.test.ts`
- `bun run build`
</verification>

<success_criteria>
- Readers can use a dedicated comparison surface that explains meaningful differences across the current registry.
- Comparison sections link back to canonical state detail pages.
- `/states` remains a gateway and the new compare route stays editorial rather than dashboard-like.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cluster-and-comparison-surfaces/12-02-SUMMARY.md`
</output>
