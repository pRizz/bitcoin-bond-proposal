---
phase: 17-expanded-surface-qa-and-closeout-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - src/routes/(site)/states/index.tsx
  - src/routes/(site)/states/clusters.tsx
  - src/routes/(site)/states/compare.tsx
  - src/lib/site/states-surfaces.ts
  - src/lib/site/content.test.ts
autonomous: true
requirements:
  - SITE-10
  - SITE-11
  - SITE-12
user_setup: []
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-06-02T21-06-55
generated_at: 2026-06-02T21:24:26Z
must_haves:
  truths:
    - "Reader can scan `/states` at 17 published records with confidence, completeness, freshness, status, proposal-type, region, and canonical drill-down cues intact."
    - "Reader can use `/states/clusters` and `/states/compare` without stale count assumptions, broken grouping, broken links, or matrix/ranking behavior."
    - "Reader can open South Dakota, Wyoming, and older benchmark detail pages through the canonical `/states/[slug]` graph path with provenance and freshness context intact."
    - "Closeout-facing project copy no longer describes the public webapp as having fifteen state-detail pages."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Current milestone state copy that reflects 17 published state-detail pages"
      contains: "seventeen published state-detail pages"
    - path: "src/routes/(site)/states/index.tsx"
      provides: "Medium-density catalog route with derived counts, filters, empty state, and StateCard grid"
      contains: "publishedRecordCount"
    - path: "src/lib/site/states-surfaces.ts"
      provides: "Pure cluster and comparison model copy plus canonical href construction"
      contains: "href: `/states/${"
    - path: "src/lib/site/content.test.ts"
      provides: "Focused stale-copy, count, canonical-link, and representative-detail regression coverage"
      contains: "fifteen published state-detail pages"
  key_links:
    - from: "src/routes/(site)/states/index.tsx"
      to: "src/lib/site/content.ts"
      via: "getStatesIndexModel() and getRegistryFreshnessSummary()"
      pattern: "getStatesIndexModel"
    - from: "src/routes/(site)/states/index.tsx"
      to: "src/components/editorial/StateCard.tsx"
      via: "catalog card href `/states/${state.slug}`"
      pattern: "href={`/states/${state.slug}`}"
    - from: "src/lib/site/states-surfaces.ts"
      to: "src/routes/(site)/states/[slug].tsx"
      via: "cluster/comparison href `/states/${slug}`"
      pattern: "href: `/states/${"
    - from: "src/routes/(site)/states/[slug].tsx"
      to: "src/lib/site/content.ts"
      via: "getStateBySlug(slug)"
      pattern: "getStateBySlug"
---

<objective>
Harden the expanded public registry surfaces before closeout.

Purpose: Phase 16 expanded the registry to 17 published states; this plan removes stale surface and closeout assumptions while preserving the existing medium-density catalog, editorial clusters, narrative comparison route, shared cards, and canonical detail path.
Output: Narrow route/model/doc copy fixes plus focused tests proving derived counts, canonical links, and representative detail consistency.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CONTEXT.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-RESEARCH.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-VERIFICATION.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/routes/(site)/states/index.tsx
@src/routes/(site)/states/clusters.tsx
@src/routes/(site)/states/compare.tsx
@src/routes/(site)/states/[slug].tsx
@src/lib/site/content.ts
@src/lib/site/states-surfaces.ts
@src/lib/site/content.test.ts
@src/components/editorial/StateCard.tsx
@src/components/editorial/RegistryFreshnessPanel.tsx
@generated/content-graph.json
@generated/refresh/state-priority-queue.json
@content/data/state-candidate-intake.json
@package.json

<interfaces>
Existing route/model accessors from `src/lib/site/content.ts`:
```typescript
export function getStatesIndexModel(): StatesIndexModel;
export function getRegistryStats(): RegistryStats;
export function getStatesClusterModel(): StatesClusterModel;
export function getStatesComparisonModel(): StatesComparisonModel;
export function getStateBySlug(slug: string): PublishedState | undefined;
export function getRegistryFreshnessSummary(): RegistryFreshnessSummary;
```

Canonical link shape from `src/lib/site/states-surfaces.ts`:
```typescript
return {
	...maybeState,
	href: `/states/${maybeState.slug}`,
	badgeLabel: getProposalFocusBadgeLabel(maybeState.proposalFocus),
	badgeTone: getProposalFocusBadgeTone(maybeState.proposalFocus),
};
```

Shared card fields from `src/components/editorial/StateCard.tsx`:
```typescript
type StateCardProps = {
	href: string;
	state: string;
	billId: string;
	status: string;
	statusAsOf: string;
	proposalKind: string;
	summary: string;
	significance: string;
	confidenceCue: ConfidenceCue;
	lastReviewed: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale count and unclear empty-state copy</name>
  <files>.planning/PROJECT.md, src/routes/(site)/states/index.tsx, src/routes/(site)/states/clusters.tsx, src/routes/(site)/states/compare.tsx, src/lib/site/states-surfaces.ts</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CONTEXT.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-RESEARCH.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
    - src/routes/(site)/states/index.tsx
    - src/routes/(site)/states/clusters.tsx
    - src/routes/(site)/states/compare.tsx
    - src/lib/site/states-surfaces.ts
  </read_first>
  <action>Implement D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 with narrow edits only. In `.planning/PROJECT.md`, replace the stale public-webapp bullet that currently says `fifteen published state-detail pages` with `seventeen published state-detail pages` or equivalent wording that still describes the same public route set. Do not edit the managed Bright Builds block in `AGENTS.md`, and do not update archived Phase 15 or Phase 16 historical evidence that correctly describes those earlier counts.</action>
  <action>In `src/routes/(site)/states/index.tsx`, keep the medium-density `StateCard` grid and keep count copy derived from `statesIndexModel.stats.publishedCount` via `publishedRecordCount`. Replace the empty state with the exact UI contract copy: heading `No matching state records` and body `Adjust the type filter or return to all published states.` Keep the filter options limited to `all`, `bond`, `reserve`, and `both`; do not add advanced filters, search, a matrix, a ranking table, or any new UI library.</action>
  <action>Scan `src/routes/(site)/states/index.tsx`, `src/routes/(site)/states/clusters.tsx`, `src/routes/(site)/states/compare.tsx`, and `src/lib/site/states-surfaces.ts` for stale assumptions. If the scan finds active-surface terms `fifteen published state-detail pages`, `15-state`, `15 state`, `ten-state`, `ten published records`, hard-coded `17-record` strings where `getRegistryStats()` or `getStatesIndexModel()` is already available, non-canonical state-detail hrefs, or copy that turns `/states/compare` into a ranking/matrix, fix only that specific copy/model expression. Preserve `StateCard`, `RegistryFreshnessPanel`, and `ActionLink`; do not create route-specific South Dakota or Wyoming exceptions.</action>
  <verify>
    <automated>rg -n "seventeen published state-detail pages" .planning/PROJECT.md</automated>
    <automated>bash -euo pipefail -c 'if rg -n "fifteen published state-detail pages|15-state|15 state|ten-state|ten published records" .planning/PROJECT.md "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" "src/routes/(site)/states/compare.tsx" src/lib/site/states-surfaces.ts; then exit 1; fi'</automated>
    <automated>rg -n "const publishedRecordCount = statesIndexModel\\.stats\\.publishedCount" "src/routes/(site)/states/index.tsx"</automated>
    <automated>rg -n "No matching state records|Adjust the type filter or return to all published states\\." "src/routes/(site)/states/index.tsx"</automated>
    <automated>rg -n 'href=\\{`/states/\\$\\{state\\.slug\\}`\\}|href: `/states/\\$\\{.*slug\\}`' "src/routes/(site)/states/index.tsx" src/lib/site/states-surfaces.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `seventeen published state-detail pages`.
    - The active closeout/surface scan command for `fifteen published state-detail pages`, `15-state`, `15 state`, `ten-state`, and `ten published records` exits 0 by finding no matches in `.planning/PROJECT.md`, the three public state route files, or `src/lib/site/states-surfaces.ts`.
    - `src/routes/(site)/states/index.tsx` still contains `const publishedRecordCount = statesIndexModel.stats.publishedCount`.
    - `src/routes/(site)/states/index.tsx` contains `No matching state records`.
    - `src/routes/(site)/states/index.tsx` contains `Adjust the type filter or return to all published states.`
    - Canonical state href checks find `/states/${state.slug}` in the catalog route and `/states/${...slug}` construction in `src/lib/site/states-surfaces.ts`.
    - `package.json` and `bun.lock` are unchanged by this task.
  </acceptance_criteria>
  <done>Active surface and closeout-facing copy reflects the 17-state registry without changing the established registry UI pattern or adding deferred features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused regression coverage for expanded surface assumptions</name>
  <files>src/lib/site/content.test.ts</files>
  <read_first>
    - src/lib/site/content.test.ts
    - src/lib/site/content.ts
    - src/lib/site/states-surfaces.ts
    - src/routes/(site)/states/index.tsx
    - src/routes/(site)/states/clusters.tsx
    - src/routes/(site)/states/compare.tsx
    - src/routes/(site)/states/[slug].tsx
    - generated/content-graph.json
    - .planning/PROJECT.md
  </read_first>
  <behavior>
    - Stale-copy regression scans active state routes, `src/lib/site/states-surfaces.ts`, and `.planning/PROJECT.md` for `fifteen published state-detail pages`, `15-state`, `15 state`, `ten-state`, and `ten published records`.
    - Catalog/index model still reports `publishedCount` 17 and existing Phase 16 group counts: `midwest: 7`, `northeast: 1`, `south: 6`, `west: 3`, failed bucket 6.
    - Cluster and comparison models keep every entry href equal to `/states/${slug}`.
    - Representative detail data for `south-dakota`, `wyoming`, and `texas` exists through `getStateBySlug()` and includes status, `statusAsOf`, `lastReviewed`, `confidenceCue`, and `freshnessCue`.
  </behavior>
  <action>Implement SITE-10, SITE-11, and SITE-12 protection in `src/lib/site/content.test.ts` before broadening any production code. Extend the existing `state surface copy avoids stale hard-coded registry counts` test so `staleCountTerms` includes exactly `fifteen published state-detail pages`, `15-state`, `15 state`, `ten-state`, and `ten published records`, and include `.planning/PROJECT.md` in the scanned files. Keep the test focused on active closeout/surface files; do not scan archived phase summaries whose historical counts are valid.</action>
  <action>Add one focused test for representative detail consistency. Use `getStateBySlug()` for `south-dakota`, `wyoming`, and `texas`; assert each returns a state whose `slug` matches, `status`, `statusAsOf`, `lastReviewed`, `confidenceCue.title`, `confidenceCue.detail`, `freshnessCue.title`, and `freshnessCue.detail` are non-empty strings. Include explicit `// Arrange`, `// Act`, and `// Assert` sections per Bright Builds testing guidance. Do not change `src/routes/(site)/states/[slug].tsx`; this task proves the existing canonical graph path rather than adding state-specific route branches.</action>
  <action>Preserve the existing tests that assert 17 published states, region counts `midwest: 7`, `northeast: 1`, `south: 6`, `west: 3`, failed status count 6, canonical cluster links, canonical comparison links, and selective narrative comparison behavior. If a test fails, fix the narrow stale-copy/link/model defect instead of weakening assertions.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts</automated>
    <automated>rg -n 'fifteen published state-detail pages|15-state|15 state|ten-state|ten published records' src/lib/site/content.test.ts</automated>
    <automated>rg -n 'south-dakota|wyoming|texas|getStateBySlug|freshnessCue|confidenceCue' src/lib/site/content.test.ts</automated>
    <automated>rg -n 'publishedCount\\)\\.toBe\\(17\\)|midwest: 7|northeast: 1|south: 6|west: 3|\\["failed", 6\\]' src/lib/site/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/lib/site/content.test.ts` exits 0.
    - `src/lib/site/content.test.ts` contains all five stale terms: `fifteen published state-detail pages`, `15-state`, `15 state`, `ten-state`, and `ten published records`.
    - `src/lib/site/content.test.ts` contains representative detail assertions for `south-dakota`, `wyoming`, and `texas`.
    - `src/lib/site/content.test.ts` contains `getStateBySlug`, `freshnessCue`, and `confidenceCue` in the representative detail test.
    - Existing 17-state count markers remain present: `publishedCount).toBe(17)`, `midwest: 7`, `northeast: 1`, `south: 6`, `west: 3`, and `["failed", 6]`.
    - No test removes the canonical cluster link, canonical comparison link, or selective narrative comparison assertions.
  </acceptance_criteria>
  <done>Focused tests prevent stale count copy, broken canonical links, and missing representative detail context from regressing after the 17-state expansion.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 17 surface hardening stays narrow</name>
  <files>.planning/PROJECT.md, src/routes/(site)/states/index.tsx, src/routes/(site)/states/clusters.tsx, src/routes/(site)/states/compare.tsx, src/lib/site/states-surfaces.ts, src/lib/site/content.test.ts</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - package.json
    - scripts/precommit.ts
    - src/lib/site/content.test.ts
    - src/routes/(site)/states/index.tsx
    - src/routes/(site)/states/clusters.tsx
    - src/routes/(site)/states/compare.tsx
    - src/lib/site/states-surfaces.ts
  </read_first>
  <action>Run the focused verification for Plan 17-01 after Tasks 1 and 2. Use the repo scripts and targeted scans to prove the public registry surfaces still compile from generated model data, no deferred UI scope was added, and package files were not changed. Do not run the full Phase 17 command ladder here; Plan 17-02 owns the full ladder, generated artifact verification, browser QA, and closeout evidence.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun test src/lib/site/content.test.ts</automated>
    <automated>bash -euo pipefail -c 'if rg -n "mystic-ui|comparison matrix|advanced filter|advanced filtering|search input" package.json bun.lock "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" "src/routes/(site)/states/compare.tsx" src/lib/site/states-surfaces.ts; then exit 1; fi'</automated>
    <automated>test -z "$(git diff --name-only -- package.json bun.lock content/states content/data/state-registry-manifest.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - `bun test src/lib/site/content.test.ts` exits 0.
    - The deferred-scope scan for `mystic-ui`, `comparison matrix`, `advanced filter`, `advanced filtering`, and `search input` returns no matches in package files or public state route/model files.
    - `test -z "$(git diff --name-only -- package.json bun.lock content/states content/data/state-registry-manifest.json)"` exits 0.
  </acceptance_criteria>
  <done>Surface hardening is verified as a narrow QA pass: no new publication work, package/library adoption, advanced filtering, matrix comparison, ranking UI, or route-specific state exception was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated graph -> public routes | `generated/content-graph.json` is adapted by `src/lib/site/content.ts` and rendered through `/states`, `/states/clusters`, `/states/compare`, and `/states/[slug]`. |
| route/model copy -> public policy interpretation | Stale or ranking-like copy can misrepresent the snapshot-based registry even when data is correct. |
| canonical slug links -> detail route | Catalog, cluster, and comparison links must stay inside the canonical `/states/[slug]` path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | `src/routes/(site)/states/index.tsx` count and empty-state copy | mitigate | Keep counts derived from `statesIndexModel.stats.publishedCount`; add grep-verifiable empty-state copy and stale-term scans. |
| T-17-01-02 | Repudiation | `.planning/PROJECT.md` closeout-facing current state | mitigate | Replace stale fifteen-page copy with seventeen-page copy and add test/grep coverage for the stale phrase. |
| T-17-01-03 | Tampering | `src/lib/site/states-surfaces.ts` cluster/comparison hrefs | mitigate | Preserve `/states/${slug}` href construction and test every cluster/comparison entry. |
| T-17-01-04 | Information Disclosure | public route scope | accept | Phase 17 reads only already published public state data; candidate/private intake remains out of rendered route scope. |
| T-17-01-05 | Denial of Service | route/model build path | mitigate | Run `bunx tsc --noEmit` and focused Bun tests after route/model changes. |
</threat_model>

<verification>
Run after all tasks:

```bash
bun run validate:content
bunx tsc --noEmit
bun test src/lib/site/content.test.ts
```

Also run these scans:

```bash
rg -n "seventeen published state-detail pages" .planning/PROJECT.md
bash -euo pipefail -c 'if rg -n "fifteen published state-detail pages|15-state|15 state|ten-state|ten published records" .planning/PROJECT.md "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" "src/routes/(site)/states/compare.tsx" src/lib/site/states-surfaces.ts; then exit 1; fi'
bash -euo pipefail -c 'if rg -n "mystic-ui|comparison matrix|advanced filter|advanced filtering|search input" package.json bun.lock "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" "src/routes/(site)/states/compare.tsx" src/lib/site/states-surfaces.ts; then exit 1; fi'
test -z "$(git diff --name-only -- package.json bun.lock content/states content/data/state-registry-manifest.json)"
```
</verification>

<success_criteria>
- SITE-10: `/states` preserves the current StateCard catalog pattern, derived counts, freshness panel, status/freshness/proposal/region cues, and a clear empty state.
- SITE-11: `/states/clusters` and `/states/compare` keep derived count/grouping behavior, canonical links, and narrative comparison scope without stale assumptions or matrix/ranking UI.
- SITE-12: South Dakota, Wyoming, and Texas detail records resolve through `getStateBySlug()` with status, provenance, confidence, and freshness fields intact.
- No deferred Phase 17 exclusions are implemented: no new publication work, advanced filters, comparison matrix, broad redesign, `mystic-ui`, or route-specific state exceptions.
</success_criteria>

<output>
After completion, create `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-01-SUMMARY.md`.
</output>
