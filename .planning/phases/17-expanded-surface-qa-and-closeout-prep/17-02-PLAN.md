---
phase: 17-expanded-surface-qa-and-closeout-prep
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - generated/content-graph.json
  - generated/refresh/state-refresh-queue.json
  - generated/refresh/state-refresh-queue.md
  - generated/refresh/state-priority-queue.json
  - generated/refresh/state-priority-queue.md
  - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts
  - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json
  - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md
autonomous: true
requirements:
  - SITE-10
  - SITE-11
  - SITE-12
  - QA-01
  - QA-02
user_setup: []
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-06-02T21-06-55
generated_at: 2026-06-02T21:24:26Z
must_haves:
  truths:
    - "Generated content and refresh artifacts prove the final v1.3 state count is 17 with zero current candidate intake rows and zero first-publication candidates."
    - "The required command ladder passes: content validation, content compilation, refresh/candidate generation, TypeScript, tests, build, and precommit."
    - "Desktop and mobile browser checks cover `/states`, `/states/clusters`, `/states/compare`, `/states/south-dakota`, `/states/wyoming`, and `/states/texas` without broken render, broken key links, horizontal overflow, or unreadable text overlap."
    - "Closeout evidence records the final 17 published states, seven v1.3 additions, zero current candidate intake rows, zero first-publication candidates, command evidence, and browser evidence."
  artifacts:
    - path: "generated/content-graph.json"
      provides: "Compiled 17-state public graph"
      contains: "\"south-dakota\""
    - path: "generated/refresh/state-priority-queue.json"
      provides: "Combined refresh/candidate queue with zero first-publication candidates"
      contains: "\"firstPublicationCandidateCount\": 0"
    - path: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts"
      provides: "Repo-owned targeted Playwright browser QA harness for Phase 17"
      contains: "/states/south-dakota"
    - path: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json"
      provides: "Machine-readable route and viewport browser evidence"
      contains: "\"status\": \"passed\""
    - path: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md"
      provides: "Milestone-ready closeout evidence for v1.3"
      contains: "17 published states"
  key_links:
    - from: "content/states/*.md"
      to: "generated/content-graph.json"
      via: "bun run compile:content"
      pattern: "\"states\""
    - from: "content/data/state-candidate-intake.json"
      to: "generated/refresh/state-priority-queue.json"
      via: "bun run refresh:queue"
      pattern: "\"firstPublicationCandidateCount\": 0"
    - from: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts"
      to: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json"
      via: "Playwright route/viewport report write"
      pattern: "\"routeChecks\""
    - from: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json"
      to: ".planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md"
      via: "manual summary of command and browser evidence"
      pattern: "Browser QA"
---

<objective>
Prove the expanded registry is ready for v1.3 closeout.

Purpose: After Plan 17-01 removes stale surface assumptions, this plan regenerates artifacts, runs the full verification ladder, performs targeted desktop/mobile browser QA, and records milestone-ready evidence.
Output: Current generated artifacts, browser QA harness/report, closeout evidence record, and passing verification commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CONTEXT.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-RESEARCH.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-VERIFICATION.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-02-SUMMARY.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@package.json
@scripts/compile-content.ts
@scripts/build-refresh-queue.ts
@scripts/precommit.ts
@generated/content-graph.json
@generated/refresh/state-refresh-queue.json
@generated/refresh/state-priority-queue.json
@content/data/state-candidate-intake.json
@src/routes/(site)/states/index.tsx
@src/routes/(site)/states/clusters.tsx
@src/routes/(site)/states/compare.tsx
@src/routes/(site)/states/[slug].tsx

<interfaces>
Generated content graph shape consumed by route models:
```typescript
type ContentGraph = {
	states: GraphState[];
	registry: {
		manifest: { states: GraphManifestEntry[] };
		publishedSlugs: string[];
		generatedAt: string;
		groups: {
			byRegion: GroupBucket<Region>;
			byProposalFocus: GroupBucket<ProposalFocus>;
			byLegislativeStatusGroup: GroupBucket<LegislativeStatusGroup>;
		};
	};
};
```

Priority queue summary shape from generated artifact:
```typescript
summary: {
	publishedRefreshCount: number;
	publishedRefreshDueCount: number;
	candidateCount: number;
	firstPublicationCandidateCount: number;
}
```

Required browser routes and viewports:
```typescript
const routes = [
	"/states",
	"/states/clusters",
	"/states/compare",
	"/states/south-dakota",
	"/states/wyoming",
	"/states/texas",
] as const;

const viewports = [
	{ name: "mobile", width: 390, height: 844 },
	{ name: "tablet", width: 768, height: 1024 },
	{ name: "desktop", width: 1280, height: 900 },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate artifacts and run the full command ladder</name>
  <files>generated/content-graph.json, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CONTEXT.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-RESEARCH.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-01-SUMMARY.md
    - content/data/state-candidate-intake.json
    - content/data/state-registry-manifest.json
    - generated/content-graph.json
    - generated/refresh/state-refresh-queue.json
    - generated/refresh/state-priority-queue.json
    - scripts/compile-content.ts
    - scripts/build-refresh-queue.ts
    - scripts/precommit.ts
    - package.json
  </read_first>
  <action>Implement D-09 and D-10 by regenerating repo-owned generated artifacts through the existing scripts only. Run the command ladder in this exact order: `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bunx tsc --noEmit`, `bun test`, `bun run build`, and `bun run precommit`. Do not manually edit generated files; if a generated artifact is wrong, fix the canonical source or existing generator that produced it. Do not add new state publication, package dependencies, route-specific state exceptions, broad redesign, `mystic-ui`, advanced filters, or matrix/ranking comparison behavior.</action>
  <action>After regeneration, run a boundary check that proves `generated/content-graph.json` has exactly 17 states and published slugs for `florida`, `kansas`, `north-dakota`, `ohio`, `utah`, `south-dakota`, and `wyoming`; `content/data/state-candidate-intake.json.candidates.length` is `0`; and `generated/refresh/state-priority-queue.json.summary.candidateCount` plus `summary.firstPublicationCandidateCount` are both `0`.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun test</automated>
    <automated>bun run build</automated>
    <automated>bun run precommit</automated>
    <automated>bun -e 'const graph = await Bun.file("generated/content-graph.json").json(); const intake = await Bun.file("content/data/state-candidate-intake.json").json(); const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const required = ["florida","kansas","north-dakota","ohio","utah","south-dakota","wyoming"]; if (graph.states.length !== 17) throw new Error(`Expected 17 graph states, got ${graph.states.length}`); for (const slug of required) { if (!graph.registry.publishedSlugs.includes(slug)) throw new Error(`Missing v1.3 published slug ${slug}`); } if (intake.candidates.length !== 0) throw new Error(`Expected zero candidate intake rows, got ${intake.candidates.length}`); if (priority.summary.candidateCount !== 0 || priority.summary.firstPublicationCandidateCount !== 0 || priority.firstPublicationCandidates.length !== 0) throw new Error("Expected zero current candidate and first-publication work"); console.log("Phase 17 generated boundary ok: 17 states, 7 v1.3 additions, 0 candidate rows, 0 first-publication candidates");'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0.
    - `bun run compile:content` exits 0.
    - `bun run refresh:queue` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - `bun test` exits 0.
    - `bun run build` exits 0.
    - `bun run precommit` exits 0.
    - Boundary check prints `Phase 17 generated boundary ok: 17 states, 7 v1.3 additions, 0 candidate rows, 0 first-publication candidates`.
    - `generated/content-graph.json` contains published slugs `florida`, `kansas`, `north-dakota`, `ohio`, `utah`, `south-dakota`, and `wyoming`.
    - `generated/refresh/state-priority-queue.json.summary.candidateCount` is `0`.
    - `generated/refresh/state-priority-queue.json.summary.firstPublicationCandidateCount` is `0`.
  </acceptance_criteria>
  <done>Generated artifacts and the full repository verification ladder prove the final expanded registry state is current and buildable.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run targeted browser QA evidence</name>
  <files>.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts, .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json</files>
  <read_first>
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
    - src/routes/(site)/states/index.tsx
    - src/routes/(site)/states/clusters.tsx
    - src/routes/(site)/states/compare.tsx
    - src/routes/(site)/states/[slug].tsx
    - src/components/editorial/StateCard.tsx
    - src/components/editorial/RegistryFreshnessPanel.tsx
    - package.json
  </read_first>
  <action>Implement D-11 and browser evidence for SITE-10, SITE-11, and SITE-12 by creating `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts`. Use Playwright's `chromium` from the existing dependency. The script must read `BASE_URL` with default `http://127.0.0.1:4173`, visit exactly these routes at mobile `390x844`, tablet `768x1024`, and desktop `1280x900`: `/states`, `/states/clusters`, `/states/compare`, `/states/south-dakota`, `/states/wyoming`, and `/states/texas`. For every route/viewport, assert the HTTP response is OK, the page text contains route-specific required copy, `document.documentElement.scrollWidth <= window.innerWidth + 2`, and visible text/control elements do not extend horizontally outside the viewport by more than 2px.</action>
  <action>The route-specific required copy must include: `/states` has `State proposals`, `Published states`, and `Open cluster reading paths`; `/states/clusters` has `Browse the registry in grouped reading lanes`, `Back to the full registry`, and `/states/[slug]`; `/states/compare` has `Compare the current registry without losing the source trail`, `Supporting records`, and `Back to the full registry`; each detail route has its state name, `Record footing`, `Freshness`, `Status as of`, and `Last reviewed`. The script must also verify key links exist: `/states` has anchors to `/states/south-dakota`, `/states/wyoming`, and `/states/texas`; `/states/clusters` and `/states/compare` have at least one canonical anchor whose href pathname starts with `/states/` and is not `/states` or `/states/clusters`; and no link check should require a matrix, scorecard, advanced filter, or new navigation item.</action>
  <action>Write `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json` with top-level fields `status`, `generatedAt`, `baseUrl`, `viewports`, `routes`, and `routeChecks`. Set `status` to `passed` only after all route/viewport checks and key link checks pass. Keep screenshots optional and do not commit large image artifacts unless a defect requires visual proof; the JSON report is the required browser evidence artifact.</action>
  <verify>
    <automated>bash -euo pipefail -c 'port=4173; bun run dev -- --host 127.0.0.1 --port "$port" >/tmp/bitcoin-bond-phase17-browser-server.log 2>&1 & server_pid=$!; trap "kill $server_pid >/dev/null 2>&1 || true" EXIT; for i in {1..60}; do if curl -fsS "http://127.0.0.1:$port/states" >/dev/null; then break; fi; if [[ "$i" -eq 60 ]]; then cat /tmp/bitcoin-bond-phase17-browser-server.log; exit 1; fi; sleep 1; done; BASE_URL="http://127.0.0.1:$port" bun .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts'</automated>
    <automated>jq -e '.status == "passed" and (.routeChecks | length) == 18 and (.routes | length) == 6 and (.viewports | length) == 3' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json</automated>
    <automated>rg -n '"/states"|"/states/clusters"|"/states/compare"|"/states/south-dakota"|"/states/wyoming"|"/states/texas"|390|768|1280|"status": "passed"' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts` exists and imports `chromium` from `playwright`.
    - Browser QA visits `/states`, `/states/clusters`, `/states/compare`, `/states/south-dakota`, `/states/wyoming`, and `/states/texas`.
    - Browser QA uses mobile `390x844`, tablet `768x1024`, and desktop `1280x900` viewports.
    - Browser QA fails on non-OK responses, missing required text, horizontal page overflow, horizontal element overflow, or missing canonical route links.
    - `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json` has `status: "passed"`.
    - The JSON report has exactly 18 route checks: 6 routes times 3 viewports.
  </acceptance_criteria>
  <done>Targeted browser evidence proves the expanded public surfaces render and link correctly across required routes and mobile/desktop-class viewports.</done>
</task>

<task type="auto">
  <name>Task 3: Record milestone closeout evidence</name>
  <files>.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CONTEXT.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-RESEARCH.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-UI-SPEC.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-VERIFICATION.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-02-SUMMARY.md
    - generated/content-graph.json
    - generated/refresh/state-priority-queue.json
    - generated/refresh/state-refresh-queue.json
    - content/data/state-candidate-intake.json
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json
  </read_first>
  <action>Implement QA-02 by creating `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md`. The document must be concise and milestone-audit ready. Include sections named exactly `Coverage Outcome`, `Generated Artifact Evidence`, `Command Evidence`, `Browser QA Evidence`, `Deferred Scope Not Included`, and `Milestone Closeout Readiness`.</action>
  <action>`Coverage Outcome` must state `17 published states`, list the seven v1.3 additions exactly as `Florida`, `Kansas`, `North Dakota`, `Ohio`, `Utah`, `South Dakota`, and `Wyoming`, and state that `content/data/state-candidate-intake.json` has zero current candidate intake rows. `Generated Artifact Evidence` must cite `generated/content-graph.json` with 17 states, `generated/refresh/state-priority-queue.json` with `candidateCount: 0` and `firstPublicationCandidateCount: 0`, and `generated/refresh/state-refresh-queue.json` as the current published-record refresh queue. `Command Evidence` must list all seven commands from Task 1 with `PASS`: `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bunx tsc --noEmit`, `bun test`, `bun run build`, and `bun run precommit`.</action>
  <action>`Browser QA Evidence` must summarize the JSON report status, the six routes, and the three viewports. `Deferred Scope Not Included` must explicitly say Phase 17 did not add new publication work, advanced filters, a comparison matrix, broad redesign, `mystic-ui`, or route-specific state exceptions. `Milestone Closeout Readiness` must say v1.3 is ready for archive/audit review once `/gsd-verify-work` or milestone closeout consumes this evidence. Do not update archived milestone files in this task.</action>
  <verify>
    <automated>test -f .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
    <automated>rg -n 'Coverage Outcome|Generated Artifact Evidence|Command Evidence|Browser QA Evidence|Deferred Scope Not Included|Milestone Closeout Readiness' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
    <automated>rg -n '17 published states|Florida|Kansas|North Dakota|Ohio|Utah|South Dakota|Wyoming|zero current candidate intake rows|candidateCount: 0|firstPublicationCandidateCount: 0' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
    <automated>rg -n 'bun run validate:content.*PASS|bun run compile:content.*PASS|bun run refresh:queue.*PASS|bunx tsc --noEmit.*PASS|bun test.*PASS|bun run build.*PASS|bun run precommit.*PASS' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
    <automated>rg -n '/states|/states/clusters|/states/compare|/states/south-dakota|/states/wyoming|/states/texas|390x844|768x1024|1280x900|status.*passed' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
    <automated>rg -n 'did not add new publication work|advanced filters|comparison matrix|broad redesign|mystic-ui|route-specific state exceptions' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-CLOSEOUT-EVIDENCE.md` exists.
    - `17-CLOSEOUT-EVIDENCE.md` has sections `Coverage Outcome`, `Generated Artifact Evidence`, `Command Evidence`, `Browser QA Evidence`, `Deferred Scope Not Included`, and `Milestone Closeout Readiness`.
    - The closeout record names `17 published states`.
    - The closeout record names all seven v1.3 additions: Florida, Kansas, North Dakota, Ohio, Utah, South Dakota, and Wyoming.
    - The closeout record states zero current candidate intake rows and `firstPublicationCandidateCount: 0`.
    - The closeout record lists all seven required commands as `PASS`.
    - The closeout record lists all six browser routes and all three viewport sizes.
    - The closeout record states Phase 17 did not add new publication work, advanced filters, a comparison matrix, broad redesign, `mystic-ui`, or route-specific state exceptions.
  </acceptance_criteria>
  <done>Milestone-ready closeout evidence records coverage, generated artifacts, commands, browser QA, and deferred scope boundaries for v1.3 archive/audit use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical content/data -> generated artifacts | Authored registry content and candidate intake become generated public graph and maintainer queues. |
| local browser runtime -> closeout evidence | Browser checks against a local server become recorded milestone evidence. |
| command output -> closeout record | Local verification commands become audit-facing statements in `17-CLOSEOUT-EVIDENCE.md`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | `generated/content-graph.json` | mitigate | Regenerate with `bun run compile:content` and verify 17 states plus required v1.3 slugs. |
| T-17-02-02 | Repudiation | command evidence | mitigate | Run and list the exact command ladder with PASS in closeout evidence. |
| T-17-02-03 | Tampering | browser route links | mitigate | Playwright harness verifies canonical links and required route text across required routes/viewports. |
| T-17-02-04 | Denial of Service | production build | mitigate | Run `bun run build` before browser evidence and closeout. |
| T-17-02-05 | Information Disclosure | candidate intake boundary | mitigate | Verify candidate intake has zero rows and priority output has zero first-publication candidates; do not expose unverified candidate content publicly. |
</threat_model>

<verification>
Run after all tasks:

```bash
bun run validate:content
bun run compile:content
bun run refresh:queue
bunx tsc --noEmit
bun test
bun run build
bun run precommit
```

Then run browser and closeout evidence checks:

```bash
bash -euo pipefail -c 'port=4173; bun run dev -- --host 127.0.0.1 --port "$port" >/tmp/bitcoin-bond-phase17-browser-server.log 2>&1 & server_pid=$!; trap "kill $server_pid >/dev/null 2>&1 || true" EXIT; for i in {1..60}; do if curl -fsS "http://127.0.0.1:$port/states" >/dev/null; then break; fi; if [[ "$i" -eq 60 ]]; then cat /tmp/bitcoin-bond-phase17-browser-server.log; exit 1; fi; sleep 1; done; BASE_URL="http://127.0.0.1:$port" bun .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts'
jq -e '.status == "passed" and (.routeChecks | length) == 18 and (.routes | length) == 6 and (.viewports | length) == 3' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa-report.json
rg -n '17 published states|Florida|Kansas|North Dakota|Ohio|Utah|South Dakota|Wyoming|candidateCount: 0|firstPublicationCandidateCount: 0' .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-CLOSEOUT-EVIDENCE.md
```
</verification>

<success_criteria>
- QA-01: Content validation, content compilation, refresh/candidate generation, TypeScript, tests, production build, precommit, and targeted browser checks pass after the expanded batch.
- QA-02: Closeout evidence records 17 final published states, the seven v1.3 additions, zero current candidate intake rows, zero first-publication candidates, command evidence, browser evidence, and explicit deferred-scope exclusions.
- SITE-10/SITE-11/SITE-12: Browser QA confirms catalog, cluster, comparison, and representative detail surfaces render, remain readable, and preserve canonical drill-down paths across mobile/tablet/desktop checks.
</success_criteria>

<output>
After completion, create `.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-02-SUMMARY.md`.
</output>
