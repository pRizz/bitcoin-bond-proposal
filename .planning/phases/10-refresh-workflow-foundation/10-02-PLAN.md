---
phase: 10-refresh-workflow-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/audit-registry-refresh.ts
  - package.json
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260409T080705Z
generated_at: 2026-04-09T08:07:05.742Z
must_haves:
  truths:
    - Maintainers can run a repo-owned refresh audit command without editing canonical content.
    - The audit report shows which published entries are current, due soon, or overdue based on snapshot dates.
    - The audit output keeps queued states visible as workflow targets without implying they are already public records.
  artifacts:
    - scripts/audit-registry-refresh.ts
    - package.json
  key_links:
    - The refresh audit reuses the same parsing and refresh-status rules as the canonical content workflow.
---

<objective>
Add a repo-owned refresh audit and review report for registry maintainers.

Purpose: Give maintainers a safe, repeatable way to inspect freshness debt before Phase 11 broadens coverage.
Output: A Bun script and package script entry that report review state from canonical data without mutating content.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refresh-workflow-foundation/10-CONTEXT.md
@.planning/phases/10-refresh-workflow-foundation/10-01-PLAN.md
@scripts/compile-content.ts
@scripts/validate-content.ts
@src/lib/content/registry-refresh.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the refresh audit script</name>
  <files>scripts/audit-registry-refresh.ts</files>
  <read_first>src/lib/content/registry-refresh.ts, content/data/state-registry-manifest.json</read_first>
  <action>Create a dry-run audit script that reads canonical manifest and state content, evaluates freshness against a report date, and prints a maintainer-readable report for published plus queued states. Support an optional explicit report date argument so the workflow stays reproducible.</action>
  <verify>bun scripts/audit-registry-refresh.ts</verify>
  <acceptance_criteria>
    - The report shows counts for current, due soon, overdue, and queued states.
    - The script can be run without changing any repo-tracked files.
  </acceptance_criteria>
  <done>Maintainers have a repo-owned audit command for registry refresh state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the audit into package scripts and output contract</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Add a discoverable package script for the refresh audit and keep its naming consistent with the existing content workflow. The contract should make clear this is a snapshot audit, not a live legislative feed.</action>
  <verify>bun run audit:refresh</verify>
  <acceptance_criteria>
    - `package.json` exposes a stable repo-owned script for the refresh audit.
    - Running the package script succeeds from the repo root.
  </acceptance_criteria>
  <done>The refresh audit is part of the repo-owned maintainer workflow surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun scripts/audit-registry-refresh.ts`
- [ ] `bun run audit:refresh`
- [ ] `bun run validate:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Maintainers can inspect registry freshness debt without mutating content
  </success_criteria>

<output>
After completion, create `.planning/phases/10-refresh-workflow-foundation/10-02-SUMMARY.md`
</output>
