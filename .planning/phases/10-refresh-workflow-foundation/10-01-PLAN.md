---
phase: 10-refresh-workflow-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/data/state-registry-manifest.json
  - content/data/state-registry-manifest.README.md
  - src/lib/content/schema.ts
  - src/lib/content/schema.test.ts
  - src/lib/content/registry-refresh.ts
  - src/lib/content/registry-refresh.test.ts
  - scripts/compile-content.ts
  - src/lib/site/content.ts
autonomous: true
requirements:
  - PIPE-04
  - PIPE-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260409T080705Z
generated_at: 2026-04-09T08:07:05.742Z
must_haves:
  truths:
    - Published manifest entries carry explicit refresh cadence metadata in canonical content.
    - Shared refresh helpers derive next-review dates and refresh status from snapshot dates.
    - The compiled content graph exposes refresh metadata for published states while excluding queued entries from the published-state surface.
  artifacts:
    - content/data/state-registry-manifest.json
    - src/lib/content/registry-refresh.ts
    - scripts/compile-content.ts
  key_links:
    - Manifest cadence fields are parsed by schema code and reused by compile output.
    - Published route generation still depends on published slugs only, not every canonical state file.
---

<objective>
Extend the canonical manifest and compiled graph with refresh-aware registry state.

Purpose: Put the review cadence and publication-boundary rules into canonical infrastructure before audit tooling or public freshness surfaces depend on them.
Output: Refresh-aware manifest entries, shared review-status helpers, and a compiled graph that preserves published-versus-queued boundaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refresh-workflow-foundation/10-CONTEXT.md
@content/data/state-registry-manifest.json
@content/data/state-registry-manifest.README.md
@src/lib/content/schema.ts
@scripts/compile-content.ts
@src/lib/site/content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add refresh cadence to published manifest entries</name>
  <files>content/data/state-registry-manifest.json, content/data/state-registry-manifest.README.md</files>
  <read_first>content/data/state-registry-manifest.README.md</read_first>
  <action>Add a small `reviewCadenceDays` field to the five currently published manifest entries only. Document that this cadence is required for published states, optional elsewhere, and still grounded in snapshot review rather than live feeds.</action>
  <verify>rg -n 'reviewCadenceDays' content/data/state-registry-manifest.json content/data/state-registry-manifest.README.md</verify>
  <acceptance_criteria>
    - Published manifest entries include explicit `reviewCadenceDays` values.
    - README explains that queued and unresearched states are not public records and do not require the same refresh contract.
  </acceptance_criteria>
  <done>The canonical manifest defines per-state review cadence for currently published entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared refresh-status helpers and schema support</name>
  <files>src/lib/content/schema.ts, src/lib/content/schema.test.ts, src/lib/content/registry-refresh.ts, src/lib/content/registry-refresh.test.ts</files>
  <read_first>src/lib/content/schema.ts</read_first>
  <action>Create a small shared helper module for review cadence math and status derivation. Extend the manifest schema to parse the new cadence field and cover the helper/schema behavior with focused unit tests.</action>
  <verify>bun test</verify>
  <acceptance_criteria>
    - Tests cover next-review calculation and refresh status derivation.
    - Manifest parsing accepts cadence on published entries without broadening unrelated schema fields.
  </acceptance_criteria>
  <done>Refresh date logic is centralized, testable, and ready for scripts plus later public surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Extend compile output without leaking queued entries</name>
  <files>scripts/compile-content.ts, src/lib/site/content.ts</files>
  <read_first>scripts/compile-content.ts, src/lib/site/content.ts</read_first>
  <action>Update compile behavior so only manifest-`published` state files become published graph entries and prerender targets, while derived refresh metadata is included for those entries. Keep enough manifest data available for later coverage/freshness surfaces.</action>
  <verify>bun run compile:content</verify>
  <acceptance_criteria>
    - Generated graph keeps published entries distinct from queued authoring state.
    - Published state objects include derived refresh metadata needed by later phases.
  </acceptance_criteria>
  <done>The compiled graph is refresh-aware and safe for future queued authoring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test`
- [ ] `bun run compile:content`
- [ ] `bun run validate:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Refresh-aware manifest and compile behavior exist as canonical infrastructure
  </success_criteria>

<output>
After completion, create `.planning/phases/10-refresh-workflow-foundation/10-01-SUMMARY.md`
</output>
