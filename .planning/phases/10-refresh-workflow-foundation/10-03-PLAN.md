---
phase: 10-refresh-workflow-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - scripts/validate-content.ts
  - content/data/README.md
autonomous: true
requirements:
  - PIPE-06
generated_by: gsd-plan-phase
user_setup: []
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260409T080705Z
generated_at: 2026-04-09T08:07:05.742Z
must_haves:
  truths:
    - Validation rejects published registry states that are missing required refresh cadence or freshness chronology.
    - Maintainer-facing content docs explain how refresh-aware manifest and state-entry authoring now work.
    - The repo-native content workflow remains green after the stricter checks.
  artifacts:
    - scripts/validate-content.ts
    - content/data/README.md
  key_links:
    - Validation rules align with the manifest refresh contract and the audit script expectations.
---

<objective>
Tighten refresh-aware validation and maintainer guidance.

Purpose: Make the new refresh contract hard to violate and easy to follow before broader content authoring resumes.
Output: Stricter validation rules plus updated maintainer guidance for refresh-aware state authoring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refresh-workflow-foundation/10-CONTEXT.md
@.planning/phases/10-refresh-workflow-foundation/10-01-PLAN.md
@.planning/phases/10-refresh-workflow-foundation/10-02-PLAN.md
@content/data/README.md
@scripts/validate-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce refresh-aware validation rules</name>
  <files>scripts/validate-content.ts</files>
  <read_first>scripts/validate-content.ts, src/lib/content/schema.ts, src/lib/content/registry-refresh.ts</read_first>
  <action>Add validation rules that enforce the new refresh contract for published states, including cadence presence and coherent chronology between `statusAsOf` and `lastReviewed`, while preserving the manifest-versus-published workflow boundary.</action>
  <verify>bun run validate:content</verify>
  <acceptance_criteria>
    - Validation fails if a published state lacks manifest cadence.
    - Validation fails if `lastReviewed` predates `statusAsOf`.
  </acceptance_criteria>
  <done>The refresh contract is enforced automatically by the repo-owned validation path.</done>
</task>

<task type="auto">
  <name>Task 2: Update maintainer guidance for refresh-aware authoring</name>
  <files>content/data/README.md</files>
  <read_first>content/data/README.md, content/data/state-registry-manifest.README.md</read_first>
  <action>Update content-layer guidance so maintainers know where cadence, freshness metadata, and manifest state belong when adding or refreshing entries. Keep the guidance short, canonical, and repo-owned.</action>
  <verify>rg -n 'reviewCadenceDays|lastReviewed|statusAsOf|audit:refresh' content/data/README.md content/data/state-registry-manifest.README.md</verify>
  <acceptance_criteria>
    - Docs explain where cadence lives and where freshness dates live.
    - Docs point maintainers to the repo-owned refresh audit command.
  </acceptance_criteria>
  <done>The authoring workflow is documented alongside the canonical content data contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run validate:content`
- [ ] `bun run audit:refresh`
- [ ] `bun run compile:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Refresh-aware content rules are enforced and documented
  </success_criteria>

<output>
After completion, create `.planning/phases/10-refresh-workflow-foundation/10-03-SUMMARY.md`
</output>
