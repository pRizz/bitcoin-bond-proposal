---
phase: 11-expanded-state-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/data/state-registry-manifest.json
autonomous: true
requirements:
  - CATA-07
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260409T105243Z
generated_at: 2026-04-09T10:52:43.446Z
must_haves:
  truths:
    - The next five-state publication batch is explicitly selected in the manifest before authoring.
    - Selected states are marked `queued`, not `published`, until their canonical entries are complete.
    - The current public published batch remains unchanged after the batch-selection step.
  artifacts:
    - content/data/state-registry-manifest.json
  key_links:
    - The queued batch can coexist with future canonical files without leaking into public published routes.
---

<objective>
Select the next five-state publication batch and align manifest status for authoring.

Purpose: Turn the chosen expansion batch into explicit canonical registry intent before writing the new state records.
Output: Manifest updates that queue the Florida, Maryland, North Carolina, Ohio, and Wyoming entries for publication.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-expanded-state-coverage/11-CONTEXT.md
@content/data/state-registry-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue the selected expansion batch in the manifest</name>
  <files>content/data/state-registry-manifest.json</files>
  <read_first>content/data/state-registry-manifest.json</read_first>
  <action>Update the manifest entries for Florida, Maryland, North Carolina, Ohio, and Wyoming so they are explicitly targeted for near-term publication. Set `registryStatus` to `queued`, assign the appropriate `proposalFocus`, tighten the short notes to match the official record, and set editorial priority in line with the batch thesis.</action>
  <verify>rg -n '\"(florida|maryland|north-carolina|ohio|wyoming)\"|\"queued\"' content/data/state-registry-manifest.json</verify>
  <acceptance_criteria>
    - Each selected state is marked `queued` in the manifest.
    - Non-selected states remain unchanged.
  </acceptance_criteria>
  <done>The next publication batch is canonical and queued for authoring.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the queued batch does not change the published surface yet</name>
  <files>content/data/state-registry-manifest.json</files>
  <read_first>scripts/compile-content.ts</read_first>
  <action>Run the content pipeline after the manifest update and confirm that the public published batch is still the original five-state set because no new entries have been promoted yet.</action>
  <verify>bun run validate:content && bun run compile:content && bun run audit:refresh</verify>
  <acceptance_criteria>
    - Validation and compile pass with the new queued states.
    - The published count remains five until the canonical entries are promoted later in the phase.
  </acceptance_criteria>
  <done>The manifest batch is queued safely without prematurely expanding the public published surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run validate:content`
- [ ] `bun run compile:content`
- [ ] `bun run audit:refresh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The next publication batch is queued and ready for authoring
  </success_criteria>

<output>
After completion, create `.planning/phases/11-expanded-state-coverage/11-01-SUMMARY.md`
</output>
