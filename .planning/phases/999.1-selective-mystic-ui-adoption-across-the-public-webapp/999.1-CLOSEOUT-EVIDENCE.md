# Phase 999.1 Closeout Evidence

## Compatibility Gate

Phase 999.1 treated Mystic UI adoption as evidence-gated, not automatic. The current public app is SolidStart with Tailwind CSS v4 via `@tailwindcss/vite` and CSS-first repo tokens in `src/styles/app.css`, while the observed Mystic UI source package path remains documented around a Vite + SolidJS + Tailwind CSS 3.x setup and a broader theme/dependency surface.

Mystic UI was locally adapted; the direct package was rejected for now because it did not clear the repo's Tailwind CSS v4, SolidStart, token, and trust-surface gates.

### Source Pin Evidence

Command:

```bash
git ls-remote https://github.com/pRizz/mystic-ui HEAD
```

Output:

```text
d36017757708ed01ef2b3b47beb14f294726411c	HEAD
```

The exact Mystic UI HEAD SHA observed for this phase is `d36017757708ed01ef2b3b47beb14f294726411c`. If direct adoption is reopened later, it must use an exact GitHub SHA pin rather than a stale npm package or floating branch.

### npm Package Evidence

Command:

```bash
npm view mystic-ui version time --json
```

Output:

```json
{
  "version": "0.0.1",
  "time": {
    "created": "2023-03-14T15:13:47.869Z",
    "0.0.1": "2023-03-14T15:13:48.052Z",
    "modified": "2023-03-14T15:13:48.511Z"
  }
}
```

The `mystic-ui` npm package is version `0.0.1`, created and last modified on 2023-03-14. That package is too stale for this repo's current SolidStart and Tailwind CSS v4 compatibility gate.

Command:

```bash
npm view @mystic-ui/cli version time --json
```

Output:

```json
{
  "version": "0.0.0-1",
  "time": {
    "created": "2024-10-27T21:36:47.724Z",
    "modified": "2024-10-28T08:45:46.420Z",
    "0.0.0-alpha": "2024-10-25T21:39:38.983Z",
    "0.0.1-alpha": "2024-10-27T21:36:48.090Z",
    "0.0.2-alpha": "2024-10-27T22:15:40.114Z",
    "0.0.0-1": "2024-10-28T08:45:46.234Z"
  }
}
```

The `@mystic-ui/cli` package is version `0.0.0-1`, last modified on 2024-10-28. This phase found no evidence that the CLI clears the repo's Tailwind CSS v4, SolidStart, token, or trust-surface gates.

### Direct Package Rejection

Direct package adoption is rejected for this phase because it would add package and lockfile churn for a narrow comparison/proof treatment, while the existing route can deliver the needed reader value through local Solid components, `panel-wash`, `data-chip`, and the current editorial token system.

This applies these phase decisions:

- D-01: adoption is evidence-gated, not automatic.
- D-02: only an exact current GitHub SHA would be eligible for a future direct adoption attempt.
- D-03: Tailwind CSS v4, token, dark-mode, or broad setup mismatch closes the direct package path for now.
- D-07: local adaptation is preferred when it delivers the proof/comparison value with less dependency risk.
- D-09: shared chrome, footer, metadata, README, profile, about, settings, and identity surfaces remain untouched.
- D-12: this evidence records that Mystic UI was locally adapted and the direct package was rejected for now.

## Local Adaptation Implemented

The visible implementation is route-local to `/states/compare`. It uses the existing `panel-wash` surface, `data-chip` count treatment, and `ActionLink` representative-state CTAs to render three compact comparison proof lanes after the freshness panel and before the existing narrative route guidance.

This local adaptation leaves `package.json`, `bun.lock`, shared chrome, footer, metadata, README, profile, about, settings, and identity surfaces unchanged. It does not add animation, particles, bokeh/orbs, global background effects, icon packages, a matrix, search, ranking, scorecard, advanced filters, `/states` changes, `/states/clusters` changes, or OpenLinks-triggering surfaces.

## Verification Evidence

- TypeScript - PASS: `bunx tsc --noEmit` exited 0.
- Focused content tests - PASS: `bun test src/lib/site/content.test.ts` exited 0 with 16 passing tests and 82 assertions.
- Full tests - PASS: `bun test` exited 0 with 43 passing tests and 122 assertions.
- Production build - PASS: `bun run build` exited 0 and prerendered `/states/compare`.
- Precommit - PASS: `bun run precommit` exited 0 after a mechanical Biome formatting correction to the Plan 01 comparison files that blocked the repository formatter check.
- Browser QA - PASS: `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts` passed at `390x844`, `768x1024`, and `1280x900`.
- Browser QA report status - PASS: `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json` records `"status": "passed"`.
- Browser QA route count - PASS: `routes.length` is `1`.
- Browser QA viewport count - PASS: `viewports.length` is `3`.
- Browser QA route-check count - PASS: `routeChecks.length` is `3`.

## Final Scope Boundary

No package or lockfile changes were made; no Mystic UI package, npm package, CLI, Tailwind 3.x helper, global theme CSS, class-based dark-mode dependency, shared chrome edit, footer edit, metadata edit, README edit, profile/about/settings edit, matrix, ranking table, advanced filter, or search surface shipped in Phase 999.1.

OpenLinks was not triggered because no identity, metadata, README, footer, profile, about, settings, or shared app chrome surface changed.
