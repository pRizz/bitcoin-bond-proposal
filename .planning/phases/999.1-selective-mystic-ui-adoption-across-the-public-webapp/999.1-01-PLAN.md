---
phase: 999.1-selective-mystic-ui-adoption-across-the-public-webapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
  - src/lib/site/states-surfaces.ts
  - src/lib/site/content.ts
  - src/lib/site/content.test.ts
  - src/routes/(site)/states/compare.tsx
autonomous: true
requirements:
  - UI-01
  - SITE-13
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 999.1-2026-06-03T01-47-12
generated_at: 2026-06-03T02:07:48.215Z
must_haves:
  truths:
    - "Reader sees a compact three-lane proof panel on `/states/compare` after the freshness panel, with reserve benchmarks, crossover proposals, and bond-side signals visible before the existing narrative sections."
    - "Reader can follow each proof lane to a canonical representative state detail route without a matrix, ranking table, advanced filter, search surface, or new package dependency."
    - "The direct Mystic UI package is rejected for this phase with exact compatibility evidence, while the local adaptation preserves Tailwind CSS v4 tokens, the dark editorial system, and existing registry primitives."
    - "Pure model tests prove the three proof lanes, canonical representative hrefs, source-aware context, and no-matrix behavior."
  artifacts:
    - path: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md"
      provides: "Compatibility evidence and direct-package rejection reason"
      contains: "Mystic UI was locally adapted"
    - path: "src/lib/site/states-surfaces.ts"
      provides: "Pure comparison proof-lane type and builder"
      contains: "buildStatesComparisonProofLanesFromComparisonModel"
    - path: "src/lib/site/content.ts"
      provides: "Content accessor for proof lanes"
      contains: "getStatesComparisonProofLanes"
    - path: "src/lib/site/content.test.ts"
      provides: "Focused proof-lane model regression coverage"
      contains: "source-aware proof lanes"
    - path: "src/routes/(site)/states/compare.tsx"
      provides: "Rendered local proof-lane panel on `/states/compare`"
      contains: "Comparison proof lanes"
  key_links:
    - from: "src/routes/(site)/states/compare.tsx"
      to: "src/lib/site/content.ts"
      via: "getStatesComparisonProofLanes()"
      pattern: "getStatesComparisonProofLanes"
    - from: "src/lib/site/content.ts"
      to: "src/lib/site/states-surfaces.ts"
      via: "buildStatesComparisonProofLanesFromComparisonModel(buildStatesComparisonModel(graphData))"
      pattern: "buildStatesComparisonProofLanesFromComparisonModel"
    - from: "src/lib/site/states-surfaces.ts"
      to: "src/routes/(site)/states/[slug].tsx"
      via: "canonical href `/states/${representativeState.slug}`"
      pattern: "href: `/states/${"
    - from: "src/routes/(site)/states/compare.tsx"
      to: "src/components/editorial/ActionLink.tsx"
      via: "proof-lane `Open state record` CTA"
      pattern: "Open state record"
---

<objective>
Add the local proof-lane adaptation on `/states/compare`.

Purpose: Phase 999.1 must answer UI-01 without taking on broad Mystic UI dependency risk or SITE-13 matrix/filter scope. This plan records the compatibility gate, rejects direct package adoption for now, adds a pure proof-lane model, and renders a compact local proof panel on the comparison route.
Output: Compatibility evidence, tested proof-lane model/accessor, and a `/states/compare` proof panel implemented with existing Tailwind CSS v4 tokens and editorial components.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-VERIFICATION.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@package.json
@bun.lock
@app.config.ts
@src/styles/app.css
@src/lib/site/states-surfaces.ts
@src/lib/site/content.ts
@src/lib/site/content.test.ts
@src/routes/(site)/states/compare.tsx
@src/components/editorial/ActionLink.tsx
@src/components/editorial/RegistryFreshnessPanel.tsx

<interfaces>
Existing comparison section keys from `src/lib/site/states-surfaces.ts`:
```typescript
export type ComparisonSectionKey =
	| "reserve-benchmarks"
	| "crossover-records"
	| "bond-side-signals";

export type ComparisonState = PublishedState & {
	href: string;
	badgeLabel: string;
	badgeTone: BadgeTone;
};

export type StatesComparisonSection = {
	key: ComparisonSectionKey;
	eyebrow: string;
	title: string;
	lead: string;
	comparison: string;
	featuredStates: ComparisonState[];
	supportingStates: ComparisonState[];
};
```

Existing route accessors from `src/lib/site/content.ts`:
```typescript
export function buildStatesComparisonModel(
	graphData: ContentGraph,
): StatesComparisonModel;

export function getStatesComparisonModel(): StatesComparisonModel;
export function getRegistryFreshnessSummary(): RegistryFreshnessSummary;
export function getRegistryStats(): RegistryStats;
```

Existing route imports in `src/routes/(site)/states/compare.tsx`:
```typescript
import { ActionLink } from "../../../components/editorial/ActionLink";
import { PageSection } from "../../../components/editorial/PageSection";
import { RegistryFreshnessPanel } from "../../../components/editorial/RegistryFreshnessPanel";
import { StateCard } from "../../../components/editorial/StateCard";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record compatibility evidence and reject direct package adoption</name>
  <files>.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
    - package.json
    - bun.lock
    - app.config.ts
    - src/styles/app.css
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <action>Implement D-01, D-02, D-03, D-07, D-09, and D-12. Re-run `git ls-remote https://github.com/pRizz/mystic-ui HEAD`, `npm view mystic-ui version time --json`, and `npm view @mystic-ui/cli version time --json`. Create `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md` with a `## Compatibility Gate` section that records the exact Mystic UI HEAD SHA `d36017757708ed01ef2b3b47beb14f294726411c`, the npm package/CLI staleness facts from the command output, and the direct-package rejection reason. Include this exact sentence: `Mystic UI was locally adapted; the direct package was rejected for now because it did not clear the repo's Tailwind CSS v4, SolidStart, token, and trust-surface gates.` Do not edit `package.json`, `bun.lock`, shared chrome, footer, metadata, README, profile, about, settings, or identity surfaces.</action>
  <verify>
    <automated>git ls-remote https://github.com/pRizz/mystic-ui HEAD</automated>
    <automated>npm view mystic-ui version time --json</automated>
    <automated>npm view @mystic-ui/cli version time --json</automated>
    <automated>rg -n "d36017757708ed01ef2b3b47beb14f294726411c|Mystic UI was locally adapted|direct package was rejected for now|Tailwind CSS v4|SolidStart|trust-surface gates" .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</automated>
    <automated>bash -euo pipefail -c 'if git diff -- package.json bun.lock | rg -n "mystic-ui|@mystic-ui|solid-motionone|solid-icons|rough-notation|@tsparticles|tailwindcss-animate|withMysticUI|mystic-ui/tailwind/theme.css"; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `999.1-CLOSEOUT-EVIDENCE.md` exists.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `d36017757708ed01ef2b3b47beb14f294726411c`.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains the exact sentence `Mystic UI was locally adapted; the direct package was rejected for now because it did not clear the repo's Tailwind CSS v4, SolidStart, token, and trust-surface gates.`
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `mystic-ui`, `@mystic-ui/cli`, `Tailwind CSS v4`, and `SolidStart`.
    - `package.json` and `bun.lock` do not gain `mystic-ui`, `@mystic-ui`, `solid-motionone`, `solid-icons`, `rough-notation`, `@tsparticles`, or `tailwindcss-animate`.
    - No shared chrome, footer, metadata, README, profile, about, settings, or identity surface appears in this task's git diff.
  </acceptance_criteria>
  <done>Compatibility evidence is recorded, direct dependency adoption is rejected for this phase, and the local adaptation path is explicitly grounded in D-01 through D-03 and D-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure comparison proof-lane model and tests</name>
  <files>src/lib/site/states-surfaces.ts, src/lib/site/content.ts, src/lib/site/content.test.ts</files>
  <read_first>
    - src/lib/site/states-surfaces.ts
    - src/lib/site/content.ts
    - src/lib/site/content.test.ts
    - generated/content-graph.json
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <behavior>
    - `buildStatesComparisonProofLanesFromComparisonModel()` returns exactly three lanes with keys `reserve-benchmarks`, `crossover-records`, and `bond-side-signals`.
    - Representative hrefs are canonical: reserve uses `/states/texas`, crossover uses `/states/north-carolina`, and bond-side uses `/states/new-hampshire`.
    - Count labels are derived from the existing comparison model, not hardcoded route-only arrays: reserve `7 records`, crossover `2 records`, and bond-side `2 records` for the current graph.
    - Context text is source-aware and dated-snapshot oriented, and does not introduce matrix, ranking, scorecard, search, or advanced-filter behavior.
  </behavior>
  <action>Implement D-04, D-06, D-08, UI-01, and SITE-13 as a functional core. In `src/lib/site/states-surfaces.ts`, add `export type StatesComparisonProofLane = { key: ComparisonSectionKey; label: string; countLabel: string; context: string; representativeState: ComparisonState; href: string; };` and `export function buildStatesComparisonProofLanesFromComparisonModel(statesComparisonModel: StatesComparisonModel): StatesComparisonProofLane[]`. The builder must find each existing section by key, derive total count from `featuredStates.length + supportingStates.length`, pick representative slugs `texas`, `north-carolina`, and `new-hampshire`, throw `Missing comparison proof section: ${key}` if a required section is absent, and throw `Missing comparison proof representative: ${slug}` if a representative is not in that section. Use the exact labels `Reserve benchmarks`, `Crossover proposals`, and `Bond-side signals`.</action>
  <action>Use these exact context strings in the pure model: reserve `Dated snapshot records show enacted, advanced, introduced, and failed reserve signals while preserving the narrative comparison model.`; crossover `Source-backed crossover records connect reserve policy to financing or packet context while keeping the canonical state page as the source of record.`; bond-side `Official bond-side evidence is narrower than the reserve lane, so each dated source-backed signal gets explicit context.` Format count labels as `${count} record` for count 1 and `${count} records` otherwise. In `src/lib/site/content.ts`, export the new type, add `export function buildStatesComparisonProofLanes(graphData: ContentGraph): StatesComparisonProofLane[]`, and add `export function getStatesComparisonProofLanes(): StatesComparisonProofLane[]` by composing the new builder with `buildStatesComparisonModel(graphData)`. Do not change the existing `buildStatesComparisonModel()` section membership or route copy.</action>
  <action>In `src/lib/site/content.test.ts`, add focused tests before implementation changes are considered complete. Tests must import `buildStatesComparisonProofLanes` and `getStatesComparisonProofLanes`, use explicit `// Arrange`, `// Act`, and `// Assert` sections when setup is non-trivial, assert the three lane keys, labels, count labels, representative slugs, and hrefs, assert every context contains either `Dated snapshot`, `Source-backed`, `source-backed`, or `source of record`, and assert joined proof-lane copy does not match `/matrix|ranking|scorecard|search|advanced filter/i`.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts</automated>
    <automated>rg -n "StatesComparisonProofLane|buildStatesComparisonProofLanesFromComparisonModel|Reserve benchmarks|Crossover proposals|Bond-side signals|Dated snapshot records|Source-backed crossover records|Official bond-side evidence" src/lib/site/states-surfaces.ts</automated>
    <automated>rg -n "buildStatesComparisonProofLanes|getStatesComparisonProofLanes|StatesComparisonProofLane" src/lib/site/content.ts src/lib/site/content.test.ts</automated>
    <automated>rg -n "/states/texas|/states/north-carolina|/states/new-hampshire|7 records|2 records|matrix|ranking|scorecard|advanced filter" src/lib/site/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/lib/site/content.test.ts` exits 0.
    - `src/lib/site/states-surfaces.ts` exports `StatesComparisonProofLane`.
    - `src/lib/site/states-surfaces.ts` exports `buildStatesComparisonProofLanesFromComparisonModel`.
    - `src/lib/site/content.ts` exports `buildStatesComparisonProofLanes`.
    - `src/lib/site/content.ts` exports `getStatesComparisonProofLanes`.
    - Focused tests assert keys `reserve-benchmarks`, `crossover-records`, and `bond-side-signals`.
    - Focused tests assert hrefs `/states/texas`, `/states/north-carolina`, and `/states/new-hampshire`.
    - Focused tests assert count labels `7 records` and `2 records`.
    - Focused tests assert no `matrix`, `ranking`, `scorecard`, `search`, or `advanced filter` copy appears in the proof lanes.
    - `package.json`, `bun.lock`, and `src/styles/app.css` are unchanged by this task.
  </acceptance_criteria>
  <done>The proof-lane data is a tested pure model derived from the existing comparison model, with canonical links and no SITE-13 matrix/filter expansion.</done>
</task>

<task type="auto">
  <name>Task 3: Render the local proof-lane panel on `/states/compare`</name>
  <files>src/routes/(site)/states/compare.tsx, .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</files>
  <read_first>
    - src/routes/(site)/states/compare.tsx
    - src/lib/site/content.ts
    - src/lib/site/states-surfaces.ts
    - src/components/editorial/ActionLink.tsx
    - src/components/editorial/RegistryFreshnessPanel.tsx
    - src/styles/app.css
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <action>Implement D-04, D-05, D-06, D-07, D-08, D-09, UI-01, and SITE-13 on `/states/compare` only. In `src/routes/(site)/states/compare.tsx`, import `getStatesComparisonProofLanes` from `../../../lib/site/content`, create `const comparisonProofLanes = getStatesComparisonProofLanes();`, and render a compact proof panel immediately after `<RegistryFreshnessPanel ... />` and before the existing `Use this route...` panel/CTAs. Keep the existing `RegistryFreshnessPanel`, existing comparison sections, `StateCard` rendering, and sibling CTAs to `/states` and `/states/clusters`.</action>
  <action>The new panel must use a single outer `div` with `panel-wash rounded-[var(--radius-card)] p-5 sm:p-6`, not nested cards. Inside it, render label text `Comparison proof lanes`, a short sentence containing `dated snapshot`, and a responsive `grid gap-4 lg:grid-cols-3`. For each lane, render `lane.label`, `lane.countLabel`, `lane.representativeState.state`, `lane.context`, and an `ActionLink` with `href={lane.href}`, `intent="secondary"`, and visible CTA text `Open state record`. Use existing classes such as `text-accent-soft`, `text-ink-subtle`, `border-border-soft`, and `data-chip`; do not add `src/styles/app.css` classes unless a test or browser defect proves the utility string cannot meet the UI spec.</action>
  <action>Append a `## Local Adaptation Implemented` section to `999.1-CLOSEOUT-EVIDENCE.md` stating that the visible implementation is route-local to `/states/compare`, uses `panel-wash`, `data-chip`, and `ActionLink`, and leaves `package.json`, `bun.lock`, shared chrome, footer, metadata, README, profile, about, settings, and identity surfaces unchanged. Do not add animation, particles, bokeh/orbs, global background effects, icon packages, a matrix, search, ranking, scorecard, advanced filters, `/states` changes, `/states/clusters` changes, or OpenLinks-triggering surfaces.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>rg -n "getStatesComparisonProofLanes|comparisonProofLanes|Comparison proof lanes|Open state record|panel-wash|data-chip" "src/routes/(site)/states/compare.tsx"</automated>
    <automated>rg -n "Local Adaptation Implemented|route-local to `/states/compare`|panel-wash|data-chip|ActionLink" .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</automated>
    <automated>bash -euo pipefail -c 'if git diff -- package.json bun.lock src/styles/app.css "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" README.md src/app.tsx src/routes | rg -n "mystic-ui|@mystic-ui|withMysticUI|mystic-ui/tailwind/theme.css|matrix|ranking table|scorecard|advanced filter|search input|footer|metadata|profile|openlinks"; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/(site)/states/compare.tsx` imports `getStatesComparisonProofLanes`.
    - `src/routes/(site)/states/compare.tsx` contains `const comparisonProofLanes = getStatesComparisonProofLanes();`.
    - The rendered route contains `Comparison proof lanes`.
    - The rendered route contains `Open state record`.
    - The rendered route maps `comparisonProofLanes`.
    - The proof panel appears after `RegistryFreshnessPanel` and before the existing panel whose copy starts `Use this route when you want`.
    - `bun test src/lib/site/content.test.ts` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - The final diff does not modify `package.json`, `bun.lock`, `src/styles/app.css`, `src/routes/(site)/states/index.tsx`, `src/routes/(site)/states/clusters.tsx`, shared chrome, footer, metadata, README, profile, about, settings, or identity surfaces.
    - The final diff contains no `mystic-ui`, `@mystic-ui`, `withMysticUI`, `mystic-ui/tailwind/theme.css`, `matrix`, `ranking table`, `scorecard`, `advanced filter`, or `search input` implementation.
  </acceptance_criteria>
  <done>`/states/compare` has a compact local proof-lane panel that improves scanability while preserving existing editorial primitives, canonical links, and deferred SITE-13 boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mystic UI upstream to repo evidence | External GitHub/npm metadata is observed and summarized in phase evidence but is not installed or executed. |
| Generated content graph to proof-lane model | Repo-owned generated state data is transformed into route-facing proof-lane copy and canonical hrefs. |
| Route render to public reader | `/states/compare` presents factual comparison context and canonical links in the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Tampering | `package.json`, `bun.lock`, Tailwind setup | mitigate | Reject direct package adoption in Task 1; acceptance checks scan for `mystic-ui`, `withMysticUI`, Mystic theme CSS, and broad dependency churn. |
| T-999.1-02 | Repudiation | `999.1-CLOSEOUT-EVIDENCE.md` | mitigate | Record exact Mystic HEAD SHA, npm package facts, and the explicit direct-package rejection sentence so the adoption decision is auditable. |
| T-999.1-03 | Information Disclosure | `/states/compare` proof panel | accept | The panel renders already-public generated registry facts and canonical public state links; no secrets, user data, or new external calls are introduced. |
| T-999.1-04 | Spoofing | Proof-lane representative links | mitigate | Derive representative states from existing comparison sections and assert canonical `/states/${slug}` hrefs in `src/lib/site/content.test.ts`. |
| T-999.1-05 | Denial of Service | Route render complexity | mitigate | Keep the panel static and dependency-free; no particles, canvas, continuous animation, runtime network calls, or heavy client libraries are introduced. |
| T-999.1-06 | Elevation of Privilege | Third-party source code | mitigate | Do not install or import Mystic UI source in this phase; no new executable third-party UI code crosses into the app. |
</threat_model>

<verification>
Run these after all tasks:

```bash
bun test src/lib/site/content.test.ts
bunx tsc --noEmit
rg -n "getStatesComparisonProofLanes|Comparison proof lanes|Open state record" "src/routes/(site)/states/compare.tsx"
rg -n "Mystic UI was locally adapted|direct package was rejected for now|Local Adaptation Implemented" .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
bash -euo pipefail -c 'if git diff --name-only | rg -n "^(package.json|bun.lock|src/styles/app.css|src/routes/\\(site\\)/states/index.tsx|src/routes/\\(site\\)/states/clusters.tsx|README.md)$"; then exit 1; fi'
```
</verification>

<success_criteria>
- Direct Mystic UI dependency adoption is rejected with exact evidence and no package/lockfile changes.
- `src/lib/site/states-surfaces.ts` and `src/lib/site/content.ts` expose tested proof-lane helpers.
- `/states/compare` renders a local three-lane proof panel after `RegistryFreshnessPanel`.
- Every proof lane links to a canonical state detail route through `ActionLink` and uses `Open state record`.
- Tests prove three proof lanes, source-aware context, canonical representative hrefs, and no matrix/ranking/filter/search behavior.
- No shared chrome, footer, metadata, README, profile/about/settings, identity, `/states`, or `/states/clusters` surface changes occur.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-01-SUMMARY.md`.
</output>
