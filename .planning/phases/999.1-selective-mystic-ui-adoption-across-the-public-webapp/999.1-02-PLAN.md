---
phase: 999.1-selective-mystic-ui-adoption-across-the-public-webapp
plan: 02
type: execute
wave: 2
depends_on:
  - 999.1-01
files_modified:
  - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts
  - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json
  - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
autonomous: true
requirements:
  - UI-01
  - SITE-13
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 999.1-2026-06-03T01-47-12
generated_at: 2026-06-03T02:07:48.215Z
must_haves:
  truths:
    - "Targeted browser QA proves `/states/compare` renders the new proof panel at 390x844, 768x1024, and 1280x900 without horizontal overflow, clipped text/controls, missing required copy, or broken representative state links."
    - "Verification covers TypeScript, focused tests, full tests, build, precommit, and forbidden-churn scans after the local adaptation."
    - "Final closeout evidence states the phase outcome as locally adapted, explains why direct Mystic UI package adoption was rejected, and records command/browser evidence."
    - "The final diff leaves `package.json`, `bun.lock`, shared chrome, footer, metadata, README, profile/about/settings, `/states`, and `/states/clusters` untouched."
  artifacts:
    - path: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts"
      provides: "Phase-scoped Playwright QA harness for `/states/compare`"
      contains: "/states/compare"
    - path: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json"
      provides: "Machine-readable browser evidence across three viewports"
      contains: "\"status\": \"passed\""
    - path: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md"
      provides: "Final phase evidence for local adaptation and direct package rejection"
      contains: "Verification Evidence"
  key_links:
    - from: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts"
      to: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json"
      via: "Playwright route/viewport report write"
      pattern: "routeChecks"
    - from: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json"
      to: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md"
      via: "final evidence summary"
      pattern: "Browser QA"
    - from: ".planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts"
      to: "/states/compare"
      via: "BASE_URL route check"
      pattern: "/states/compare"
---

<objective>
Prove and close the local Mystic-inspired adaptation.

Purpose: After Plan 01 implements the `/states/compare` proof panel, this plan performs targeted browser QA, runs the full verification ladder, scans for forbidden dependency/UI churn, and records final phase evidence.
Output: Phase-scoped browser QA harness/report and complete closeout evidence for the local adaptation decision.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
@.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-01-SUMMARY.md
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts
@.planning/phases/17-expanded-surface-qa-and-closeout-prep/17-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@package.json
@src/routes/(site)/states/compare.tsx
@src/lib/site/content.ts
@src/lib/site/states-surfaces.ts
@src/lib/site/content.test.ts
@src/components/editorial/ActionLink.tsx
@src/components/editorial/RegistryFreshnessPanel.tsx

<interfaces>
Required browser route and viewports:
```typescript
const route = "/states/compare" as const;
const viewports = [
	{ name: "mobile", width: 390, height: 844 },
	{ name: "tablet", width: 768, height: 1024 },
	{ name: "desktop", width: 1280, height: 900 },
] as const;
```

Required proof-panel links:
```typescript
const requiredRepresentativeLinks = [
	"/states/texas",
	"/states/north-carolina",
	"/states/new-hampshire",
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run targeted `/states/compare` browser QA</name>
  <files>.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts, .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json</files>
  <read_first>
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-01-SUMMARY.md
    - .planning/phases/17-expanded-surface-qa-and-closeout-prep/17-browser-qa.ts
    - src/routes/(site)/states/compare.tsx
    - src/components/editorial/ActionLink.tsx
    - src/components/editorial/RegistryFreshnessPanel.tsx
    - package.json
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <action>Implement D-10 and D-11 with a phase-scoped Bun/TypeScript Playwright script at `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts`. Adapt the Phase 17 harness shape but scope it to `/states/compare` only. The script must import `chromium` and `type Page` from `playwright`, read `BASE_URL` with default `http://127.0.0.1:4173`, check viewports `390x844`, `768x1024`, and `1280x900`, and write `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json` with top-level fields `status`, `generatedAt`, `baseUrl`, `viewports`, `routes`, and `routeChecks`.</action>
  <action>For each viewport, navigate to `/states/compare` and assert OK response, body text contains `Compare the current registry without losing the source trail`, `Freshness`, `Comparison proof lanes`, `Reserve benchmarks`, `Crossover proposals`, `Bond-side signals`, `Open state record`, `dated snapshot`, `Back to the full registry`, and `Open cluster reading paths`; the page has no horizontal overflow; visible anchors/buttons/text controls/headings/paragraphs/spans do not extend outside the viewport by more than 2px; anchors exist for `/states/texas`, `/states/north-carolina`, `/states/new-hampshire`, `/states`, and `/states/clusters`; and request checks for each representative state link return OK. Focus the first `Open state record` link and assert either outline style is not `none` or box shadow is not `none`, proving a visible focus state.</action>
  <action>Run the script through the existing dev-server pattern from Phase 17: `bash -euo pipefail -c 'port=4173; bun run dev -- --host 127.0.0.1 --port "$port" >/tmp/bitcoin-bond-phase999-1-browser-server.log 2>&1 & server_pid=$!; trap "kill $server_pid >/dev/null 2>&1 || true" EXIT; for i in {1..60}; do if curl -fsS "http://127.0.0.1:$port/states/compare" >/dev/null; then break; fi; if [[ "$i" -eq 60 ]]; then cat /tmp/bitcoin-bond-phase999-1-browser-server.log; exit 1; fi; sleep 1; done; BASE_URL="http://127.0.0.1:$port" bun .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts'`. Do not add screenshots unless debugging a failure; the JSON report is the required committed browser evidence.</action>
  <verify>
    <automated>bash -euo pipefail -c 'port=4173; bun run dev -- --host 127.0.0.1 --port "$port" >/tmp/bitcoin-bond-phase999-1-browser-server.log 2>&1 & server_pid=$!; trap "kill $server_pid >/dev/null 2>&1 || true" EXIT; for i in {1..60}; do if curl -fsS "http://127.0.0.1:$port/states/compare" >/dev/null; then break; fi; if [[ "$i" -eq 60 ]]; then cat /tmp/bitcoin-bond-phase999-1-browser-server.log; exit 1; fi; sleep 1; done; BASE_URL="http://127.0.0.1:$port" bun .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts'</automated>
    <automated>jq -e '.status == "passed" and (.routeChecks | length) == 3 and (.routes | length) == 1 and (.viewports | length) == 3' .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json</automated>
    <automated>rg -n '"/states/compare"|"/states/texas"|"/states/north-carolina"|"/states/new-hampshire"|390|768|1280|"status": "passed"|focus' .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa.ts .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json</automated>
  </verify>
  <acceptance_criteria>
    - `999.1-browser-qa.ts` exists and imports `chromium` from `playwright`.
    - `999.1-browser-qa.ts` checks `/states/compare` at exactly `390x844`, `768x1024`, and `1280x900`.
    - `999.1-browser-qa.ts` asserts required proof-panel text: `Comparison proof lanes`, `Reserve benchmarks`, `Crossover proposals`, `Bond-side signals`, and `Open state record`.
    - `999.1-browser-qa.ts` asserts representative links `/states/texas`, `/states/north-carolina`, and `/states/new-hampshire`.
    - `999.1-browser-qa.ts` asserts visible focus state for an `Open state record` link.
    - `999.1-browser-qa-report.json` exists with `status: "passed"`.
    - `jq -e '.status == "passed" and (.routeChecks | length) == 3 and (.routes | length) == 1 and (.viewports | length) == 3' 999.1-browser-qa-report.json` exits 0.
  </acceptance_criteria>
  <done>Browser QA proves the changed comparison route renders and links correctly at mobile, tablet, and desktop widths without overflow or focus-state regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Run full verification and finalize closeout evidence</name>
  <files>.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CONTEXT.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-RESEARCH.md
    - .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-UI-SPEC.md
    - src/routes/(site)/states/compare.tsx
    - src/lib/site/content.ts
    - src/lib/site/states-surfaces.ts
    - src/lib/site/content.test.ts
    - package.json
    - bun.lock
    - src/styles/app.css
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <action>Implement D-10, D-12, UI-01, and SITE-13 closeout. Run `bunx tsc --noEmit`, `bun test src/lib/site/content.test.ts`, `bun test`, `bun run build`, and `bun run precommit` after browser QA passes. Then update `999.1-CLOSEOUT-EVIDENCE.md` with a `## Verification Evidence` section recording PASS lines for TypeScript, focused tests, full tests, production build, precommit, browser QA report status, route count `1`, viewport count `3`, and route-check count `3`. Keep the existing compatibility decision sentence from Plan 01 unchanged.</action>
  <action>Add a `## Final Scope Boundary` section stating: `No package or lockfile changes were made; no Mystic UI package, npm package, CLI, Tailwind 3.x helper, global theme CSS, class-based dark-mode dependency, shared chrome edit, footer edit, metadata edit, README edit, profile/about/settings edit, matrix, ranking table, advanced filter, or search surface shipped in Phase 999.1.` Also state that OpenLinks was not triggered because no identity, metadata, README, footer, profile, about, settings, or shared app chrome surface changed. If any of those surfaces did change, stop and use the `openlinks-identity-presence` skill before proceeding.</action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun test src/lib/site/content.test.ts</automated>
    <automated>bun test</automated>
    <automated>bun run build</automated>
    <automated>bun run precommit</automated>
    <automated>jq -e '.status == "passed" and (.routeChecks | length) == 3 and (.routes | length) == 1 and (.viewports | length) == 3' .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json</automated>
    <automated>rg -n "Verification Evidence|TypeScript - PASS|Focused content tests - PASS|Full tests - PASS|Production build - PASS|Precommit - PASS|Browser QA - PASS|Final Scope Boundary|No package or lockfile changes were made|OpenLinks was not triggered" .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md</automated>
    <automated>bash -euo pipefail -c 'if git diff --name-only | rg -n "^(package\\.json|bun\\.lock|src/styles/app\\.css|src/routes/\\(site\\)/states/index\\.tsx|src/routes/\\(site\\)/states/clusters\\.tsx|README\\.md|src/app\\.tsx|src/components/editorial/SiteHeader\\.tsx|src/components/editorial/SiteFooter\\.tsx|src/routes/\\(site\\)/(about|profile|settings)\\.tsx|src/lib/site/(metadata|openlinks|identity).*)$"; then exit 1; fi'</automated>
    <automated>bash -euo pipefail -c 'if git diff -- package.json bun.lock src/styles/app.css "src/routes/(site)/states/index.tsx" "src/routes/(site)/states/clusters.tsx" README.md src/app.tsx | rg -n "mystic-ui|@mystic-ui|withMysticUI|mystic-ui/tailwind/theme.css|\\.dark|matrix|ranking table|scorecard|advanced filter|search input|footer|metadata|profile|openlinks"; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits 0.
    - `bun test src/lib/site/content.test.ts` exits 0.
    - `bun test` exits 0.
    - `bun run build` exits 0.
    - `bun run precommit` exits 0.
    - Browser report shape check exits 0 with `status == "passed"`, `routeChecks.length == 3`, `routes.length == 1`, and `viewports.length == 3`.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `Verification Evidence`.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains PASS lines for TypeScript, focused content tests, full tests, production build, precommit, and browser QA.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `Final Scope Boundary`.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `No package or lockfile changes were made`.
    - `999.1-CLOSEOUT-EVIDENCE.md` contains `OpenLinks was not triggered`.
    - The final diff does not modify `package.json`, `bun.lock`, `src/styles/app.css`, `/states`, `/states/clusters`, shared chrome, footer, metadata, README, profile, about, settings, or identity surfaces.
  </acceptance_criteria>
  <done>Final evidence records the local adaptation outcome, command/browser proof, no-direct-adoption reason, and all scope boundaries needed for phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local browser server to Playwright QA | The dev server renders route HTML that the phase QA harness inspects across viewports. |
| QA report to phase evidence | Machine-readable browser report facts are summarized in human-readable closeout evidence. |
| Final diff to public app | Verification must prove no forbidden package, shared chrome, metadata, or deferred comparison-surface churn entered the public app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-07 | Tampering | Browser QA report | mitigate | Write report from a deterministic Bun/Playwright script and validate `status`, route count, viewport count, and route-check count with `jq`. |
| T-999.1-08 | Repudiation | `999.1-CLOSEOUT-EVIDENCE.md` | mitigate | Record explicit PASS lines for TypeScript, tests, build, precommit, browser QA, and the final scope boundary. |
| T-999.1-09 | Denial of Service | `/states/compare` responsive render | mitigate | Browser QA checks horizontal page overflow and visible text/control overflow at 390x844, 768x1024, and 1280x900. |
| T-999.1-10 | Spoofing | Representative state navigation | mitigate | Browser QA verifies anchors and OK responses for `/states/texas`, `/states/north-carolina`, and `/states/new-hampshire`. |
| T-999.1-11 | Information Disclosure | Dev-server QA | accept | QA runs against local public routes with no user data, credentials, secrets, or external service calls. |
| T-999.1-12 | Elevation of Privilege | Package/shared-surface churn | mitigate | Final scans fail if package files, Tailwind setup, shared chrome, footer, metadata, README, profile/about/settings, or OpenLinks-triggering surfaces change unexpectedly. |
</threat_model>

<verification>
Run these after all tasks:

```bash
bunx tsc --noEmit
bun test src/lib/site/content.test.ts
bun test
bun run build
bun run precommit
jq -e '.status == "passed" and (.routeChecks | length) == 3 and (.routes | length) == 1 and (.viewports | length) == 3' .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-browser-qa-report.json
rg -n "Verification Evidence|Final Scope Boundary|OpenLinks was not triggered" .planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-CLOSEOUT-EVIDENCE.md
bash -euo pipefail -c 'if git diff --name-only | rg -n "^(package\\.json|bun\\.lock|src/styles/app\\.css|src/routes/\\(site\\)/states/index\\.tsx|src/routes/\\(site\\)/states/clusters\\.tsx|README\\.md|src/app\\.tsx|src/components/editorial/SiteHeader\\.tsx|src/components/editorial/SiteFooter\\.tsx|src/routes/\\(site\\)/(about|profile|settings)\\.tsx|src/lib/site/(metadata|openlinks|identity).*)$"; then exit 1; fi'
```
</verification>

<success_criteria>
- Browser QA report passes for `/states/compare` at 390x844, 768x1024, and 1280x900.
- Browser QA proves required proof-panel copy, representative links, route CTAs, focus state, and no horizontal overflow.
- TypeScript, focused content tests, full tests, build, precommit, and browser report shape checks pass.
- Final evidence states the outcome as local adaptation and records why direct Mystic UI package adoption was rejected.
- Final scans prove no package/lockfile, Tailwind setup, shared chrome, footer, metadata, README, profile/about/settings, `/states`, `/states/clusters`, matrix, ranking, advanced filter, search, or OpenLinks-triggering surface changes shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-selective-mystic-ui-adoption-across-the-public-webapp/999.1-02-SUMMARY.md`.
</output>
