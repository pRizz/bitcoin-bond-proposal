---
phase: 16-throughput-expansion-and-deferral-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/states/south-dakota-hb-1155.md
  - content/states/wyoming-hb-0201.md
  - content/data/state-registry-manifest.json
  - content/data/state-candidate-intake.json
autonomous: true
requirements:
  - CATA-12
  - CATA-13
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-06-02T12-12-22
generated_at: 2026-06-02T12:36:30Z
must_haves:
  truths:
    - "South Dakota and Wyoming are resolved in generated priority order: each is either published with current official-source footing or deferred with a concrete non-public reason."
    - "No public `content/states/*.md` entry exists for a candidate whose official final status cannot be confirmed from current state sources."
    - "Published South Dakota and Wyoming entries, when source checks pass, are terminal failed/inactive reserve-side records with official source trails, confidence notes, `statusAsOf`, and `lastReviewed`."
    - "Any remaining unpublished candidate has `readiness: \"defer\"`, `nextAction: \"defer-until-stronger-official-source\"`, and a deferral reason that names checked official sources and the condition for publication."
  artifacts:
    - path: "content/states/south-dakota-hb-1155.md"
      provides: "South Dakota HB 1155 public state entry when official source checks pass"
      contains: "Deferred to the 41st legislative day"
    - path: "content/states/wyoming-hb-0201.md"
      provides: "Wyoming HB0201 public state entry when official source checks pass"
      contains: "Died in Committee Returned Bill Pursuant to HR 5-4"
    - path: "content/data/state-registry-manifest.json"
      provides: "Published or unresearched boundary for South Dakota and Wyoming"
      contains: "\"slug\": \"south-dakota\""
    - path: "content/data/state-candidate-intake.json"
      provides: "Machine-readable deferral ledger outside public content"
      contains: "\"candidates\""
  key_links:
    - from: "content/states/south-dakota-hb-1155.md"
      to: "content/data/state-registry-manifest.json"
      via: "matching frontmatter slug `south-dakota` and manifest slug"
      pattern: "\"slug\": \"south-dakota\""
    - from: "content/states/wyoming-hb-0201.md"
      to: "content/data/state-registry-manifest.json"
      via: "matching frontmatter slug `wyoming` and manifest slug"
      pattern: "\"slug\": \"wyoming\""
    - from: "content/data/state-candidate-intake.json"
      to: "generated/refresh/state-priority-queue.json"
      via: "Plan 16-02 `bun run refresh:queue`"
      pattern: "\"firstPublicationCandidates\""
---

<objective>
Resolve the two remaining high-interest candidates, South Dakota HB 1155 and Wyoming HB0201, through the official-source publication gate.

Purpose: Phase 16 must grow the registry only when source quality supports public content and must otherwise keep deferral evidence in the non-public candidate ledger.
Output: Canonical South Dakota and Wyoming state entries if source checks pass, or explicit candidate deferrals if either source trail cannot be confirmed.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-CONTEXT.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-RESEARCH.md
@.planning/phases/15-first-expansion-publication-wave/15-VERIFICATION.md
@.planning/phases/15-first-expansion-publication-wave/15-03-SUMMARY.md
@content/data/state-candidate-intake.json
@content/data/state-registry-manifest.json
@content/data/README.md
@content/states/florida-sb-550.md
@content/states/north-dakota-hb-1184.md
@src/lib/content/schema.ts
@src/lib/site/candidate-priority.ts
@scripts/validate-content.ts
@package.json

<interfaces>
State entry frontmatter contract from `src/lib/content/schema.ts`:
```typescript
export const StateEntryFrontmatterSchema = z.object({
	title: NonEmptyStringSchema,
	slug: SlugSchema,
	summary: NonEmptyStringSchema,
	state: NonEmptyStringSchema,
	recordType: RecordTypeSchema,
	proposalKind: ProposalKindValueSchema,
	proposalSubtype: ProposalSubtypeValueSchema,
	billId: NonEmptyStringSchema,
	chamber: NonEmptyStringSchema,
	status: NonEmptyStringSchema,
	legislativeStatusGroup: LegislativeStatusGroupSchema,
	statusAsOf: IsoDateSchema,
	lastReviewed: IsoDateSchema,
	sponsors: z.array(NonEmptyStringSchema).min(1),
	primarySources: z.array(SourceLinkSchema).min(1),
	secondarySources: z.array(SourceLinkSchema).default([]),
	confidence: z.enum(["high", "medium", "low"]),
	effect: NonEmptyStringSchema,
	classificationNote: z.string().trim().optional(),
});
```

Candidate deferral contract from `src/lib/content/schema.ts`:
```typescript
if (entry.readiness === "defer" && !entry.deferralReason) {
	context.addIssue({
		code: "custom",
		message: "deferred candidates require a deferralReason",
		path: ["deferralReason"],
	});
}

const expectedNextAction = expectedNextActionByReadiness[entry.readiness];
if (entry.nextAction !== expectedNextAction) {
	context.addIssue({
		code: "custom",
		message: `${entry.readiness} candidates require nextAction ${expectedNextAction}`,
		path: ["nextAction"],
	});
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve South Dakota HB 1155 through the official-source gate</name>
  <files>content/states/south-dakota-hb-1155.md, content/data/state-registry-manifest.json, content/data/state-candidate-intake.json</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-CONTEXT.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-RESEARCH.md
    - content/data/state-candidate-intake.json
    - content/data/state-registry-manifest.json
    - content/data/README.md
    - content/states/florida-sb-550.md
    - content/states/north-dakota-hb-1184.md
    - src/lib/content/schema.ts
  </read_first>
  <behavior>
    - Source pass: official South Dakota bill, introduced-text, vote, minutes/report/journal checks confirm HB 1155 and `Deferred to the 41st legislative day`; publish `content/states/south-dakota-hb-1155.md`, mark the manifest row `published`, and remove `south-dakota` from candidate intake.
    - Source fail: do not create `content/states/south-dakota-hb-1155.md`; keep the manifest row `unresearched`; keep the candidate row with `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and a concrete `deferralReason`.
  </behavior>
  <action>Implement D-01 through D-09 for South Dakota first, before Wyoming per D-02. Re-check official South Dakota sources before editing public content per D-04. Use the official bill API `https://sdlegislature.gov/api/Bills/26633`, introduced text `https://mylrc.sdlegislature.gov/api/Documents/298084.pdf`, vote API `https://sdlegislature.gov/api/Votes/83383`, minutes `https://mylrc.sdlegislature.gov/api/Documents/Minutes/301323.pdf?Year=2026`, committee report `https://mylrc.sdlegislature.gov/api/Documents/CommitteeReport/301846.pdf?Year=2026`, and journal `https://mylrc.sdlegislature.gov/api/Documents/Journal/301710.pdf?Year=2026`. If these checks still confirm BillId `26633`, bill `HB 1155`, title `permit the state to invest in Bitcoin.`, introduced text mechanics, House Commerce and Energy action `Deferred to the 41st legislative day`, ActionDate `2026-02-06T10:00:00-06:00`, Yeas `10`, and Nays `3`, publish the public record per D-05 and D-09.</action>
  <action>When the South Dakota source gate passes, create `content/states/south-dakota-hb-1155.md` using the existing failed-entry structure from `content/states/florida-sb-550.md` and `content/states/north-dakota-hb-1184.md`. Use these exact frontmatter values: `title: South Dakota HB 1155`, `slug: south-dakota`, `state: South Dakota`, `recordType: legislative-bill`, `proposalKind: reserve`, `proposalSubtype: strategic-reserve`, `billId: HB 1155`, `chamber: South Dakota House`, `status: Deferred to the 41st legislative day`, `legislativeStatusGroup: failed`, `statusAsOf: 2026-02-06`, `lastReviewed: 2026-06-02`, sponsors `Rep. Logan Manhart`, `Sen. Tom Pischke`, `Rep. Tim Goodwin`, `Rep. Phil Jensen`, and `Rep. Dylan Jordan`, `secondarySources: []`, `confidence: high`, and an `effect` stating that the bill would have permitted the State Investment Council to invest up to ten percent of eligible state moneys in Bitcoin but did not become law after the committee deferred it to the 41st legislative day. Put the official source URLs above in `primarySources` and in `## Source Trail`. Keep the prose terminal: the record is useful as failed official-source evidence, not enacted authority or active momentum.</action>
  <action>When the South Dakota source gate passes, update only the South Dakota row in `content/data/state-registry-manifest.json` to `registryStatus: "published"`, `proposalFocus: "reserve"`, `shortNote: "Failed 2026 State Investment Council Bitcoin authority proposal."`, and `editorialPriority: "reserve-priority"` while preserving `region: "midwest"`. Remove only the `south-dakota` row from `content/data/state-candidate-intake.json` per D-09. When the South Dakota source gate fails, do not create the Markdown file; keep the manifest row `unresearched`; update the South Dakota candidate row to `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, `status: "Official introduced text found; final official status not confirmed during Phase 16 source gate"`, `statusAsOf: "2026-06-02"`, and a `deferralReason` naming every official URL checked above and stating that publication requires a current official final-status source confirming the committee deferral or another terminal action.</action>
  <verify>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then bill_json=$(curl -fsSL https://sdlegislature.gov/api/Bills/26633); printf "%s\n" "$bill_json" | rg "\"BillId\":26633"; printf "%s\n" "$bill_json" | rg "\"BillNumber\":1155"; printf "%s\n" "$bill_json" | rg "permit the state to invest in Bitcoin"; printf "%s\n" "$bill_json" | rg "\"FirstName\":\"Logan\""; printf "%s\n" "$bill_json" | rg "\"LastName\":\"Manhart\""; printf "%s\n" "$bill_json" | rg "\"FirstName\":\"Tom\""; printf "%s\n" "$bill_json" | rg "\"LastName\":\"Pischke\""; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then vote_json=$(curl -fsSL https://sdlegislature.gov/api/Votes/83383); printf "%s\n" "$vote_json" | rg "\"StatusText\":\"Deferred to the 41st legislative day\""; printf "%s\n" "$vote_json" | rg "\"Yeas\":10"; printf "%s\n" "$vote_json" | rg "\"Nays\":3"; printf "%s\n" "$vote_json" | rg "2026-02-06T10:00:00-06:00"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then bill_text=$(curl -fsSL https://mylrc.sdlegislature.gov/api/Documents/298084.pdf | pdftotext - -); printf "%s\n" "$bill_text" | rg "State Investment Council"; printf "%s\n" "$bill_text" | rg "invest in Bitcoin"; printf "%s\n" "$bill_text" | rg "ten percent"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then minutes_text=$(curl -fsSL "https://mylrc.sdlegislature.gov/api/Documents/Minutes/301323.pdf?Year=2026" | pdftotext - -); printf "%s\n" "$minutes_text" | rg "HB 1155"; printf "%s\n" "$minutes_text" | rg "Deferred to the 41st legislative day"; printf "%s\n" "$minutes_text" | rg "10-3"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then report_text=$(curl -fsSL "https://mylrc.sdlegislature.gov/api/Documents/CommitteeReport/301846.pdf?Year=2026" | pdftotext - -); printf "%s\n" "$report_text" | rg "HB 1155"; printf "%s\n" "$report_text" | rg "Deferred to the 41st Legislative Day"; fi'</automated>
    <automated>bun -e 'import { parseStateCandidateIntake, parseStateRegistryManifest } from "./src/lib/content/schema.ts"; const manifest = parseStateRegistryManifest(await Bun.file("content/data/state-registry-manifest.json").json()); const intake = parseStateCandidateIntake(await Bun.file("content/data/state-candidate-intake.json").json()); const row = manifest.states.find((entry) => entry.slug === "south-dakota"); const maybeCandidate = intake.candidates.find((entry) => entry.slug === "south-dakota"); const fileExists = await Bun.file("content/states/south-dakota-hb-1155.md").exists(); if (fileExists) { if (row?.registryStatus !== "published") throw new Error("South Dakota file exists but manifest row is not published"); if (maybeCandidate) throw new Error("South Dakota published file exists but candidate row remains"); } else { if (row?.registryStatus !== "unresearched") throw new Error("Deferred South Dakota must keep manifest unresearched"); if (!maybeCandidate) throw new Error("Deferred South Dakota must remain in candidate intake"); if (maybeCandidate.readiness !== "defer") throw new Error("Deferred South Dakota candidate row must use readiness defer"); if (maybeCandidate.nextAction !== "defer-until-stronger-official-source") throw new Error("Deferred South Dakota candidate row has wrong nextAction"); if (!maybeCandidate.deferralReason?.trim()) throw new Error("Deferred South Dakota candidate row is missing deferralReason"); }'</automated>
  </verify>
  <acceptance_criteria>
    - If the source gate passes, `test -f content/states/south-dakota-hb-1155.md` exits 0.
    - If the source gate passes, `rg -n "slug: south-dakota|Deferred to the 41st legislative day|statusAsOf: 2026-02-06|lastReviewed: 2026-06-02|confidence: high|## Source Trail|## Confidence / Completeness Note" content/states/south-dakota-hb-1155.md` returns matches.
    - If the source gate passes, `bun -e 'const intake = await Bun.file("content/data/state-candidate-intake.json").json(); if (intake.candidates.some((entry) => entry.slug === "south-dakota")) process.exit(1);'` exits 0.
    - If the source gate fails, `test ! -f content/states/south-dakota-hb-1155.md` exits 0 and the South Dakota candidate row has `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and a non-empty `deferralReason` naming the official sources checked.
  </acceptance_criteria>
  <done>South Dakota is either published as a high-confidence failed reserve-side record or deferred in the non-public candidate ledger with a concrete official-source gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve Wyoming HB0201 through the official-source gate</name>
  <files>content/states/wyoming-hb-0201.md, content/data/state-registry-manifest.json, content/data/state-candidate-intake.json</files>
  <read_first>
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-CONTEXT.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-RESEARCH.md
    - content/data/state-candidate-intake.json
    - content/data/state-registry-manifest.json
    - content/data/README.md
    - content/states/florida-sb-550.md
    - content/states/north-dakota-hb-1184.md
    - src/lib/content/schema.ts
  </read_first>
  <behavior>
    - Source pass: official Wyoming OData, introduced text, and digest checks confirm HB0201 `inactive` status and `H:Died in Committee Returned Bill Pursuant to HR 5-4`; publish `content/states/wyoming-hb-0201.md`, mark the manifest row `published`, and remove `wyoming` from candidate intake.
    - Source fail: do not create `content/states/wyoming-hb-0201.md`; keep the manifest row `unresearched`; keep the candidate row with `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and a concrete `deferralReason`.
  </behavior>
  <action>Implement D-01 through D-09 for Wyoming after the South Dakota gate per D-02. Re-check official Wyoming sources before editing public content per D-04. Use the official BillReferences OData URL `https://api.wyoleg.gov/v1/odata/BillReferences?year=2025&billNumber=HB0201&expand=substituteBills,vetoes,amendments($expand=amendmentBudgetSections)`, the official BillInformations OData URL `https://api.wyoleg.gov/v1/odata/BillInformations?select=BillNum,ShortTitle,Year,ChapterNo,Sponsor,EnrolledNo,LastActionDate,LastAction,SignedDate,EffectiveDate,BillType,SpecialSessionValue,BillStatus&filter=Year%20eq%202025%20and%20SpecialSessionValue%20eq%20null%20and%20BillNum%20eq%20%27HB0201%27`, introduced text `https://www.wyoleg.gov/2025/Introduced/HB0201.pdf`, and digest `https://www.wyoleg.gov/2025/Digest/HB0201.pdf`. If these checks still confirm `HB0201`, title `State funds-investment in Bitcoin.`, `billStatus`/`BillStatus` `inactive`, last action `H:Died in Committee Returned Bill Pursuant to HR 5-4`, final date `2025-03-03`, and H09 Minerals do-pass failure `1-7-1-0-0` on 2025-02-10, publish the public record per D-05 and D-09.</action>
  <action>When the Wyoming source gate passes, create `content/states/wyoming-hb-0201.md` using the same failed-entry structure as `content/states/florida-sb-550.md` and `content/states/north-dakota-hb-1184.md`. Use these exact frontmatter values: `title: Wyoming HB0201`, `slug: wyoming`, `state: Wyoming`, `recordType: legislative-bill`, `proposalKind: reserve`, `proposalSubtype: strategic-reserve`, `billId: HB0201`, `chamber: Wyoming House`, `status: H:Died in Committee Returned Bill Pursuant to HR 5-4`, `legislativeStatusGroup: failed`, `statusAsOf: 2025-03-03`, `lastReviewed: 2026-06-02`, sponsors `Rep. Wasserburger`, `Rep. Brown, G`, `Rep. Filer`, `Rep. Lucas`, `Rep. Singh`, and `Sen. Smith, D`, `secondarySources: []`, `confidence: high`, and an `effect` stating that the bill would have authorized state and permanent-fund investment in Bitcoin subject to three-percent caps and custody requirements but died in committee. Put the official source URLs above in `primarySources` and in `## Source Trail`. Keep the prose terminal and inactive.</action>
  <action>When the Wyoming source gate passes, update only the Wyoming row in `content/data/state-registry-manifest.json` to `registryStatus: "published"`, `proposalFocus: "reserve"`, `shortNote: "Failed 2025 state-funds Bitcoin investment proposal."`, and `editorialPriority: "reserve-priority"` while preserving `region: "west"`. Remove only the `wyoming` row from `content/data/state-candidate-intake.json` per D-09. When the Wyoming source gate fails, do not create the Markdown file; keep the manifest row `unresearched`; update the Wyoming candidate row to `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, `status: "Official introduced text found; final official status not confirmed during Phase 16 source gate"`, `statusAsOf: "2026-06-02"`, and a `deferralReason` naming every official URL checked above and stating that publication requires a current official final-status source confirming inactive/died-in-committee posture or another terminal action.</action>
  <verify>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then bill_ref_json=$(curl -fsSL "https://api.wyoleg.gov/v1/odata/BillReferences?year=2025&billNumber=HB0201&expand=substituteBills,vetoes,amendments(\$expand=amendmentBudgetSections)"); printf "%s\n" "$bill_ref_json" | rg "HB0201"; printf "%s\n" "$bill_ref_json" | rg "State funds-investment in Bitcoin"; printf "%s\n" "$bill_ref_json" | rg "inactive"; printf "%s\n" "$bill_ref_json" | rg "H:Died in Committee Returned Bill Pursuant to HR 5-4"; printf "%s\n" "$bill_ref_json" | rg "2025-03-03"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then bill_info_json=$(curl -fsSL "https://api.wyoleg.gov/v1/odata/BillInformations?select=BillNum,ShortTitle,Year,ChapterNo,Sponsor,EnrolledNo,LastActionDate,LastAction,SignedDate,EffectiveDate,BillType,SpecialSessionValue,BillStatus&filter=Year%20eq%202025%20and%20SpecialSessionValue%20eq%20null%20and%20BillNum%20eq%20%27HB0201%27"); printf "%s\n" "$bill_info_json" | rg "HB0201"; printf "%s\n" "$bill_info_json" | rg "inactive"; printf "%s\n" "$bill_info_json" | rg "H:Died in Committee Returned Bill Pursuant to HR 5-4"; printf "%s\n" "$bill_info_json" | rg "2025-03-03"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then bill_text=$(curl -fsSL https://www.wyoleg.gov/2025/Introduced/HB0201.pdf | pdftotext - -); printf "%s\n" "$bill_text" | rg -i "State funds-investment in Bitcoin"; printf "%s\n" "$bill_text" | rg "three percent"; printf "%s\n" "$bill_text" | rg "qualified custodian"; printf "%s\n" "$bill_text" | rg "permanent funds"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then digest_text=$(curl -fsSL https://www.wyoleg.gov/2025/Digest/HB0201.pdf | pdftotext - -); printf "%s\n" "$digest_text" | rg "H09 - Minerals"; printf "%s\n" "$digest_text" | rg "1-7-1-0-0"; printf "%s\n" "$digest_text" | rg "Died in Committee Returned Bill Pursuant to HR 5-4"; printf "%s\n" "$digest_text" | rg "3/3/2025"; fi'</automated>
    <automated>bun -e 'import { parseStateCandidateIntake, parseStateRegistryManifest } from "./src/lib/content/schema.ts"; const manifest = parseStateRegistryManifest(await Bun.file("content/data/state-registry-manifest.json").json()); const intake = parseStateCandidateIntake(await Bun.file("content/data/state-candidate-intake.json").json()); const row = manifest.states.find((entry) => entry.slug === "wyoming"); const maybeCandidate = intake.candidates.find((entry) => entry.slug === "wyoming"); const fileExists = await Bun.file("content/states/wyoming-hb-0201.md").exists(); if (fileExists) { if (row?.registryStatus !== "published") throw new Error("Wyoming file exists but manifest row is not published"); if (maybeCandidate) throw new Error("Wyoming published file exists but candidate row remains"); } else { if (row?.registryStatus !== "unresearched") throw new Error("Deferred Wyoming must keep manifest unresearched"); if (!maybeCandidate) throw new Error("Deferred Wyoming must remain in candidate intake"); if (maybeCandidate.readiness !== "defer") throw new Error("Deferred Wyoming candidate row must use readiness defer"); if (maybeCandidate.nextAction !== "defer-until-stronger-official-source") throw new Error("Deferred Wyoming candidate row has wrong nextAction"); if (!maybeCandidate.deferralReason?.trim()) throw new Error("Deferred Wyoming candidate row is missing deferralReason"); }'</automated>
  </verify>
  <acceptance_criteria>
    - If the source gate passes, `test -f content/states/wyoming-hb-0201.md` exits 0.
    - If the source gate passes, `rg -n "slug: wyoming|Died in Committee Returned Bill Pursuant to HR 5-4|statusAsOf: 2025-03-03|lastReviewed: 2026-06-02|confidence: high|## Source Trail|## Confidence / Completeness Note" content/states/wyoming-hb-0201.md` returns matches.
    - If the source gate passes, `bun -e 'const intake = await Bun.file("content/data/state-candidate-intake.json").json(); if (intake.candidates.some((entry) => entry.slug === "wyoming")) process.exit(1);'` exits 0.
    - If the source gate fails, `test ! -f content/states/wyoming-hb-0201.md` exits 0 and the Wyoming candidate row has `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and a non-empty `deferralReason` naming the official sources checked.
  </acceptance_criteria>
  <done>Wyoming is either published as a high-confidence failed reserve-side record or deferred in the non-public candidate ledger with a concrete official-source gap.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the publication and deferral boundary</name>
  <files>content/data/state-registry-manifest.json, content/data/state-candidate-intake.json</files>
  <read_first>
    - content/data/state-registry-manifest.json
    - content/data/state-candidate-intake.json
    - content/data/README.md
    - src/lib/content/schema.ts
    - scripts/validate-content.ts
  </read_first>
  <action>Validate the final canonical boundary after Tasks 1 and 2 per D-03, D-07, D-08, D-09, and CATA-12/CATA-13. If both source gates passed, `content/data/state-candidate-intake.json` must be exactly `{ "candidates": [] }`, South Dakota and Wyoming manifest rows must be `published`, and both Markdown files must exist. If one or both source gates failed, only failed states may remain in candidate intake, each remaining row must use `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, a non-empty `deferralReason`, and a manifest row that remains `unresearched`; no public Markdown file may exist for a deferred state. Do not add a separate public deferral page, route, catalog copy, cluster QA, comparison QA, or browser-surface check in Phase 16; those are deferred to Phase 17.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bun -e 'import { parseStateCandidateIntake, parseStateRegistryManifest } from "./src/lib/content/schema.ts"; const manifest = parseStateRegistryManifest(await Bun.file("content/data/state-registry-manifest.json").json()); const intake = parseStateCandidateIntake(await Bun.file("content/data/state-candidate-intake.json").json()); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); for (const slug of ["south-dakota","wyoming"]) { const filePath = slug === "south-dakota" ? "content/states/south-dakota-hb-1155.md" : "content/states/wyoming-hb-0201.md"; const fileExists = await Bun.file(filePath).exists(); const maybeCandidate = intake.candidates.find((entry) => entry.slug === slug); const row = rows.get(slug); if (fileExists) { if (row?.registryStatus !== "published") throw new Error(`${slug} has public content but manifest is not published`); if (maybeCandidate) throw new Error(`${slug} has public content but remains in candidate intake`); } else { if (!maybeCandidate) throw new Error(`${slug} has no public content and no candidate ledger row`); if (row?.registryStatus !== "unresearched") throw new Error(`${slug} deferred row must remain unresearched in manifest`); if (maybeCandidate.readiness !== "defer" || maybeCandidate.nextAction !== "defer-until-stronger-official-source" || !maybeCandidate.deferralReason) throw new Error(`${slug} deferred candidate row is incomplete`); } } console.log(`Phase 16 boundary valid; remaining candidate count: ${intake.candidates.length}`);'</automated>
    <automated>bun -e 'const intake = await Bun.file("content/data/state-candidate-intake.json").json(); const bothFilesExist = (await Bun.file("content/states/south-dakota-hb-1155.md").exists()) && (await Bun.file("content/states/wyoming-hb-0201.md").exists()); if (bothFilesExist && intake.candidates.length !== 0) throw new Error("Both states published but candidate intake is not empty"); if (bothFilesExist) console.log("Both Phase 16 candidates published and candidate intake is empty.");'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0.
    - Every public South Dakota/Wyoming Markdown file has a matching `published` manifest row and no candidate intake row.
    - Every deferred South Dakota/Wyoming candidate row has no public Markdown file, keeps a manifest row with `registryStatus: "unresearched"`, and includes `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and a non-empty `deferralReason`.
    - If both source gates pass, `content/data/state-candidate-intake.json` is exactly `{ "candidates": [] }` after formatting.
  </acceptance_criteria>
  <done>The Phase 16 canonical content/data boundary is internally consistent and ready for generated-artifact regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official state source -> public Markdown | Current legislative status and bill mechanics cross from official APIs/PDFs into public explanatory content. |
| candidate intake -> public registry | Maintainer-only candidate rows may become public only after the source gate passes. |
| public Markdown -> manifest | A state file becomes reachable public registry content only when its slug is published in the manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | `content/states/south-dakota-hb-1155.md` | mitigate | Require official bill, text, vote, minutes, and report/journal spot checks before publishing South Dakota; otherwise defer outside public content. |
| T-16-01-02 | Tampering | `content/states/wyoming-hb-0201.md` | mitigate | Require official OData, introduced text, and digest spot checks before publishing Wyoming; otherwise defer outside public content. |
| T-16-01-03 | Repudiation | terminal status claims | mitigate | Store `statusAsOf`, `lastReviewed`, official source URLs, `confidence: high`, and confidence/completeness notes in each published entry. |
| T-16-01-04 | Information Disclosure | `content/data/state-candidate-intake.json` vs `content/states/*.md` | mitigate | Keep failed source gates in candidate intake only, with deferral reasons, and verify no public Markdown exists for deferred states. |
| T-16-01-05 | Elevation of Privilege | generated public registry reachability | accept | Phase 16 edits static content/data only; no auth, role, database, or privileged runtime boundary is introduced. |
</threat_model>

<verification>
Run after all tasks:

```bash
bun run validate:content
bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then sd_vote=$(curl -fsSL https://sdlegislature.gov/api/Votes/83383); printf "%s\n" "$sd_vote" | rg "\"StatusText\":\"Deferred to the 41st legislative day\""; printf "%s\n" "$sd_vote" | rg "\"Yeas\":10"; printf "%s\n" "$sd_vote" | rg "\"Nays\":3"; fi'
bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then wy_info=$(curl -fsSL "https://api.wyoleg.gov/v1/odata/BillInformations?select=BillNum,ShortTitle,Year,ChapterNo,Sponsor,EnrolledNo,LastActionDate,LastAction,SignedDate,EffectiveDate,BillType,SpecialSessionValue,BillStatus&filter=Year%20eq%202025%20and%20SpecialSessionValue%20eq%20null%20and%20BillNum%20eq%20%27HB0201%27"); printf "%s\n" "$wy_info" | rg "HB0201"; printf "%s\n" "$wy_info" | rg "inactive"; printf "%s\n" "$wy_info" | rg "H:Died in Committee Returned Bill Pursuant to HR 5-4"; fi'
bun -e 'import { parseStateCandidateIntake, parseStateRegistryManifest } from "./src/lib/content/schema.ts"; const manifest = parseStateRegistryManifest(await Bun.file("content/data/state-registry-manifest.json").json()); const intake = parseStateCandidateIntake(await Bun.file("content/data/state-candidate-intake.json").json()); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); for (const [slug, filePath] of [["south-dakota", "content/states/south-dakota-hb-1155.md"], ["wyoming", "content/states/wyoming-hb-0201.md"]]) { const fileExists = await Bun.file(filePath).exists(); const row = rows.get(slug); const maybeCandidate = intake.candidates.find((entry) => entry.slug === slug); if (fileExists) { if (row?.registryStatus !== "published") throw new Error(`${slug} public file exists but manifest row is not published`); if (maybeCandidate) throw new Error(`${slug} public file exists but candidate row remains`); } else { if (row?.registryStatus !== "unresearched") throw new Error(`${slug} deferred row must keep manifest unresearched`); if (!maybeCandidate) throw new Error(`${slug} deferred row must remain in candidate intake`); if (maybeCandidate.readiness !== "defer" || maybeCandidate.nextAction !== "defer-until-stronger-official-source" || !maybeCandidate.deferralReason?.trim()) throw new Error(`${slug} deferred candidate row is incomplete`); } }'
bun -e 'import { parseStateCandidateIntake } from "./src/lib/content/schema.ts"; const data = await Bun.file("content/data/state-candidate-intake.json").json(); parseStateCandidateIntake(data); console.log(`candidate rows: ${data.candidates.length}`);'
```
</verification>

<success_criteria>
- South Dakota and Wyoming are attempted in priority order.
- Each state is either published with official-source terminal status evidence or deferred with a concrete machine-readable deferral reason.
- No source-unconfirmed candidate is exposed through `content/states/*.md`.
- If both official-source checks pass, `content/states/south-dakota-hb-1155.md` and `content/states/wyoming-hb-0201.md` exist, both manifest rows are `published`, and `content/data/state-candidate-intake.json` contains an empty `candidates` array.
- If a source check fails, the corresponding manifest row remains `unresearched` and the candidate row preserves the publication-resume condition in `deferralReason`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-throughput-expansion-and-deferral-ledger/16-01-SUMMARY.md`.
</output>
