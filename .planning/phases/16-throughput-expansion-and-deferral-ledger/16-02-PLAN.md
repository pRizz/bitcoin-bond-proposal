---
phase: 16-throughput-expansion-and-deferral-ledger
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - generated/content-graph.json
  - generated/refresh/state-refresh-queue.json
  - generated/refresh/state-refresh-queue.md
  - generated/refresh/state-priority-queue.json
  - generated/refresh/state-priority-queue.md
  - src/lib/site/content.test.ts
  - src/lib/site/candidate-priority.test.ts
  - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md
autonomous: true
requirements:
  - CATA-12
  - CATA-13
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-06-02T12-12-22
generated_at: 2026-06-02T12:36:30Z
must_haves:
  truths:
    - "Generated content graph and refresh artifacts match the final publish-or-defer outcome from Plan 16-01."
    - "If both source gates pass, the registry compiles to 17 published state entries and the first-publication candidate list is empty."
    - "If either source gate fails, generated priority artifacts retain only deferred candidate rows and do not expose unverified public state content."
    - "Focused tests prove updated published counts and zero remaining first-publication candidates when both states publish."
    - "Full verification, code review evidence, and lifecycle validation pass before Phase 17 surface QA begins."
  artifacts:
    - path: "generated/content-graph.json"
      provides: "Compiled public route data for the Phase 16 registry outcome"
      contains: "\"publishedSlugs\""
    - path: "generated/refresh/state-priority-queue.json"
      provides: "Machine-readable combined refresh/candidate queue"
      contains: "\"firstPublicationCandidates\""
    - path: "generated/refresh/state-priority-queue.md"
      provides: "Maintainer-readable refresh and candidate queue"
      contains: "First Publication Candidates"
    - path: "src/lib/site/content.test.ts"
      provides: "Published count and grouped registry expectations"
      contains: "publishedCount"
    - path: "src/lib/site/candidate-priority.test.ts"
      provides: "Zero-candidate priority behavior coverage"
      contains: "firstPublicationCandidateCount"
  key_links:
    - from: "content/states/*.md"
      to: "generated/content-graph.json"
      via: "bun run compile:content"
      pattern: "\"states\""
    - from: "content/data/state-candidate-intake.json"
      to: "generated/refresh/state-priority-queue.json"
      via: "bun run refresh:queue"
      pattern: "\"candidateIntake\""
    - from: "generated/content-graph.json"
      to: "src/lib/site/content.test.ts"
      via: "imported fixture graph"
      pattern: "generated/content-graph.json"
    - from: "generated/refresh/state-priority-queue.json"
      to: "src/lib/site/candidate-priority.test.ts"
      via: "combined priority model summary expectations"
      pattern: "firstPublicationCandidateCount"
---

<objective>
Regenerate generated artifacts, update focused tests, and run the full Phase 16 verification contract.

Purpose: Plan 16-01 changes canonical content/data; this plan proves the compiled public registry, maintainer priority queue, tests, build, code review, and lifecycle metadata all match that outcome without pulling Phase 17 browser-surface QA into scope.
Output: Updated generated content/refresh artifacts, focused test expectations, code review artifact, verification evidence, and lifecycle validation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-CONTEXT.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-RESEARCH.md
@.planning/phases/16-throughput-expansion-and-deferral-ledger/16-01-SUMMARY.md
@content/data/state-candidate-intake.json
@content/data/state-registry-manifest.json
@content/data/README.md
@generated/content-graph.json
@generated/refresh/state-priority-queue.json
@generated/refresh/state-priority-queue.md
@scripts/compile-content.ts
@scripts/build-refresh-queue.ts
@scripts/precommit.ts
@src/lib/site/content.test.ts
@src/lib/site/candidate-priority.test.ts
@src/lib/content/schema.test.ts
@package.json

<interfaces>
Compiled graph shape from `scripts/compile-content.ts`:
```typescript
return {
	states: compiledStates,
	registry: {
		manifest: registryManifest,
		publishedSlugs: compiledStates.map((state) => state.slug),
		generatedAt,
		groups: buildRegistryGroups(compiledStates),
	},
	taxonomy,
};
```

Combined priority summary from `src/lib/site/candidate-priority.ts`:
```typescript
summary: {
	publishedRefreshCount: input.refreshQueue.entries.length,
	publishedRefreshDueCount: input.refreshQueue.entries.filter(
		(entry) => entry.priority === "due",
	).length,
	candidateCount: candidateIntake.summary.candidateCount,
	firstPublicationCandidateCount: firstPublicationCandidates.length,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate generated content and refresh artifacts</name>
  <files>generated/content-graph.json, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md, generated/refresh/state-priority-queue.json, generated/refresh/state-priority-queue.md</files>
  <read_first>
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-01-SUMMARY.md
    - content/data/state-candidate-intake.json
    - content/data/state-registry-manifest.json
    - content/data/README.md
    - scripts/compile-content.ts
    - scripts/build-refresh-queue.ts
    - generated/content-graph.json
    - generated/refresh/state-priority-queue.json
  </read_first>
  <action>Implement D-10 by running repo-owned generation scripts after Plan 16-01 canonical edits: `bun run validate:content`, then `bun run compile:content`, then `bun run refresh:queue`. Do not manually edit files under `generated/`; if generated output is wrong, fix the canonical content/data input or the existing generator behavior that produced it. Do not modify routes, components, catalog, cluster, comparison, browser-surface QA, package files, or lockfiles in this phase.</action>
  <action>After regeneration, run a generated-outcome check. If both `content/states/south-dakota-hb-1155.md` and `content/states/wyoming-hb-0201.md` exist, `generated/content-graph.json.states.length` must be `17`, `registry.publishedSlugs` must include `south-dakota` and `wyoming`, `generated/refresh/state-priority-queue.json.summary.candidateCount` must be `0`, and `firstPublicationCandidates.length` must be `0`. If either Markdown file does not exist because Plan 16-01 deferred a candidate, the graph state count must equal the number of manifest rows marked `published`, and each remaining candidate must be `readiness: "defer"` with `nextAction: "defer-until-stronger-official-source"`.</action>
  <verify>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bun -e 'const graph = await Bun.file("generated/content-graph.json").json(); const manifest = await Bun.file("content/data/state-registry-manifest.json").json(); const intake = await Bun.file("content/data/state-candidate-intake.json").json(); const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const publishedManifestCount = manifest.states.filter((entry) => entry.registryStatus === "published").length; if (graph.states.length !== publishedManifestCount) throw new Error(`Graph states ${graph.states.length} !== published manifest rows ${publishedManifestCount}`); const sdExists = await Bun.file("content/states/south-dakota-hb-1155.md").exists(); const wyExists = await Bun.file("content/states/wyoming-hb-0201.md").exists(); if (sdExists && wyExists) { for (const slug of ["south-dakota","wyoming"]) { if (!graph.registry.publishedSlugs.includes(slug)) throw new Error(`Missing published slug ${slug}`); } if (graph.states.length !== 17) throw new Error(`Expected 17 states after both publish, got ${graph.states.length}`); if (intake.candidates.length !== 0) throw new Error("Expected empty candidate intake after both publish"); if (priority.summary.candidateCount !== 0 || priority.firstPublicationCandidates.length !== 0) throw new Error("Expected zero generated candidate work after both publish"); } else { for (const entry of intake.candidates) { if (entry.readiness !== "defer" || entry.nextAction !== "defer-until-stronger-official-source" || !entry.deferralReason) throw new Error(`Deferred candidate ${entry.slug} is incomplete`); } } console.log(`Generated graph states: ${graph.states.length}; candidates: ${intake.candidates.length}; first-publication candidates: ${priority.firstPublicationCandidates.length}`);'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate:content` exits 0.
    - `bun run compile:content` exits 0.
    - `bun run refresh:queue` exits 0.
    - If both states publish, `generated/content-graph.json` contains exactly 17 state entries and `registry.publishedSlugs` includes `south-dakota` and `wyoming`.
    - If both states publish, `generated/refresh/state-priority-queue.json.summary.candidateCount` is `0` and `generated/refresh/state-priority-queue.json.firstPublicationCandidates.length` is `0`.
    - If either state is deferred, generated artifacts match the manifest published count and every remaining candidate is a complete `defer` row.
  </acceptance_criteria>
  <done>Generated content and refresh artifacts are regenerated from canonical inputs and match the final Phase 16 publish-or-defer outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update focused tests for expanded counts and empty candidate work</name>
  <files>src/lib/site/content.test.ts, src/lib/site/candidate-priority.test.ts</files>
  <read_first>
    - src/lib/site/content.test.ts
    - src/lib/site/candidate-priority.test.ts
    - src/lib/content/schema.test.ts
    - generated/content-graph.json
    - generated/refresh/state-priority-queue.json
    - content/data/state-candidate-intake.json
    - src/lib/site/candidate-priority.ts
    - src/lib/content/schema.ts
  </read_first>
  <behavior>
    - If both states publish, `src/lib/site/content.test.ts` expects `publishedCount` 17, region counts `midwest: 7`, `northeast: 1`, `south: 6`, `west: 3`, and failed status count 6.
    - If both states publish, `src/lib/site/candidate-priority.test.ts` covers an empty candidate list and verifies `candidateCount: 0`, `readyToAuthorCount: 0`, `needsStatusConfirmationCount: 0`, `deferredCount: 0`, `authorableCount: 0`, and `firstPublicationCandidateCount: 0`.
    - If a state is deferred, tests assert the actual generated graph count and complete deferred-candidate behavior without weakening schema or source-boundary checks.
  </behavior>
  <action>Implement D-11 using tests before production changes if a generator/model behavior change is needed. In the expected both-published outcome, update `src/lib/site/content.test.ts` count expectations from the 15-record Phase 15 graph to the 17-record Phase 16 graph: `expectedRegionCounts` becomes `{ midwest: 7, northeast: 1, south: 6, west: 3 }`, `model.stats.publishedCount` becomes `17`, and the legislative-status bucket expectation for failed records becomes `["failed", 6]`. Preserve the stale-copy regression test, canonical detail-link assertions, and selective comparison/cluster behavior. If the actual Plan 16-01 outcome deferred one candidate, derive the count expectations from the generated graph and add assertions proving the deferred row stays out of `content/states/` and out of public `publishedSlugs`.</action>
  <action>Add or update a focused `src/lib/site/candidate-priority.test.ts` unit test for empty candidate intake after both states publish. The test should call `buildCandidatePriorityModel([], "2026-06-02T00:00:00.000Z")` and `buildCombinedPriorityQueueModel({ refreshQueue, candidates: [], generatedAt: "2026-06-02T00:00:00.000Z" })`, then assert zero candidate counts and an empty `firstPublicationCandidates` array. Use explicit `// Arrange`, `// Act`, and `// Assert` sections per Bright Builds testing standards. Do not change `src/lib/content/schema.ts` or weaken `src/lib/content/schema.test.ts`; the existing parser already allows an empty `candidates` array and enforces deferred-row reasons.</action>
  <verify>
    <automated>bun test src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts src/lib/content/schema.test.ts</automated>
    <automated>bun -e 'const graph = await Bun.file("generated/content-graph.json").json(); const manifest = await Bun.file("content/data/state-registry-manifest.json").json(); const intake = await Bun.file("content/data/state-candidate-intake.json").json(); const priority = await Bun.file("generated/refresh/state-priority-queue.json").json(); const publishedManifestCount = manifest.states.filter((entry) => entry.registryStatus === "published").length; if (graph.states.length !== publishedManifestCount) throw new Error(`Generated graph states ${graph.states.length} !== published manifest rows ${publishedManifestCount}`); const tracked = [["south-dakota", "content/states/south-dakota-hb-1155.md"], ["wyoming", "content/states/wyoming-hb-0201.md"]]; const fileStates = await Promise.all(tracked.map(async ([slug, filePath]) => ({ slug, filePath, exists: await Bun.file(filePath).exists() }))); const bothPublished = fileStates.every((entry) => entry.exists); if (bothPublished) { if (graph.states.length !== 17) throw new Error(`Expected 17 states for both-published tests, got ${graph.states.length}`); for (const { slug } of fileStates) { if (!graph.registry.publishedSlugs.includes(slug)) throw new Error(`Missing published slug ${slug}`); } if (priority.summary.candidateCount !== 0 || priority.firstPublicationCandidates.length !== 0) throw new Error("Expected zero generated candidate work after both publish"); } else { for (const { slug, exists } of fileStates) { if (exists) continue; const row = manifest.states.find((entry) => entry.slug === slug); const maybeCandidate = intake.candidates.find((entry) => entry.slug === slug); if (graph.registry.publishedSlugs.includes(slug)) throw new Error(`${slug} is deferred but appears in public publishedSlugs`); if (row?.registryStatus !== "unresearched") throw new Error(`${slug} deferred manifest row must remain unresearched`); if (!maybeCandidate) throw new Error(`${slug} missing deferred candidate row`); if (maybeCandidate.readiness !== "defer" || maybeCandidate.nextAction !== "defer-until-stronger-official-source" || !maybeCandidate.deferralReason?.trim()) throw new Error(`${slug} deferred candidate row is incomplete`); } }'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md && -f content/states/wyoming-hb-0201.md ]]; then rg -n "publishedCount\\)\\.toBe\\(17\\)" src/lib/site/content.test.ts; rg -n "midwest: 7" src/lib/site/content.test.ts; rg -n "northeast: 1" src/lib/site/content.test.ts; rg -n "south: 6" src/lib/site/content.test.ts; rg -n "west: 3" src/lib/site/content.test.ts; rg -n "\\[\"failed\", 6\\]" src/lib/site/content.test.ts; rg -n "candidateCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "readyToAuthorCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "needsStatusConfirmationCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "deferredCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "authorableCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "firstPublicationCandidateCount: 0" src/lib/site/candidate-priority.test.ts; rg -n "buildCandidatePriorityModel\\(\\[\\]" src/lib/site/candidate-priority.test.ts; else rg -n "publishedManifestCount|graph\\.states\\.length|generated/content-graph\\.json" src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts; rg -n "publishedSlugs" src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts; rg -n "unresearched" src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts; rg -n "readiness.*defer|defer-until-stronger-official-source" src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts; rg -n "deferralReason" src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/lib/site/content.test.ts src/lib/site/candidate-priority.test.ts src/lib/content/schema.test.ts` exits 0.
    - Generated graph state count equals the count of manifest rows with `registryStatus: "published"` for both the published and deferral outcomes.
    - If both South Dakota and Wyoming files exist, Task 2 requires the 17-state test markers: `publishedCount).toBe(17)`, region counts `midwest: 7`, `northeast: 1`, `south: 6`, `west: 3`, failed bucket `["failed", 6]`, and candidate-priority zero-work markers including `candidateCount: 0` and `firstPublicationCandidateCount: 0`.
    - If either South Dakota or Wyoming file does not exist, Task 2 does not require the both-published markers; instead, tests must cover the deferral path or generated boundary by asserting graph-to-manifest count agreement, no unpublished South Dakota/Wyoming slug in `publishedSlugs`, an `unresearched` manifest row for each unpublished tracked state, and a complete candidate row with `readiness: "defer"`, `nextAction: "defer-until-stronger-official-source"`, and non-empty `deferralReason`.
    - No test removes the stale hard-coded count regression or canonical detail-link assertions from `src/lib/site/content.test.ts`.
  </acceptance_criteria>
  <done>Focused tests prove the final generated registry count and candidate-priority empty-work behavior without reducing schema or freshness coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification, code review, and lifecycle gates</name>
  <files>.planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md</files>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - package.json
    - scripts/precommit.ts
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-CONTEXT.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-RESEARCH.md
    - .planning/phases/16-throughput-expansion-and-deferral-ledger/16-01-SUMMARY.md
  </read_first>
  <action>Implement D-12 after Tasks 1 and 2. Re-run official-source spot checks for South Dakota and Wyoming, then run the repo verification commands in this order: `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bunx tsc --noEmit`, `bun test`, `bun run build`, and `bun run precommit`. Include `bun run format:check` and `bun run lint` as explicit precommit constituents if `bun run precommit` does not already print them clearly. Do not scope in Phase 17 catalog/cluster/comparison/browser-surface QA.</action>
  <action>Run the code review workflow for the Phase 16 changed files before declaring the phase clean: `/gsd-code-review 16 --depth=standard --files=content/states/south-dakota-hb-1155.md,content/states/wyoming-hb-0201.md,content/data/state-registry-manifest.json,content/data/state-candidate-intake.json,generated/content-graph.json,generated/refresh/state-refresh-queue.json,generated/refresh/state-refresh-queue.md,generated/refresh/state-priority-queue.json,generated/refresh/state-priority-queue.md,src/lib/site/content.test.ts,src/lib/site/candidate-priority.test.ts`. The workflow must produce `.planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md`; if findings are nonzero, fix them or route through the review-fix workflow before final verification. Run lifecycle validation with the shared phase lifecycle id `16-2026-06-02T12-12-22` and mode `yolo`.</action>
  <verify>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/south-dakota-hb-1155.md ]]; then sd_vote=$(curl -fsSL https://sdlegislature.gov/api/Votes/83383); printf "%s\n" "$sd_vote" | rg "\"StatusText\":\"Deferred to the 41st legislative day\""; printf "%s\n" "$sd_vote" | rg "\"Yeas\":10"; printf "%s\n" "$sd_vote" | rg "\"Nays\":3"; fi'</automated>
    <automated>bash -euo pipefail -c 'if [[ -f content/states/wyoming-hb-0201.md ]]; then wy_info=$(curl -fsSL "https://api.wyoleg.gov/v1/odata/BillInformations?select=BillNum,ShortTitle,Year,ChapterNo,Sponsor,EnrolledNo,LastActionDate,LastAction,SignedDate,EffectiveDate,BillType,SpecialSessionValue,BillStatus&filter=Year%20eq%202025%20and%20SpecialSessionValue%20eq%20null%20and%20BillNum%20eq%20%27HB0201%27"); printf "%s\n" "$wy_info" | rg "HB0201"; printf "%s\n" "$wy_info" | rg "inactive"; printf "%s\n" "$wy_info" | rg "H:Died in Committee Returned Bill Pursuant to HR 5-4"; fi'</automated>
    <automated>bun -e 'import { parseStateCandidateIntake, parseStateRegistryManifest } from "./src/lib/content/schema.ts"; const manifest = parseStateRegistryManifest(await Bun.file("content/data/state-registry-manifest.json").json()); const intake = parseStateCandidateIntake(await Bun.file("content/data/state-candidate-intake.json").json()); const rows = new Map(manifest.states.map((entry) => [entry.slug, entry])); for (const [slug, filePath] of [["south-dakota", "content/states/south-dakota-hb-1155.md"], ["wyoming", "content/states/wyoming-hb-0201.md"]]) { const fileExists = await Bun.file(filePath).exists(); const row = rows.get(slug); const maybeCandidate = intake.candidates.find((entry) => entry.slug === slug); if (fileExists) { if (row?.registryStatus !== "published") throw new Error(`${slug} public file exists but manifest row is not published`); if (maybeCandidate) throw new Error(`${slug} public file exists but candidate row remains`); } else { if (row?.registryStatus !== "unresearched") throw new Error(`${slug} deferred row must keep manifest unresearched`); if (!maybeCandidate) throw new Error(`${slug} deferred row must remain in candidate intake`); if (maybeCandidate.readiness !== "defer") throw new Error(`${slug} deferred candidate must use readiness defer`); if (maybeCandidate.nextAction !== "defer-until-stronger-official-source") throw new Error(`${slug} deferred candidate has wrong nextAction`); if (!maybeCandidate.deferralReason?.trim()) throw new Error(`${slug} deferred candidate is missing deferralReason`); } }'</automated>
    <automated>bun run validate:content</automated>
    <automated>bun run compile:content</automated>
    <automated>bun run refresh:queue</automated>
    <automated>bunx tsc --noEmit</automated>
    <automated>bun test</automated>
    <automated>bun run build</automated>
    <automated>bun run precommit</automated>
    <automated>node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 16 --expect-id 16-2026-06-02T12-12-22 --expect-mode yolo --require-plans --raw</automated>
    <automated>test -f .planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md</automated>
    <automated>bash -euo pipefail -c 'review_status=$(node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md --field status); if [[ "$review_status" != *clean* && "$review_status" != *skipped* ]]; then printf "Unexpected review status: %s\n" "$review_status"; exit 1; fi'</automated>
    <automated>test -z "$(git diff --name-only -- src/routes src/components src/styles package.json bun.lock)"</automated>
  </verify>
  <acceptance_criteria>
    - Official South Dakota spot check confirms `Deferred to the 41st legislative day`, Yeas `10`, and Nays `3`, or the South Dakota row is deferred with a deferral reason from Plan 16-01.
    - Official Wyoming spot check confirms `inactive` and `H:Died in Committee Returned Bill Pursuant to HR 5-4`, or the Wyoming row is deferred with a deferral reason from Plan 16-01.
    - `bun run validate:content` exits 0.
    - `bun run compile:content` exits 0.
    - `bun run refresh:queue` exits 0.
    - `bunx tsc --noEmit` exits 0.
    - `bun test` exits 0.
    - `bun run build` exits 0.
    - `bun run precommit` exits 0.
    - `.planning/phases/16-throughput-expansion-and-deferral-ledger/16-REVIEW.md` exists with frontmatter `status: clean` or `status: skipped`.
    - Lifecycle validation exits 0 for `--expect-id 16-2026-06-02T12-12-22 --expect-mode yolo --require-plans`.
    - `test -z "$(git diff --name-only -- src/routes src/components src/styles package.json bun.lock)"` exits 0.
  </acceptance_criteria>
  <done>Phase 16 verification, code review evidence, and lifecycle metadata are clean, with Phase 17 browser-surface QA left out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical content/data -> generated artifacts | Authored state entries, manifest rows, and candidate intake become generated public route data and maintainer queues. |
| generated artifacts -> tests/build | Generated graph and priority outputs are imported by tests and build-time route models. |
| phase execution -> lifecycle/review artifacts | Local execution outputs become planning evidence consumed by GSD verification and Phase 17. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | `generated/content-graph.json` | mitigate | Regenerate only through `bun run compile:content` and verify graph count equals published manifest rows. |
| T-16-02-02 | Tampering | `generated/refresh/state-priority-queue.json` | mitigate | Regenerate through `bun run refresh:queue` and verify zero first-publication candidates when both states publish, or complete deferred rows when any candidate remains. |
| T-16-02-03 | Repudiation | verification evidence | mitigate | Run official spot checks, content validation, compile, refresh generation, TypeScript, tests, build, precommit, code review, and lifecycle validation. |
| T-16-02-04 | Information Disclosure | public/private candidate boundary | mitigate | Verify deferred candidates stay in `content/data/state-candidate-intake.json` only and do not appear as public Markdown or published graph slugs. |
| T-16-02-05 | Denial of Service | static build route generation | mitigate | Run `bun run build` after generated graph refresh to catch route/model failures before Phase 17 QA. |
</threat_model>

<verification>
Run after all tasks:

```bash
bash -euo pipefail -c 'sd_vote=$(curl -fsSL https://sdlegislature.gov/api/Votes/83383); printf "%s\n" "$sd_vote" | rg "\"StatusText\":\"Deferred to the 41st legislative day\""; printf "%s\n" "$sd_vote" | rg "\"Yeas\":10"; printf "%s\n" "$sd_vote" | rg "\"Nays\":3"'
bash -euo pipefail -c 'wy_info=$(curl -fsSL "https://api.wyoleg.gov/v1/odata/BillInformations?select=BillNum,ShortTitle,Year,ChapterNo,Sponsor,EnrolledNo,LastActionDate,LastAction,SignedDate,EffectiveDate,BillType,SpecialSessionValue,BillStatus&filter=Year%20eq%202025%20and%20SpecialSessionValue%20eq%20null%20and%20BillNum%20eq%20%27HB0201%27"); printf "%s\n" "$wy_info" | rg "HB0201"; printf "%s\n" "$wy_info" | rg "inactive"; printf "%s\n" "$wy_info" | rg "H:Died in Committee Returned Bill Pursuant to HR 5-4"'
bun run validate:content
bun run compile:content
bun run refresh:queue
bunx tsc --noEmit
bun test
bun run build
bun run precommit
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 16 --expect-id 16-2026-06-02T12-12-22 --expect-mode yolo --require-plans --raw
```

After `.planning/phases/16-throughput-expansion-and-deferral-ledger/16-VERIFICATION.md` exists, run:

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 16 --expect-id 16-2026-06-02T12-12-22 --expect-mode yolo --require-plans --require-verification --raw
```
</verification>

<success_criteria>
- Generated artifacts match the final Plan 16-01 publish-or-defer outcome.
- If both source gates pass, the graph has 17 states, South Dakota and Wyoming are published slugs, candidate intake is empty, and generated first-publication candidates are zero.
- If a source gate fails, the deferred candidate remains outside public state content with a concrete deferral reason and generated priority artifacts reflect that deferral.
- Focused tests cover updated counts and empty candidate work where applicable.
- `bun run validate:content`, `bun run compile:content`, `bun run refresh:queue`, `bunx tsc --noEmit`, `bun test`, `bun run build`, `bun run precommit`, code review artifact validation, and lifecycle validation pass.
- No Phase 17 catalog/cluster/comparison/browser-surface QA or UI changes are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-throughput-expansion-and-deferral-ledger/16-02-SUMMARY.md`.
</output>
