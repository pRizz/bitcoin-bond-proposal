---
phase: 01-foundation-and-trust-contracts
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/content/schema.ts
  - src/lib/content/load-markdown.ts
  - scripts/validate-content.ts
  - scripts/compile-content.ts
  - generated/.gitkeep
autonomous: true
requirements:
  - CATA-03
user_setup: []
must_haves:
  truths:
    - Canonical Markdown and data files are validated at the boundary before any public or PDF output is generated.
    - Proposal kind and subtype rules are enforced by shared schemas rather than repeated ad hoc checks.
    - The repo can compile validated content into generated read models without parsing raw Markdown in later phases.
  artifacts:
    - src/lib/content/schema.ts
    - src/lib/content/load-markdown.ts
    - scripts/validate-content.ts
    - scripts/compile-content.ts
    - generated/.gitkeep
  key_links:
    - validate-content consumes the controlled taxonomy artifact from content/data.
    - compile-content runs only on content that has already passed boundary validation.
---

<objective>
Implement the shared schema and content-processing layer for Phase 1, including frontmatter parsing, publishability validation, and generated content compilation.

Purpose: Make the trust contract executable and reusable before the project starts authoring public entries or generating PDFs from canonical content.
Output: Shared content schemas plus validate and compile scripts that later plans and phases can rely on.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-trust-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-trust-contracts/01-RESEARCH.md
@content/data/proposal-taxonomy.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared content schemas and loaders</name>
  <files>src/lib/content/schema.ts, src/lib/content/load-markdown.ts</files>
  <action>Build the shared content boundary around Markdown frontmatter and controlled data artifacts. Model the canonical document and proposal-entry shapes with Zod. Ensure the loader code parses frontmatter once and hands the rest of the system domain-friendly structures rather than raw maps. Keep the schema strict but small, focused only on the fields locked in the phase context.</action>
  <verify>bunx tsc --noEmit</verify>
  <done>There is a single shared schema layer that future document, catalog, and PDF work can depend on.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce publishability and metadata validation</name>
  <files>scripts/validate-content.ts</files>
  <action>Create a validation script that checks required metadata, taxonomy values, date fields, and publishability gates. Treat incomplete or ambiguous canonical public entries as failures. Make the script fail loudly and visibly instead of silently skipping bad content. Keep validation aligned to the methodology and publishability decisions from the phase context.</action>
  <verify>bun run validate:content</verify>
  <done>The repo has a deterministic validation gate that protects canonical content quality.</done>
</task>

<task type="auto">
  <name>Task 3: Compile validated content into generated read models</name>
  <files>scripts/compile-content.ts, generated/.gitkeep</files>
  <action>Create the compile step that normalizes validated content into generated artifacts for later site consumption. Keep the generated output intentionally simple in Phase 1: enough to prove the contract and avoid runtime parsing, but not so elaborate that the phase overbuilds the future site. Ensure the script only compiles from already validated content.</action>
  <verify>bun run compile:content</verify>
  <done>The repo can generate a stable derived content layer from canonical sources.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install`
- [ ] `bunx tsc --noEmit`
- [ ] `bun run validate:content`
- [ ] `bun run compile:content`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical content validation and compilation work as the trust boundary for later phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-trust-contracts/01-02-SUMMARY.md`
</output>
