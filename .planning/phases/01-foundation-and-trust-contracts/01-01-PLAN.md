---
phase: 01-foundation-and-trust-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - bunfig.toml
  - .gitignore
  - content/data/proposal-taxonomy.json
  - content/data/README.md
autonomous: true
requirements:
  - CATA-03
user_setup: []
must_haves:
  truths:
    - The repo has a Bun/TypeScript workspace shape that later plans can build on without re-bootstrap work.
    - Canonical content directories and baseline taxonomy artifacts exist in the repo.
    - Phase 1 work has an explicit place for canonical sources versus derived artifacts.
  artifacts:
    - package.json
    - tsconfig.json
    - bunfig.toml
    - .gitignore
    - content/data/proposal-taxonomy.json
    - content/data/README.md
  key_links:
    - package.json exposes scripts that later plans can implement instead of inventing new entrypoints.
    - content/data/proposal-taxonomy.json reflects the controlled taxonomy locked in 01-CONTEXT.md.
---

<objective>
Bootstrap the repository into a Bun/TypeScript content workspace with the canonical directories and baseline taxonomy artifacts needed for the rest of Phase 1.

Purpose: Create a stable filesystem and toolchain foundation before schemas, docs, and PDF workflows are layered on top.
Output: Workspace config, canonical content directories, and seed taxonomy/readme artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-trust-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-trust-contracts/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Bun and TypeScript workspace</name>
  <files>package.json, tsconfig.json, bunfig.toml, .gitignore</files>
  <action>Create the initial Bun-based workspace configuration for a content-first repo. Add the core dependencies and script names that later plans will fill in, including validation, compile, PDF, and pre-commit entrypoints. Keep the config minimal and aligned with the repo's current greenfield state. Add ignore rules for ephemeral generated intermediates while keeping the required packet PDFs commit-friendly.</action>
  <verify>test -f package.json && test -f tsconfig.json && test -f bunfig.toml</verify>
  <done>The repo has a coherent package/tooling baseline and no later Phase 1 plan needs to invent its own bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical content directory skeleton</name>
  <files>content/data/README.md</files>
  <action>Create the canonical top-level content layout implied by the phase context: docs, states, explainers, and data. Seed the structure with lightweight tracked files only where needed to make the layout durable in git. Make the README in content/data explain the role of controlled vocabularies, manifests, and generated inputs so later plans do not blur authored versus derived content.</action>
  <verify>test -d content/docs && test -d content/states && test -d content/explainers && test -d content/data</verify>
  <done>The canonical content tree exists and clearly distinguishes authored content from future generated artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the locked proposal taxonomy artifact</name>
  <files>content/data/proposal-taxonomy.json</files>
  <action>Encode the Phase 1 taxonomy decisions into a small controlled artifact that lists allowed proposal kinds and subtypes, plus short descriptions for authoring and validation use. Keep the artifact intentionally small and aligned to the exact controlled set from the context file rather than anticipating future taxonomy sprawl.</action>
  <verify>rg -n '\"reserve\"|\"bond\"|\"both\"|\"strategic-reserve\"|\"bitcoin-linked-bond\"' content/data/proposal-taxonomy.json</verify>
  <done>The repo contains one explicit, reusable source for the reserve-vs-bond classification vocabulary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install`
- [ ] `bunx tsc --showConfig >/dev/null`
- [ ] `test -d content/docs && test -d content/states && test -d content/explainers && test -d content/data`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a concrete workspace and content skeleton that later plans can depend on
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-trust-contracts/01-01-SUMMARY.md`
</output>
