---
phase: 01-foundation-and-trust-contracts
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - scripts/build-pdf.ts
  - scripts/precommit.ts
  - .githooks/pre-commit
  - pdf/.gitkeep
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - The required packet docs can be turned into PDFs from canonical Markdown without hand editing generated files.
    - Pre-commit enforces the narrow Phase 1 trust contract and fails on broken or stale packet artifacts.
    - The PDF and pre-commit workflow is scoped to the packet and content boundary rather than the whole future site.
  artifacts:
    - scripts/build-pdf.ts
    - scripts/precommit.ts
    - .githooks/pre-commit
    - pdf/.gitkeep
  key_links:
    - build-pdf consumes canonical Markdown from content/docs rather than separate PDF-only source files.
    - precommit runs validation, compile, and required PDF generation in the agreed sequence.
---

<objective>
Implement the packet PDF generation flow and the repo-owned pre-commit workflow that enforces Phase 1’s validation and artifact rules.

Purpose: Make the trust contract operational and ensure the repo always has handoff-ready packet artifacts derived from canonical sources.
Output: PDF builder, pre-commit orchestrator, repo-owned hook, and committed packet-output directory scaffold.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-trust-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-trust-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-trust-contracts/01-02-PLAN.md
@.planning/phases/01-foundation-and-trust-contracts/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Markdown-to-PDF packet renderer</name>
  <files>scripts/build-pdf.ts, pdf/.gitkeep</files>
  <action>Implement the Phase 1 PDF build around Playwright-driven HTML-to-PDF rendering. Scope it narrowly to the three required packet docs. Reuse canonical Markdown sources from content/docs and keep renderer-specific logic behind the build script so the source contract stays stable even if the renderer changes later.</action>
  <verify>bun run build:pdf</verify>
  <done>The repo can generate the required packet PDFs from canonical Markdown in a deterministic way.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the pre-commit orchestrator</name>
  <files>scripts/precommit.ts</files>
  <action>Create a pre-commit entrypoint that runs the narrow Phase 1 sequence: validate content, compile generated content, build required PDFs, and fail fast on any broken gate. Do not let this script sprawl into a full site-build or later-phase task runner.</action>
  <verify>bun run precommit</verify>
  <done>The project has one deterministic pre-commit workflow aligned to Phase 1 trust rules.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the repo-owned git hook path</name>
  <files>.githooks/pre-commit</files>
  <action>Create a repo-owned pre-commit hook that invokes the orchestrator script and document the expectation through the implementation itself. Use git’s repo-owned hook path approach instead of an opaque workstation-only hook. Keep the hook tiny and make the script do the real work.</action>
  <verify>test -x .githooks/pre-commit && git config core.hooksPath .githooks && .githooks/pre-commit</verify>
  <done>The hook path is repo-owned, executable, and enforces the agreed pre-commit checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run validate:content`
- [ ] `bun run compile:content`
- [ ] `bun run build:pdf`
- [ ] `bun run precommit`
- [ ] `test -f pdf/illinois-one-pager.pdf && test -f pdf/illinois-draft-bill.pdf && test -f pdf/methodology.pdf`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo enforces and produces the required Phase 1 packet artifacts from canonical sources
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-trust-contracts/01-04-SUMMARY.md`
</output>
