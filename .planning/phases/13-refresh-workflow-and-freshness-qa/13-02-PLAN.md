---
phase: 13-refresh-workflow-and-freshness-qa
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/lib/site/content.ts
  - src/components/editorial/RegistryFreshnessPanel.tsx
  - src/components/editorial/StateCard.tsx
  - src/routes/(site)/states/index.tsx
  - src/routes/(site)/states/clusters.tsx
  - src/routes/(site)/states/compare.tsx
  - src/routes/(site)/states/[slug].tsx
  - src/lib/site/content.test.ts
autonomous: true
requirements:
  - REFR-03
must_haves:
  truths:
    - Reader sees consistent freshness summaries on the catalog, cluster, and comparison routes.
    - State cards show explicit `status as of` and `last reviewed` dates across grouped registry surfaces.
    - State detail pages explain freshness using the same shared rule set as the maintainer queue.
  artifacts:
    - path: src/lib/site/content.ts
      provides: Shared freshness-summary accessors for public routes plus state-level freshness cues.
      exports:
        - getRegistryFreshnessSummary
    - path: src/components/editorial/RegistryFreshnessPanel.tsx
      provides: Shared route-level freshness summary panel.
      contains: Freshness
    - path: src/components/editorial/StateCard.tsx
      provides: Shared card-level freshness dates for registry surfaces.
      contains: Status as of
    - path: src/routes/(site)/states/index.tsx
      provides: Catalog freshness summary and explicit freshness dates on cards.
      contains: Freshness
    - path: src/routes/(site)/states/clusters.tsx
      provides: Cluster-surface freshness summary plus shared card freshness dates.
      contains: Freshness
    - path: src/routes/(site)/states/compare.tsx
      provides: Comparison-surface freshness summary plus shared card freshness dates.
      contains: Freshness
    - path: src/routes/(site)/states/[slug].tsx
      provides: Detail-page freshness explanation panel aligned with shared thresholds.
      contains: Freshness
  key_links:
    - from: src/routes/(site)/states/index.tsx
      to: src/components/editorial/RegistryFreshnessPanel.tsx
      via: shared route-level freshness summary
      pattern: RegistryFreshnessPanel
    - from: src/routes/(site)/states/index.tsx
      to: src/components/editorial/StateCard.tsx
      via: shared state-card freshness dates
      pattern: statusAsOf
    - from: src/routes/(site)/states/[slug].tsx
      to: src/lib/site/content.ts
      via: detail freshness cue from shared content model
      pattern: freshnessCue
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-04-12T01-19-57
generated_at: 2026-04-12T01:19:57.768Z
---

<objective>
Apply the shared freshness contract across the public registry surfaces so readers can
see dated-snapshot cues on the catalog, grouped routes, comparison route, and canonical
state detail pages.

Purpose: Satisfy `REFR-03` by turning the Phase 13 freshness logic into visible public
trust cues without creating live-tracker behavior.
Output: Shared freshness summary panel, card-level exact dates, detail-page freshness
panel, and route wiring across `/states`, `/states/clusters`, `/states/compare`, and
`/states/[slug]`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-refresh-workflow-and-freshness-qa/13-CONTEXT.md
@.planning/phases/13-refresh-workflow-and-freshness-qa/13-RESEARCH.md
@.planning/phases/13-refresh-workflow-and-freshness-qa/13-01-PLAN.md
@.planning/phases/11-broader-national-publication-batch/11-02-SUMMARY.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-01-SUMMARY.md
@.planning/phases/12-cluster-and-comparison-surfaces/12-02-SUMMARY.md
@src/lib/site/content.ts
@src/components/editorial/StateCard.tsx
@src/routes/(site)/states/index.tsx
@src/routes/(site)/states/clusters.tsx
@src/routes/(site)/states/compare.tsx
@src/routes/(site)/states/[slug].tsx
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread shared freshness summaries and cues through the site model</name>
  <files>src/lib/site/content.ts, src/lib/site/content.test.ts</files>
  <behavior>
    - Test 1: state detail and catalog selectors expose the same freshness cue for a shared state record.
    - Test 2: routes can access one registry-level freshness summary without recomputing counts locally.
  </behavior>
  <action>Read `src/lib/site/content.ts`, `src/lib/site/registry-freshness.ts`, and `src/lib/site/content.test.ts` first. Then thread the shared freshness cue and registry-level freshness summary through the public content model so routes can render consistent public cues without reimplementing threshold logic locally.</action>
  <verify>
    <automated>bun test src/lib/site/registry-freshness.test.ts src/lib/site/content.test.ts</automated>
  </verify>
  <done>The content layer exposes one route-ready freshness summary and one shared state-level freshness cue.</done>
</task>

<task type="auto">
  <name>Task 2: Apply freshness summaries and dates across the public routes</name>
  <files>src/components/editorial/RegistryFreshnessPanel.tsx, src/components/editorial/StateCard.tsx, src/routes/(site)/states/index.tsx, src/routes/(site)/states/clusters.tsx, src/routes/(site)/states/compare.tsx, src/routes/(site)/states/[slug].tsx</files>
  <action>Read the existing states routes and `StateCard` first. Then add one shared freshness-summary panel for the catalog and grouped routes, surface exact `status as of` and `last reviewed` dates on cards, and add a short freshness explanation panel on the state-detail route. Keep the public copy calm and explicit about dated snapshots; do not add live-tracker widgets, urgent alert language, or new route categories.</action>
  <verify>
    <automated>bun run build && rg -n 'Freshness|Status as of|Reviewed' src/components/editorial/StateCard.tsx src/routes/'(site)'/states/index.tsx src/routes/'(site)'/states/clusters.tsx src/routes/'(site)'/states/compare.tsx \"src/routes/(site)/states/[slug].tsx\"</automated>
  </verify>
  <done>Catalog, cluster, comparison, and detail surfaces all expose visible freshness cues using one shared contract.</done>
</task>

</tasks>
