---
phase: 13-refresh-workflow-and-freshness-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site/registry-freshness.ts
  - src/lib/site/registry-freshness.test.ts
  - src/lib/site/content.ts
  - scripts/build-refresh-queue.ts
  - package.json
  - scripts/precommit.ts
  - generated/refresh/README.md
  - generated/refresh/state-refresh-queue.json
  - generated/refresh/state-refresh-queue.md
autonomous: true
requirements:
  - REFR-01
must_haves:
  truths:
    - Maintainer can generate a repo-owned refresh queue from the existing compiled registry data without mutating canonical state content.
    - Refresh priority differentiates active legislative posture from terminal records through explicit thresholds rather than ad hoc judgment.
    - Refresh outputs live under `generated/refresh/` and regenerate through the existing Bun script workflow.
  artifacts:
    - path: src/lib/site/registry-freshness.ts
      provides: Shared freshness-threshold and refresh-queue model helpers.
      exports:
        - buildStateFreshnessCue
        - buildRegistryFreshnessSummary
        - buildRefreshQueueModel
    - path: src/lib/site/registry-freshness.test.ts
      provides: Unit coverage for freshness thresholds and queue ordering.
      contains: refresh queue
    - path: scripts/build-refresh-queue.ts
      provides: Repo-owned queue generation for `generated/refresh/`.
      contains: state-refresh-queue
    - path: generated/refresh/state-refresh-queue.json
      provides: Machine-readable refresh queue output for maintainers.
      contains: review due
    - path: generated/refresh/state-refresh-queue.md
      provides: Human-readable refresh queue report for maintainers.
      contains: Review Due
  key_links:
    - from: scripts/build-refresh-queue.ts
      to: src/lib/site/content.ts
      via: shared published-state and refresh-queue selectors
      pattern: getRefreshQueueModel
    - from: src/lib/site/content.ts
      to: src/lib/site/registry-freshness.ts
      via: shared freshness summary and queue helpers
      pattern: buildRefreshQueueModel
    - from: scripts/precommit.ts
      to: scripts/build-refresh-queue.ts
      via: repo-owned automation step
      pattern: refresh:queue
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-04-12T01-19-57
generated_at: 2026-04-12T01:19:57.768Z
---

<objective>
Implement the repo-owned refresh queue workflow so maintainers can regenerate an auditable
list of published records whose review dates or active legislative posture now need
attention.

Purpose: Satisfy `REFR-01` with one shared freshness rule set, one Bun generation script,
and committed refresh-only outputs under `generated/refresh/`.
Output: Shared freshness/queue helpers, unit coverage, refresh generation script, and
tracked queue artifacts wired into the existing pre-commit flow.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-refresh-workflow-and-freshness-qa/13-CONTEXT.md
@.planning/phases/13-refresh-workflow-and-freshness-qa/13-RESEARCH.md
@.planning/phases/10-coverage-expansion-contract-and-data-shape/10-CONTEXT.md
@.planning/phases/10-coverage-expansion-contract-and-data-shape/10-02-SUMMARY.md
@content/data/README.md
@content/data/state-registry-manifest.README.md
@generated/refresh/README.md
@scripts/compile-content.ts
@src/lib/site/content.ts
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared freshness and refresh-queue helpers with unit coverage</name>
  <files>src/lib/site/registry-freshness.ts, src/lib/site/registry-freshness.test.ts, src/lib/site/content.ts</files>
  <behavior>
    - Test 1: active records become review-due when their legislative status snapshot is stale even if they were reviewed recently.
    - Test 2: terminal records stay current when they were reviewed recently, even if the terminal status date is old.
    - Test 3: registry freshness summary and refresh queue counts stay deterministic and sort due items before soon items.
  </behavior>
  <action>Read `src/lib/site/content.ts`, `generated/content-graph.json`, and the Phase 10 freshness docs first. Then add one small pure helper module that classifies each published record into current / review soon / review due using explicit thresholds for active versus terminal records, builds a registry freshness summary, and builds a queue model. Thread those derived models through `src/lib/site/content.ts` so downstream scripts and routes reuse the same logic instead of inventing route-local rules.</action>
  <verify>
    <automated>bun test src/lib/site/registry-freshness.test.ts src/lib/site/content.test.ts</automated>
  </verify>
  <done>The repo has one tested freshness/queue rule set that every later surface can consume.</done>
</task>

<task type="auto">
  <name>Task 2: Generate refresh artifacts and wire them into the Bun workflow</name>
  <files>scripts/build-refresh-queue.ts, package.json, scripts/precommit.ts, generated/refresh/README.md, generated/refresh/state-refresh-queue.json, generated/refresh/state-refresh-queue.md</files>
  <action>Read `scripts/compile-content.ts`, `scripts/precommit.ts`, and `generated/refresh/README.md` first. Then add a repo-owned Bun script that writes both JSON and Markdown refresh-queue outputs under `generated/refresh/`, document the output files and regeneration command, and wire queue generation into the pre-commit flow immediately after content compilation. Keep the queue advisory only: it should never mutate canonical state content or imply live tracking.</action>
  <verify>
    <automated>bun run refresh:queue && rg -n 'Queue entries|Review Due|Review Soon' generated/refresh/state-refresh-queue.md</automated>
  </verify>
  <done>Maintainers can regenerate refresh-only queue artifacts from one Bun command, and the repo workflow keeps them current with the compiled graph.</done>
</task>

</tasks>
